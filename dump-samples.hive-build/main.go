package main

import "hiveapp/hive"


func clone_slides_0_Slide(v slides_0_Slide) slides_0_Slide {
	out := v
	return out
}

type slides_0_Example struct {
	Caption string
	CaptionPt string
	Code string
	CodePt string
}

type slides_0_Slide struct {
	Title string
	TitlePt string
	Explanation string
	ExplanationPt string
	Examples []slides_0_Example
	Download bool
	Shot string
}

func slides_0_all() []slides_0_Slide {
	return []slides_0_Slide{slides_0_Slide{Title: "Hello, World", TitlePt: "Olá, Mundo", Explanation: "Every Hive program starts at proc main(): void, in the file handed to `hive run` or `hive build`. echo prints a value and appends a newline, converting whatever it is given to text the way it would be shown.", ExplanationPt: "Todo programa Hive começa em proc main(): void, no arquivo passado para `hive run` ou `hive build`. echo imprime um valor e adiciona uma nova linha, convertendo o que quer que receba em texto da mesma forma que seria exibido.", Examples: []slides_0_Example{slides_0_Example{Caption: "", CaptionPt: "", Code: "proc main(): void {\n\techo \"it works\"\n}", CodePt: "proc main(): void {\n\techo \"funciona\"\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "Running these examples", TitlePt: "Rodando estes exemplos", Explanation: "Every example from here on is a complete program: put it in a file ending in .hive, and one command runs it. The compiler is a single executable with nothing to install beside it — no runtime, no package manager, no project file — so the download link below is the whole of the setup.\n\nhive check reads a program and reports what is wrong with it without building anything, which is why it is the one worth reaching for first. hive run compiles and runs in a single step, hive build leaves a native executable behind, hive test runs a file's tests and reports coverage, and hive container writes a Dockerfile that builds and runs the program. Every pass reports as much as it can find rather than stopping at the first thing, so a program with six mistakes is a program whose author gets told about six.", ExplanationPt: "Todo exemplo daqui em diante é um programa completo: coloque-o em um arquivo terminado em .hive, e um comando o roda. O compilador é um único executável sem nada para instalar ao lado dele — sem runtime, sem gerenciador de pacotes, sem arquivo de projeto — então o link de download abaixo é toda a configuração que existe.\n\nhive check lê um programa e reporta o que há de errado nele sem construir nada, e é por isso que vale a pena recorrer a ele primeiro. hive run compila e roda em um único passo, hive build deixa um executável nativo pronto, hive test roda os testes de um arquivo e reporta a cobertura, e hive container escreve um Dockerfile que constrói e roda o programa. Toda passagem reporta tudo que consegue encontrar em vez de parar na primeira coisa, então um programa com seis erros é um programa cujo autor é avisado sobre os seis.", Examples: []slides_0_Example{slides_0_Example{Caption: "", CaptionPt: "", Code: "$ printf 'proc main(): void {\\n\\techo \"it works\"\\n}\\n' > hello.hive\n\n$ hive check hello.hive\nNo problems found in hello.hive (0s)\n\n$ hive run hello.hive\nit works\n\n$ hive build hello.hive\nCompiled hello.hive -> hello (0s)", CodePt: "$ printf 'proc main(): void {\\n\\techo \"funciona\"\\n}\\n' > hello.hive\n\n$ hive check hello.hive\nNo problems found in hello.hive (0s)\n\n$ hive run hello.hive\nfunciona\n\n$ hive build hello.hive\nCompiled hello.hive -> hello (0s)"}}, Download: true, Shot: ""}, slides_0_Slide{Title: "Comments", TitlePt: "Comentários", Explanation: "A comment runs from // to the end of the line, and nothing more. There is no block comment form in Hive itself — inside a query body, which is SQL rather than Hive, SQL's own -- and /* */ are recognised instead.", ExplanationPt: "Um comentário vai de // até o fim da linha, e nada mais. Não existe forma de comentário em bloco no próprio Hive — dentro do corpo de uma query, que é SQL e não Hive, os -- e /* */ do próprio SQL são reconhecidos em seu lugar.", Examples: []slides_0_Example{slides_0_Example{Caption: "", CaptionPt: "", Code: "proc main(): void {\n\t// A comment. The whole line, and nothing of the next.\n\tx := 1 // ...or the tail of one.\n\techo x\n}", CodePt: "proc main(): void {\n\t// Um comentário. A linha toda, e nada da próxima.\n\tx := 1 // ...ou o resto de uma.\n\techo x\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "Names have shapes", TitlePt: "Nomes têm formas", Explanation: "A name's spelling says what it is, and each shape is enforced where the name is declared. camelCase names variables, parameters, fields and every proc/func/query. PascalCase names types, their variants, and atoms. UPPER_CASE is how a constant is written: a variable nothing reassigns. Keywords are lower case, and only lower case — a keyword spelled in another casing, like Proc or IF, is a compile error naming both readings rather than a usable identifier.", ExplanationPt: "A grafia de um nome diz o que ele é, e cada forma é obrigatória no lugar onde o nome é declarado. camelCase nomeia variáveis, parâmetros, campos e todo proc/func/query. PascalCase nomeia tipos, suas variantes e átomos. UPPER_CASE é como se escreve uma constante: uma variável que nada reatribui. Palavras-chave são em minúsculas, e somente em minúsculas — uma palavra-chave grafada de outra forma, como Proc ou IF, é um erro de compilação que aponta as duas leituras em vez de um identificador utilizável.", Examples: []slides_0_Example{slides_0_Example{Caption: "", CaptionPt: "", Code: "type Shape {\n\tCircle { radius: Int }\n}\n\nproc main(): void {\n\tname := \"bee\"             // camelCase: a variable\n\tMAX_RETRIES := 5          // UPPER_CASE: a constant\n\tshape := Shape.Circle(4)  // PascalCase: a type and its variant\n\n\techo \"{name}, retry up to {MAX_RETRIES} times, shape={shape}\"\n}", CodePt: "type Shape {\n\tCircle { radius: Int }\n}\n\nproc main(): void {\n\tname := \"bee\"             // camelCase: uma variável\n\tMAX_RETRIES := 5          // UPPER_CASE: uma constante\n\tshape := Shape.Circle(4)  // PascalCase: um tipo e sua variante\n\n\techo \"{name}, tente até {MAX_RETRIES} vezes, forma={shape}\"\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "Variables and constants", TitlePt: "Variáveis e constantes", Explanation: ":= infers a type from the value, including a static vector length. A type written out front with = states it instead, which is the only way to say [dyn], or to give a value nothing else says the shape of somewhere to land. A binding is immutable unless it says mut, and a mut variable may never be spelled UPPER_CASE — that shape is reserved for something nothing reassigns.", ExplanationPt: ":= infere um tipo a partir do valor, incluindo um tamanho estático de vetor. Um tipo escrito explicitamente na frente com = o declara em vez disso, que é a única forma de dizer [dyn], ou de dar uma forma a um valor que, sozinho, não determina nenhuma. Uma ligação é imutável a menos que diga mut, e uma variável mut nunca pode ser grafada em UPPER_CASE — essa forma é reservada para algo que nada reatribui.", Examples: []slides_0_Example{slides_0_Example{Caption: "", CaptionPt: "", Code: "proc main(): void {\n\tx := 1                     // inferred, immutable\n\tmut count := 0              // inferred, reassignable\n\tmut Str[dyn] rows = []       // annotated: says \"dynamic\"\n\tMAX := 100                  // a constant, by its shape alone\n\n\tcount = count + 1\n\tappend(rows, \"first\")\n\techo \"{x} {count} {rows} {MAX}\"\n}", CodePt: "proc main(): void {\n\tx := 1                     // inferido, imutável\n\tmut count := 0              // inferido, reatribuível\n\tmut Str[dyn] rows = []       // anotado: diz \"dinâmico\"\n\tMAX := 100                  // uma constante, só pela forma\n\n\tcount = count + 1\n\tappend(rows, \"first\")\n\techo \"{x} {count} {rows} {MAX}\"\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "Scalar types", TitlePt: "Tipos escalares", Explanation: "Str holds UTF-8 text. Int is a 64-bit signed integer whose overflow wraps silently. Float is a 64-bit float. Bool is true or false. Atom is an interned symbol, written #Name — a label, not a condition, so `if flag` is a compile error when flag is an Atom. void is a return type only; there is no void value, field or element. There is no implicit numeric widening anywhere: an Int never becomes a Float on its own.", ExplanationPt: "Str guarda texto UTF-8. Int é um inteiro de 64 bits com sinal cujo overflow dá a volta silenciosamente. Float é um float de 64 bits. Bool é true ou false. Atom é um símbolo internado, escrito #Nome — um rótulo, não uma condição, então `if flag` é um erro de compilação quando flag é um Atom. void é apenas um tipo de retorno; não existe valor, campo ou elemento void. Não há ampliação numérica implícita em lugar nenhum: um Int nunca se torna um Float por conta própria.", Examples: []slides_0_Example{slides_0_Example{Caption: "", CaptionPt: "", Code: "proc main(): void {\n\tStr   name   = \"bee\"\n\tInt   count  = 42\n\tFloat weight = 3.14\n\tBool  ready  = true\n\tAtom  state  = #Ready\n\n\techo \"{name} {count} {weight} {ready} {state}\"\n}", CodePt: "proc main(): void {\n\tStr   name   = \"bee\"\n\tInt   count  = 42\n\tFloat weight = 3.14\n\tBool  ready  = true\n\tAtom  state  = #Ready\n\n\techo \"{name} {count} {weight} {ready} {state}\"\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "Strings", TitlePt: "Strings", Explanation: "A double-quoted string interpolates with {expression} and escapes with \\n, \\t, \\\", \\\\, and \\{ for a literal brace. A backtick string may span lines, and its indentation is stripped at compile time down to the shortest common margin, so it is the natural way to hold a block of text — or, as on this very page, a block of Hive source. It interpolates {expression} the way a double-quoted string does, and its one escape is \\{ for a literal brace: a backslash anywhere else is just a backslash.\n\nA Str is a sequence of characters, and everything addresses it as one. len counts characters where bytes counts the UTF-8 they take, so for \"café\" they are 4 and 5. It is indexed and sliced by character too — s[0] is the first character, as a one-character Str, and s[1:3] is characters one through three with both bounds inclusive — and every one of those is proved in range at compile time, exactly as a vector's is. A Str carries no declared length, so an index into one is always guarded or comes from indexOf, whose Ok payload is a position the string really has.\n\nWhat a Str is not is storage. s[0] = \"x\" is a compile error: a string is a value, the same as an Int, and characters are not all the same width, so writing one would move every character after it. You build the new string instead.", ExplanationPt: "Uma string entre aspas duplas interpola com {expressão} e usa escapes com \\n, \\t, \\\", \\\\, e \\{ para uma chave literal. Uma string entre crases pode ocupar várias linhas, e sua indentação é removida em tempo de compilação até a menor margem em comum, o que a torna a forma natural de guardar um bloco de texto — ou, como nesta própria página, um bloco de código-fonte Hive. Ela interpola {expressão} do mesmo jeito que uma string entre aspas duplas, e seu único escape é \\{ para uma chave literal: uma barra invertida em qualquer outro lugar é só uma barra invertida.\n\nUma Str é uma sequência de caracteres, e tudo a endereça como tal. len conta caracteres onde bytes conta o UTF-8 que eles ocupam, então para \"café\" são 4 e 5. Ela também é indexada e fatiada por caractere — s[0] é o primeiro caractere, como uma Str de um caractere, e s[1:3] são os caracteres de um a três com os dois limites inclusivos — e cada um deles é comprovado dentro dos limites em tempo de compilação, exatamente como o de um vetor. Uma Str não carrega tamanho declarado, então um índice nela sempre está sob guarda ou vem de indexOf, cuja carga Ok é uma posição que a string realmente tem.\n\nO que uma Str não é: armazenamento. s[0] = \"x\" é um erro de compilação: uma string é um valor, igual a um Int, e caracteres não têm todos a mesma largura, então escrever um moveria todos os caracteres depois dele. Você constrói a string nova no lugar disso.", Examples: []slides_0_Example{slides_0_Example{Caption: "Interpolation and escapes", CaptionPt: "Interpolação e escapes", Code: "proc main(): void {\n\tname := \"bee\"\n\techo \"hello, {name}!\"       // interpolated\n\techo \"a literal brace: \\{\" // escaped\n}", CodePt: "proc main(): void {\n\tname := \"bee\"\n\techo \"olá, {name}!\"          // interpolado\n\techo \"uma chave literal: \\{\" // escapado\n}"}, slides_0_Example{Caption: "Backtick strings", CaptionPt: "Strings entre crases", Code: (("proc main(): void {\n\tname := \"bee\"\n\tblock := " + "`\n\t\thello, {name}\n\t\ta literal brace: \\{\n\t`\n") + "\techo block\n}"), CodePt: (("proc main(): void {\n\tname := \"bee\"\n\tblock := " + "`\n\t\tolá, {name}\n\t\tuma chave literal: \\{\n\t`\n") + "\techo block\n}")}, slides_0_Example{Caption: "Counted, indexed and sliced by character", CaptionPt: "Contada, indexada e fatiada por caractere", Code: "proc main(): void {\n\ts := \"café au lait\"\n\n\techo len(s)                  // 12 characters\n\techo bytes(s)                // 13 bytes — é takes two\n\n\tif s bounds 3 {\n\t\techo s[3]                // \"é\", the fourth character\n\t\techo s[0:3]              // \"café\" — both bounds inclusive\n\t}\n\n\t// A position from indexOf is one the string really has, so the\n\t// slice taken at it needs no guard of its own.\n\tif indexOf(s, \"au\") is Result.Ok(at) {\n\t\techo s[at:]              // \"au lait\"\n\t}\n}", CodePt: "proc main(): void {\n\ts := \"café au lait\"\n\n\techo len(s)                  // 12 caracteres\n\techo bytes(s)                // 13 bytes — é ocupa dois\n\n\tif s bounds 3 {\n\t\techo s[3]                // \"é\", o quarto caractere\n\t\techo s[0:3]              // \"café\" — os dois limites inclusivos\n\t}\n\n\t// Uma posição vinda de indexOf é uma que a string realmente tem,\n\t// então a fatia tirada nela não precisa de guarda própria.\n\tif indexOf(s, \"au\") is Result.Ok(at) {\n\t\techo s[at:]              // \"au lait\"\n\t}\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "Operators", TitlePt: "Operadores", Explanation: "+ adds two numbers, concatenates two Str, and joins two vectors into a new one whose length is the two lengths added. - * / % ** are the rest of the arithmetic, and unary - and unary ! are the language's two prefix operators. == and != compare any two values of one type structurally — element by element down a vector, field by field through a declared type, and ignoring the order a map's keys were set in — while comparing two values of different types is a compile error rather than a silent false. < > <= >= order Int, Float and Str. && and || short-circuit, and both take a Bool and nothing else: there is no truthiness in Hive, so an Int, a Str, an Atom or a vector is not a condition.\n\n! takes a Bool and answers with the opposite one, at the same precedence as unary -, which puts it tighter than every comparison and every conjunction: !found && ready is (!found) && ready, and !(a == b) needs its parentheses. Anything but a Bool is a compile error, for the same reason a condition may not be one. The single Bool it refuses is a match — ! may not be applied to an expression holding an `is`, because `is` narrows the value and binds parts of it and neither survives being negated, so a name it bound could never be set. The else is where that case goes, which is where it always went. A mutable number steps with the compound assignments += -= *= /= and with ++ and --, each one shorthand for the matching x = x <op> ….\n\nNone of these ever converts a number behind your back: an Int does not become a Float, so hive.math.max(0, health) is a compile error rather than a surprise, and hive.conv.itf is how you widen one on purpose.", ExplanationPt: "+ soma dois números, concatena dois Str, e junta dois vetores em um novo cujo tamanho é a soma dos dois tamanhos. - * / % ** são o resto da aritmética, e o - unário e o ! unário são os dois operadores prefixos da linguagem. == e != comparam quaisquer dois valores de um mesmo tipo estruturalmente — elemento por elemento ao longo de um vetor, campo por campo através de um tipo declarado, e ignorando a ordem em que as chaves de um mapa foram definidas — enquanto comparar dois valores de tipos diferentes é um erro de compilação, não um false silencioso. < > <= >= ordenam Int, Float e Str. && e || fazem curto-circuito, e ambos recebem um Bool e nada mais: não existe truthiness em Hive, então um Int, uma Str, um Atom ou um vetor não é uma condição.\n\n! recebe um Bool e responde com o oposto, na mesma precedência do - unário, o que o deixa mais forte que qualquer comparação e qualquer conjunção: !found && ready é (!found) && ready, e !(a == b) precisa dos parênteses. Qualquer coisa que não seja um Bool é erro de compilação, pelo mesmo motivo que uma condição também não pode ser. O único Bool que ele recusa é um casamento — ! não pode ser aplicado a uma expressão que contenha um `is`, porque o `is` estreita o valor e vincula partes dele, e nada disso sobrevive a uma negação: um nome que ele vinculasse nunca poderia ser preenchido. O else é onde esse caso vai, que é onde ele sempre foi. Um número mutável avança com as atribuições compostas += -= *= /= e com ++ e --, cada uma sendo um atalho para o x = x <op> … correspondente.\n\nNenhum deles converte um número pelas suas costas: um Int não vira um Float, então hive.math.max(0, health) é um erro de compilação em vez de uma surpresa, e hive.conv.itf é como se amplia um de propósito.", Examples: []slides_0_Example{slides_0_Example{Caption: "Arithmetic and comparison", CaptionPt: "Aritmética e comparação", Code: "proc main(): void {\n\techo 7 + 3 * 2        // 13 — * binds tighter than +\n\techo 7 / 2            // 3 — Int division truncates\n\techo 7 % 2            // 1 — the remainder\n\techo 2 ** 10          // 1024\n\techo -7               // one of the two prefix operators\n\n\techo 3 < 4            // ordering: Int, Float and Str\n\techo \"bee\" == \"bee\"   // structural equality\n\techo [1, 2] != [2, 1]\n}", CodePt: "proc main(): void {\n\techo 7 + 3 * 2        // 13 — * liga mais forte que +\n\techo 7 / 2            // 3 — divisão de Int trunca\n\techo 7 % 2            // 1 — o resto\n\techo 2 ** 10          // 1024\n\techo -7               // um dos dois operadores prefixos\n\n\techo 3 < 4            // ordenação: Int, Float e Str\n\techo \"bee\" == \"bee\"   // igualdade estrutural\n\techo [1, 2] != [2, 1]\n}"}, slides_0_Example{Caption: "Text and vectors join with the same +", CaptionPt: "Texto e vetores se juntam com o mesmo +", Code: "proc main(): void {\n\techo \"hive\" + \" \" + \"tour\"   // Str: concatenation\n\techo [1, 2] + [3]             // vectors: a new one, the lengths added\n}", CodePt: "proc main(): void {\n\techo \"hive\" + \" \" + \"tour\"   // Str: concatenação\n\techo [1, 2] + [3]             // vetores: um novo, os tamanhos somados\n}"}, slides_0_Example{Caption: "Logic, and negation", CaptionPt: "Lógica, e negação", Code: "proc main(): void {\n\tready := true\n\twarm := false\n\n\techo ready && warm\n\techo ready || warm\n\techo !warm\n\n\t// Tighter than every comparison, so both of these need their parentheses.\n\techo !(ready && warm)\n\techo !(1 == 2)\n\n\t// The one Bool it refuses is a match: the else is where that case goes.\n\tif indexOf([1, 2], 3) is Result.Error(_) { echo \"not there\" }\n}", CodePt: "proc main(): void {\n\tready := true\n\twarm := false\n\n\techo ready && warm\n\techo ready || warm\n\techo !warm\n\n\t// Mais forte que qualquer comparação, então estes dois precisam dos parênteses.\n\techo !(ready && warm)\n\techo !(1 == 2)\n\n\t// O único Bool que ele recusa é um casamento: o else é onde esse caso vai.\n\tif indexOf([1, 2], 3) is Result.Error(_) { echo \"não está lá\" }\n}"}, slides_0_Example{Caption: "Stepping a number", CaptionPt: "Avançando um número", Code: "proc main(): void {\n\tmut n := 10\n\tn += 5\n\tn -= 3\n\tn *= 2\n\tn /= 4\n\tn++\n\tn--\n\techo n     // 6\n}", CodePt: "proc main(): void {\n\tmut n := 10\n\tn += 5\n\tn -= 3\n\tn *= 2\n\tn /= 4\n\tn++\n\tn--\n\techo n     // 6\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "Arithmetic at the edges", TitlePt: "Aritmética nas bordas", Explanation: "Most of arithmetic is unsurprising. These are the cases worth stating exactly, and what they have in common is that each one is a value rather than a crash. Division and remainder by zero both answer with 0. Int overflow wraps, two's-complement and silently — which is what makes 2 ** 100 come back as 0, since ** on Int is repeated multiplication and wraps the same way. A negative Int exponent has no integral answer, so it yields 0 rather than a fraction, while n ** 0 is 1. A remainder takes the sign of its dividend, so -7 % 3 is -1. Float arithmetic does reach non-finite values, so 10.0 ** 400.0 is +Inf; for the mathematical answer to a power, work in Float, where ** is real exponentiation.\n\nOne case in the whole language is unspecified: converting a Float that is +Inf, -Inf, NaN or simply too large into an Int, through hive.conv.ceil, floor or round. Go leaves that conversion implementation-dependent and Hive does not paper over it, so the answer may differ between Go versions and architectures. Non-finite values reach a program through Float overflow and through hive.conv.stf(\"Inf\"), so check the range you expect before converting if it matters.", ExplanationPt: "A maior parte da aritmética não surpreende. Estes são os casos que valem ser ditos com exatidão, e o que eles têm em comum é que cada um é um valor, e não uma falha. Divisão e resto por zero respondem ambos com 0. Overflow de Int dá a volta, em complemento de dois e silenciosamente — e é isso que faz 2 ** 100 dar 0, já que ** sobre Int é multiplicação repetida e dá a volta da mesma forma. Um expoente Int negativo não tem resposta inteira, então produz 0 em vez de uma fração, enquanto n ** 0 é 1. Um resto toma o sinal do seu dividendo, então -7 % 3 é -1. A aritmética de Float alcança valores não finitos, então 10.0 ** 400.0 é +Inf; para a resposta matemática de uma potência, trabalhe em Float, onde ** é exponenciação de verdade.\n\nUm caso na linguagem inteira é não especificado: converter um Float que seja +Inf, -Inf, NaN ou simplesmente grande demais em um Int, através de hive.conv.ceil, floor ou round. O Go deixa essa conversão dependente da implementação e o Hive não disfarça isso, então a resposta pode diferir entre versões do Go e arquiteturas. Valores não finitos chegam a um programa por overflow de Float e por hive.conv.stf(\"Inf\"), então confira a faixa que você espera antes de converter, se isso importa.", Examples: []slides_0_Example{slides_0_Example{Caption: "", CaptionPt: "", Code: "proc main(): void {\n\techo 1 / 0          // 0 — division by zero is a value, not a crash\n\techo 5 % 0          // 0\n\techo -7 % 3         // -1 — the remainder takes the sign of the dividend\n\techo 2 ** 100       // 0 — Int overflow wraps, two's-complement, silently\n\techo 2 ** -1        // 0 — a negative Int exponent has no integral answer\n\techo 2 ** 0         // 1\n\techo 10.0 ** 400.0  // +Inf — Float arithmetic does reach non-finite values\n}", CodePt: "proc main(): void {\n\techo 1 / 0          // 0 — divisão por zero é um valor, não uma falha\n\techo 5 % 0          // 0\n\techo -7 % 3         // -1 — o resto toma o sinal do dividendo\n\techo 2 ** 100       // 0 — overflow de Int dá a volta, silenciosamente\n\techo 2 ** -1        // 0 — um expoente Int negativo não tem resposta inteira\n\techo 2 ** 0         // 1\n\techo 10.0 ** 400.0  // +Inf — Float alcança valores não finitos\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "Control flow", TitlePt: "Controle de fluxo", Explanation: "if, else if and else take a Bool, and only a Bool — an Atom is not a condition, and neither is an Int, a Str or a vector. Names bound by an `is` in the condition are in scope in that branch's body, and in the rest of the same condition after &&.\n\nThere are two loops. The counting one is for i := 0; i < 10; i++, whose counter is scoped to the loop and implicitly mutable, so it needs no mut — and any of its three clauses may be left out, which is how a while loop is written. The iterating one is for each name in values: it walks a vector binding each element to an immutable name, and because it never indexes it needs no bounds proof at all. for each name: T in values overrides the inferred element type. break leaves the innermost enclosing loop and continue skips to its next iteration; either one outside a loop is a compile error.\n\nEvery non-void callable has to return on every path, and a callable that does not is rejected by name rather than quietly handed a zero value. A path terminates by ending in return, in assert or panic, in an if/else whose every branch terminates, or in an else-less if/else if chain that covers its subject's whole type.", ExplanationPt: "if, else if e else recebem um Bool, e somente um Bool — um Atom não é uma condição, e um Int, uma Str ou um vetor também não. Nomes vinculados por um `is` na condição estão no escopo do corpo daquele ramo, e no resto da mesma condição depois de &&.\n\nExistem dois laços. O de contagem é for i := 0; i < 10; i++, cujo contador tem escopo do laço e é implicitamente mutável, então não precisa de mut — e qualquer uma de suas três cláusulas pode ser omitida, que é como se escreve um laço while. O de iteração é for each name in values: ele percorre um vetor vinculando cada elemento a um nome imutável, e como nunca indexa, não precisa de prova de limites alguma. for each name: T in values sobrepõe o tipo de elemento inferido. break sai do laço mais interno e continue pula para a próxima iteração dele; qualquer um dos dois fora de um laço é um erro de compilação.\n\nTodo callable não-void precisa retornar em todo caminho, e um que não retorna é recusado pelo nome em vez de receber silenciosamente um valor zero. Um caminho termina acabando em return, em assert ou panic, em um if/else cujos ramos todos terminam, ou em uma cadeia if/else if sem else que cobre o tipo inteiro do seu sujeito.", Examples: []slides_0_Example{slides_0_Example{Caption: "if, else if, else", CaptionPt: "if, else if, else", Code: "proc main(): void {\n\tframes := 7\n\n\tif frames > 10 {\n\t\techo \"a strong colony\"\n\t} else if frames > 3 {\n\t\techo \"coming along\"\n\t} else {\n\t\techo \"needs feeding\"\n\t}\n}", CodePt: "proc main(): void {\n\tframes := 7\n\n\tif frames > 10 {\n\t\techo \"uma colônia forte\"\n\t} else if frames > 3 {\n\t\techo \"vai indo\"\n\t} else {\n\t\techo \"precisa de alimento\"\n\t}\n}"}, slides_0_Example{Caption: "The counting loop", CaptionPt: "O laço de contagem", Code: "proc main(): void {\n\tfor i := 0; i < 3; i++ {\n\t\techo \"frame {i}\"\n\t}\n\n\t// Any clause may be left out. With only a condition, it is a while loop.\n\tmut left := 3\n\tfor ; left > 0; {\n\t\tleft -= 1\n\t}\n\techo left\n}", CodePt: "proc main(): void {\n\tfor i := 0; i < 3; i++ {\n\t\techo \"quadro {i}\"\n\t}\n\n\t// Qualquer cláusula pode ser omitida. Só com condição, é um laço while.\n\tmut left := 3\n\tfor ; left > 0; {\n\t\tleft -= 1\n\t}\n\techo left\n}"}, slides_0_Example{Caption: "for each: walking a vector", CaptionPt: "for each: percorrendo um vetor", Code: "proc main(): void {\n\thives := [\"north\", \"south\", \"orchard\"]\n\n\tfor each name in hives {\n\t\techo name\n\t}\n\n\tfor each name: Str in hives {   // the annotation overrides inference\n\t\techo len(name)\n\t}\n}", CodePt: "proc main(): void {\n\thives := [\"north\", \"south\", \"orchard\"]\n\n\tfor each name in hives {\n\t\techo name\n\t}\n\n\tfor each name: Str in hives {   // a anotação sobrepõe a inferência\n\t\techo len(name)\n\t}\n}"}, slides_0_Example{Caption: "break and continue", CaptionPt: "break e continue", Code: "proc main(): void {\n\tfor each name in [\"north\", \"\", \"south\", \"orchard\"] {\n\t\tif name == \"\" {\n\t\t\tcontinue          // skip to the next one\n\t\t}\n\t\tif name == \"south\" {\n\t\t\tbreak             // leave the loop entirely\n\t\t}\n\t\techo name\n\t}\n}", CodePt: "proc main(): void {\n\tfor each name in [\"north\", \"\", \"south\", \"orchard\"] {\n\t\tif name == \"\" {\n\t\t\tcontinue          // pula para o próximo\n\t\t}\n\t\tif name == \"south\" {\n\t\t\tbreak             // sai do laço por completo\n\t\t}\n\t\techo name\n\t}\n}"}, slides_0_Example{Caption: "Every path must return", CaptionPt: "Todo caminho precisa retornar", Code: "func grade(frames: Int): Str {\n\tif frames > 10 {\n\t\treturn \"strong\"\n\t} else if frames > 3 {\n\t\treturn \"growing\"\n\t}\n\treturn \"weak\"    // without this tail, the func does not compile\n}\n\nproc main(): void {\n\techo grade(7)\n}", CodePt: "func grade(frames: Int): Str {\n\tif frames > 10 {\n\t\treturn \"forte\"\n\t} else if frames > 3 {\n\t\treturn \"crescendo\"\n\t}\n\treturn \"fraca\"   // sem esta última linha, o func não compila\n}\n\nproc main(): void {\n\techo grade(7)\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "assert and panic", TitlePt: "assert e panic", Explanation: "assert cond says: this must hold. What happens when it does not is decided by where it is written. In ordinary code a failed assertion has proved the program wrong, so the program stops. Inside a test it has proved the test wrong, so the failure is recorded and every test after it still runs. Either way its operands are evaluated exactly once, whether it holds or not.\n\nA recorded failure quotes the condition's own source text and, for a comparison, prints both sides — which is why a test needs no assertion vocabulary beyond this one word. There is no assertEquals, no expect, no matcher library: the compiler already has the text of what you wrote and the static types of its parts, so it can say what the two sides came out as without being told how.\n\npanic value stops the program immediately, rendering value exactly the way echo would — so panic err prints an error's message, and an atom prints its name rather than its number. Unlike assert it always fires, and it takes any value rather than only a Bool. Because it never returns, a branch or a tail ending in panic counts as a terminating path, which is what lets it close off a tail the type system cannot see is unreachable. The one exception to \"stops the program\" is inside a hive.syslink service, where a panic kills only that service and leaves the node running.", ExplanationPt: "assert cond diz: isto precisa valer. O que acontece quando não vale é decidido por onde ele está escrito. Em código comum, uma asserção que falha provou que o programa está errado, então o programa para. Dentro de um test, ela provou que o teste está errado, então a falha é registrada e todo teste depois dele ainda roda. De qualquer forma, seus operandos são avaliados exatamente uma vez, valendo ou não.\n\nUma falha registrada cita o próprio texto-fonte da condição e, no caso de uma comparação, imprime os dois lados — e é por isso que um teste não precisa de vocabulário de asserção além dessa única palavra. Não existe assertEquals, não existe expect, não existe biblioteca de matchers: o compilador já tem o texto do que você escreveu e os tipos estáticos das suas partes, então consegue dizer no que os dois lados deram sem que ninguém lhe explique como.\n\npanic value para o programa na hora, renderizando value exatamente como o echo faria — então panic err imprime a mensagem de um erro, e um átomo imprime seu nome em vez do seu número. Diferente de assert, ele sempre dispara, e recebe qualquer valor em vez de apenas um Bool. Como nunca retorna, um ramo ou um final que termina em panic conta como caminho que termina, que é o que permite fechar um final que o sistema de tipos não consegue ver que é inalcançável. A única exceção a \"para o programa\" é dentro de um serviço hive.syslink, onde um panic mata apenas aquele serviço e deixa o nó rodando.", Examples: []slides_0_Example{slides_0_Example{Caption: "In ordinary code, it stops the program", CaptionPt: "Em código comum, ele para o programa", Code: "func total(prices: Int[]): Int {\n\tmut sum := 0\n\tfor each p in prices { sum += p }\n\treturn sum\n}\n\nproc main(): void {\n\tassert total([3, 4]) == 7    // holds: nothing happens\n\techo \"the basket adds up\"\n\tassert total([3, 4]) == 8    // does not: the program stops, here\n\techo \"never reached\"\n}", CodePt: "func total(prices: Int[]): Int {\n\tmut sum := 0\n\tfor each p in prices { sum += p }\n\treturn sum\n}\n\nproc main(): void {\n\tassert total([3, 4]) == 7    // vale: nada acontece\n\techo \"o carrinho fecha a conta\"\n\tassert total([3, 4]) == 8    // não vale: o programa para, aqui\n\techo \"nunca alcançado\"\n}"}, slides_0_Example{Caption: "panic closes off an impossible tail", CaptionPt: "panic fecha um final impossível", Code: "type Shape {\n\tCircle    { radius: Int }\n\tRectangle { width: Int, height: Int }\n}\n\nfunc area(shape: Shape): Int {\n\tif shape is Shape.Circle(r)            { return 3 * r * r }\n\telse if shape is Shape.Rectangle(w, h) { return w * h }\n\tpanic \"a shape with no area: {shape}\"\n}\n\nproc main(): void {\n\techo area(Shape.Circle(2))\n}", CodePt: "type Shape {\n\tCircle    { radius: Int }\n\tRectangle { width: Int, height: Int }\n}\n\nfunc area(shape: Shape): Int {\n\tif shape is Shape.Circle(r)            { return 3 * r * r }\n\telse if shape is Shape.Rectangle(w, h) { return w * h }\n\tpanic \"uma forma sem área: {shape}\"\n}\n\nproc main(): void {\n\techo area(Shape.Circle(2))\n}"}, slides_0_Example{Caption: "In a test, it records and carries on", CaptionPt: "Em um test, ele registra e segue em frente", Code: "func total(prices: Int[]): Int {\n\tmut sum := 0\n\tfor each p in prices { sum += p }\n\treturn sum\n}\n\ntest \"an empty basket costs nothing\" {\n\tInt[0] empty = []\n\tassert total(empty) == 0\n}\n\ntest \"this one is wrong on purpose\" {\n\tassert total([3, 4]) == 8\n}", CodePt: "func total(prices: Int[]): Int {\n\tmut sum := 0\n\tfor each p in prices { sum += p }\n\treturn sum\n}\n\ntest \"um carrinho vazio não custa nada\" {\n\tInt[0] empty = []\n\tassert total(empty) == 0\n}\n\ntest \"este está errado de propósito\" {\n\tassert total([3, 4]) == 8\n}"}, slides_0_Example{Caption: "What that failure prints", CaptionPt: "O que essa falha imprime", Code: "$ hive test basket.hive\n  PASS  an empty basket costs nothing\n  FAIL  this one is wrong on purpose\n        assert total(...) == 8\n          left:  7\n          right: 8\n\n  2 tests: 1 passed, 1 failed\n  coverage: 100.0% of statements (7/7)", CodePt: "$ hive test basket.hive\n  PASS  um carrinho vazio não custa nada\n  FAIL  este está errado de propósito\n        assert total(...) == 8\n          left:  7\n          right: 8\n\n  2 tests: 1 passed, 1 failed\n  coverage: 100.0% of statements (7/7)"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "Vectors", TitlePt: "Vetores", Explanation: "A vector is memory-contiguous and homogeneous. Str[3] is static: exactly three, always. Str[dyn] is dynamic: it promises nothing about its length and is the only kind append works on. Str[] is a parameter-only spelling that accepts either. A variable or field must say which of the two real kinds it holds — that promise is what the bounds checker rests on. + concatenates into a new vector; == compares structurally, element by element.", ExplanationPt: "Um vetor é contíguo na memória e homogêneo. Str[3] é estático: exatamente três, sempre. Str[dyn] é dinâmico: não promete nada sobre seu tamanho e é o único tipo sobre o qual append funciona. Str[] é uma grafia só para parâmetros que aceita qualquer um dos dois. Uma variável ou campo precisa dizer qual dos dois tipos reais ela guarda — essa promessa é o que sustenta o verificador de limites. + concatena em um novo vetor; == compara estruturalmente, elemento por elemento.", Examples: []slides_0_Example{slides_0_Example{Caption: "", CaptionPt: "", Code: "proc main(): void {\n\tv := [\"a\", \"b\", \"c\"]          // Str[3] — a static length, inferred\n\tmut Str[dyn] w = [\"a\", \"b\"]    // dynamic — the only kind append accepts\n\tappend(w, \"c\")\n\n\tfirst := v[0]\n\tmiddle := v[0:1]                // slicing, both bounds inclusive\n\techo \"{first} {middle} {w}\"\n}", CodePt: "proc main(): void {\n\tv := [\"a\", \"b\", \"c\"]          // Str[3] — um tamanho estático, inferido\n\tmut Str[dyn] w = [\"a\", \"b\"]    // dinâmico — o único tipo que append aceita\n\tappend(w, \"c\")\n\n\tfirst := v[0]\n\tmiddle := v[0:1]                // slicing, ambos os limites inclusivos\n\techo \"{first} {middle} {w}\"\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "Builtins", TitlePt: "Funções nativas", Explanation: "These are always in scope, with no import needed. len counts a vector's elements or a Str's characters; bytes counts a vector's backing storage or a Str's UTF-8 bytes. append and prepend grow a mutable dynamic vector at either end; drop removes a range and hands back exactly what it removed. join concatenates a vector of Str with a separator between each pair, and split is its inverse. replaceFirst and replaceAll rewrite one occurrence of a substring or every one of them, answering with a new string rather than writing through the old. indexOf finds the first equal element — or, on a Str, the first occurrence of a substring, counted in characters — and, unlike a bare -1, an Ok index is always safe to use unguarded. row and column pull a Table's row or column by its first cell or its top cell, and toTable cuts a flat vector of cells into a Table of rows that wide, the last row holding what was left. map, filter and filterMap each walk a vector with a func — map transforms every element, filter keeps the ones a predicate says yes to, filterMap does both in one pass — and sort puts a vector in order, in place when it stands alone as a statement over a mut vector.\n\nA declaration of your own — a func, proc, query or type sharing one of these names — wins over the builtin at every bare call, which stays reachable as hive.<name>.", ExplanationPt: "Estas estão sempre no escopo, sem precisar de import. len conta os elementos de um vetor ou os caracteres de uma Str; bytes conta o armazenamento de um vetor ou os bytes UTF-8 de uma Str. append e prepend fazem crescer um vetor dinâmico mutável em uma das pontas; drop remove um intervalo e devolve exatamente o que removeu. join concatena um vetor de Str com um separador entre cada par, e split é o seu inverso. replaceFirst e replaceAll reescrevem uma ocorrência de um trecho ou todas elas, devolvendo uma string nova em vez de escrever sobre a antiga. indexOf encontra o primeiro elemento igual — ou, em uma Str, a primeira ocorrência de um trecho, contada em caracteres — e, diferente de um -1 solto, um índice Ok é sempre seguro de usar sem guarda. row e column extraem a linha ou coluna de uma Table pela primeira célula ou pela célula do topo, e toTable corta um vetor plano de células em uma Table de linhas com essa largura, a última ficando com o que sobrou. map, filter e filterMap percorrem um vetor com um func — map transforma cada elemento, filter mantém aqueles que um predicado aprova, filterMap faz as duas coisas em uma passada — e sort põe um vetor em ordem, no lugar quando aparece isolado como instrução sobre um vetor mut.\n\nUma declaração sua — um func, proc, query ou type com um desses nomes — vence a função nativa em toda chamada sem qualificação, que continua acessível como hive.<nome>.", Examples: []slides_0_Example{slides_0_Example{Caption: "Length and bytes", CaptionPt: "Tamanho e bytes", Code: "proc main(): void {\n\tfruit := [\"pear\", \"apple\", \"kiwi\"]\n\techo len(fruit)\n\techo bytes(\"café\")\n}", CodePt: "proc main(): void {\n\tfruit := [\"pear\", \"apple\", \"kiwi\"]\n\techo len(fruit)\n\techo bytes(\"café\")\n}"}, slides_0_Example{Caption: "Growing and shrinking", CaptionPt: "Crescendo e encolhendo", Code: "proc main(): void {\n\tmut Str[dyn] basket = [\"pear\"]\n\tappend(basket, \"apple\")\n\tprepend(basket, \"kiwi\")\n\techo basket\n\n\tif basket bounds 0 {\n\t\techo drop(basket, 0, 0)\n\t}\n\techo basket\n}", CodePt: "proc main(): void {\n\tmut Str[dyn] basket = [\"pear\"]\n\tappend(basket, \"apple\")\n\tprepend(basket, \"kiwi\")\n\techo basket\n\n\tif basket bounds 0 {\n\t\techo drop(basket, 0, 0)\n\t}\n\techo basket\n}"}, slides_0_Example{Caption: "Joining, splitting and finding", CaptionPt: "Juntando, dividindo e encontrando", Code: "proc main(): void {\n\tfruit := [\"pear\", \"apple\", \"kiwi\"]\n\tline := join(fruit, \", \")\n\techo split(line, \", \")\n\n\tif indexOf(fruit, \"apple\") is Result.Ok(i) {\n\t\techo fruit[i]\n\t}\n}", CodePt: "proc main(): void {\n\tfruit := [\"pear\", \"apple\", \"kiwi\"]\n\tline := join(fruit, \", \")\n\techo split(line, \", \")\n\n\tif indexOf(fruit, \"apple\") is Result.Ok(i) {\n\t\techo fruit[i]\n\t}\n}"}, slides_0_Example{Caption: "Rewriting a string", CaptionPt: "Reescrevendo uma string", Code: "proc main(): void {\n\tpath := \"a/b/c\"\n\n\techo replaceFirst(path, \"/\", \" > \")   // a > b/c\n\techo replaceAll(path, \"/\", \" > \")     // a > b > c\n\n\t// Neither writes through the string it was given: a Str is a\n\t// value, so path is what it always was.\n\techo path                             // a/b/c\n\n\t// Nothing to find is not an error — the string comes back as\n\t// it was.\n\techo replaceAll(path, \"z\", \"!\")       // a/b/c\n}", CodePt: "proc main(): void {\n\tcaminho := \"a/b/c\"\n\n\techo replaceFirst(caminho, \"/\", \" > \")   // a > b/c\n\techo replaceAll(caminho, \"/\", \" > \")     // a > b > c\n\n\t// Nenhum dos dois escreve sobre a string recebida: uma Str é um\n\t// valor, então caminho é o que sempre foi.\n\techo caminho                             // a/b/c\n\n\t// Não encontrar nada não é erro — a string volta como estava.\n\techo replaceAll(caminho, \"z\", \"!\")       // a/b/c\n}"}, slides_0_Example{Caption: "Walking a vector", CaptionPt: "Percorrendo um vetor", Code: "func isLong(fruit: Str): Bool {\n\treturn len(fruit) > 4\n}\n\nfunc evenHalf(n: Int): Result<Int, Bool> {\n\tif n % 2 == 0 { return Result.Ok(n / 2) }\n\treturn Result.Error(false)\n}\n\nproc main(): void {\n\tfruit := [\"pear\", \"apple\", \"kiwi\"]\n\techo map(fruit, isLong)\n\techo filter(fruit, isLong)\n\techo filterMap([1, 2, 3, 4], evenHalf)\n\techo sort(fruit)\n}", CodePt: "func isLong(fruit: Str): Bool {\n\treturn len(fruit) > 4\n}\n\nfunc evenHalf(n: Int): Result<Int, Bool> {\n\tif n % 2 == 0 { return Result.Ok(n / 2) }\n\treturn Result.Error(false)\n}\n\nproc main(): void {\n\tfruit := [\"pear\", \"apple\", \"kiwi\"]\n\techo map(fruit, isLong)\n\techo filter(fruit, isLong)\n\techo filterMap([1, 2, 3, 4], evenHalf)\n\techo sort(fruit)\n}"}, slides_0_Example{Caption: "Tables", CaptionPt: "Tabelas", Code: "proc main(): void {\n\ttable := [[\"name\", \"role\"], [\"ada\", \"engineer\"], [\"grace\", \"admiral\"]]\n\techo row(table, \"ada\")\n\techo column(table, \"role\")\n\techo toTable([\"a\", \"b\", \"c\", \"d\", \"e\"], 2)\n}", CodePt: "proc main(): void {\n\ttable := [[\"name\", \"role\"], [\"ada\", \"engineer\"], [\"grace\", \"admiral\"]]\n\techo row(table, \"ada\")\n\techo column(table, \"role\")\n\techo toTable([\"a\", \"b\", \"c\", \"d\", \"e\"], 2)\n}"}, slides_0_Example{Caption: "A declaration of your own wins", CaptionPt: "Uma declaração sua vence", Code: "// Ours. Its own parameters, its own arity, and it is what every bare\n// call to len in this file means from here on.\nfunc len(label: Str, extra: Int): Str {\n\treturn \"{label}{extra}\"\n}\n\nproc main(): void {\n\tStr[dyn] v = [\"a\", \"b\"]\n\n\techo len(\"x\", 2)          // ours: \"x2\"\n\techo hive.len(v)          // the builtin, by its long name: 2\n\techo hive.join(v, \"-\")    // the long name works whether you shadowed or not\n}", CodePt: "// A nossa. Seus próprios parâmetros, sua própria aridade, e é o que toda\n// chamada solta a len neste arquivo significa daqui em diante.\nfunc len(label: Str, extra: Int): Str {\n\treturn \"{label}{extra}\"\n}\n\nproc main(): void {\n\tStr[dyn] v = [\"a\", \"b\"]\n\n\techo len(\"x\", 2)          // a nossa: \"x2\"\n\techo hive.len(v)          // a nativa, pelo nome longo: 2\n\techo hive.join(v, \"-\")    // o nome longo funciona tendo sombreado ou não\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "Pipes", TitlePt: "Pipes", Explanation: "x | f(a) is sugar for f(x, a): the value on the left becomes the call's first argument, so a chain of them reads in the order it runs rather than inside out. | is left-associative, and a stage with nothing else in it needs no parentheses — x | f is f(x), as x | f() is.\n\nIt binds tighter than every binary operator and looser than the prefix ones, which lets a pipeline sit on either side of one: v | len > 2 compares the length. And it takes the value written beside it, so 1 + 2 | f is 1 + f(2), and a computed value goes in parentheses.\n\nA proc, a builtin and a constructor all pipe, and a hole in a stage still makes that call a partial application. What a pipe leaves behind is the call somebody could have written by hand, so nothing else about it changes — there is no pipeline object, and nothing is deferred.", ExplanationPt: "x | f(a) é açúcar sintático para f(x, a): o valor da esquerda vira o primeiro argumento da chamada, então uma sequência deles se lê na ordem em que roda, e não de dentro para fora. O | é associativo à esquerda, e um estágio que não tem mais nada dispensa os parênteses — x | f é f(x), assim como x | f() é.\n\nEle se liga mais forte que qualquer operador binário e mais fraco que os prefixados, o que permite um pipeline dos dois lados de um deles: v | len > 2 compara o comprimento. E ele pega o valor escrito ao lado dele, então 1 + 2 | f é 1 + f(2); um valor calculado vai entre parênteses.\n\nproc, builtin e construtor entram num pipe do mesmo jeito, e um buraco num estágio continua fazendo daquela chamada uma aplicação parcial. O que um pipe deixa para trás é a chamada que alguém poderia ter escrito à mão, então nada mais muda — não existe objeto de pipeline, e nada fica adiado.", Examples: []slides_0_Example{slides_0_Example{Caption: "A chain reads in the order it runs", CaptionPt: "Uma sequência se lê na ordem em que roda", Code: "func isEven(n: Int): Bool { return n % 2 == 0 }\nfunc label(n: Int): Str { return \"#{n}\" }\n\nproc main(): void {\n\tInt[dyn] nums = [1, 2, 3, 4, 5, 6]\n\n\techo nums | filter(isEven) | map(label) | join(\", \")\n\n\t// The same call, and what the line above compiles to.\n\techo join(map(filter(nums, isEven), label), \", \")\n}", CodePt: "func isEven(n: Int): Bool { return n % 2 == 0 }\nfunc label(n: Int): Str { return \"#{n}\" }\n\nproc main(): void {\n\tInt[dyn] nums = [1, 2, 3, 4, 5, 6]\n\n\techo nums | filter(isEven) | map(label) | join(\", \")\n\n\t// A mesma chamada, e no que a linha acima compila.\n\techo join(map(filter(nums, isEven), label), \", \")\n}"}, slides_0_Example{Caption: "What it binds to", CaptionPt: "A quem ele se liga", Code: "func double(n: Int): Int { return n * 2 }\n\nproc main(): void {\n\tStr[dyn] words = [\"a\", \"b\", \"c\"]\n\n\t// Tighter than a comparison, so this is len(words) > 2.\n\techo words | len > 2\n\n\t// And it takes the value beside it: 1 + double(2), not double(3).\n\techo 1 + 2 | double\n\techo (1 + 2) | double\n}", CodePt: "func double(n: Int): Int { return n * 2 }\n\nproc main(): void {\n\tStr[dyn] words = [\"a\", \"b\", \"c\"]\n\n\t// Mais forte que uma comparação, então isto é len(words) > 2.\n\techo words | len > 2\n\n\t// E ele pega o valor ao lado: 1 + double(2), e não double(3).\n\techo 1 + 2 | double\n\techo (1 + 2) | double\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "Bounds, proved at compile time", TitlePt: "Limites, comprovados em tempo de compilação", Explanation: "A dedicated flow-sensitive pass proves every index and slice in range before the program ever runs. A literal index into a known length compiles outright; a computed index has to sit under a guard the pass can read, such as `if v bounds i { v[i] }` — sugar for `i >= 0 && i < len(v)`. Anything the pass cannot prove safe is a compile error, not a runtime panic — `echo v[len(v) - 1]` on its own is rejected outright, computed index and all. This very server leans on it: the slide number from the URL is checked with exactly this guard before it ever indexes the list of slides.\n\nA Str is proved exactly the same way, with one difference worth knowing: a string never has a length the compiler knows, because there is no Str[3] for text. So even `s[1]` on a string literal sitting right there is refused, and every index into one is either guarded or came from indexOf.", ExplanationPt: "Uma passagem dedicada e sensível ao fluxo comprova que todo índice e slice está dentro dos limites antes mesmo do programa rodar. Um índice literal dentro de um tamanho conhecido compila sem mais nada; um índice calculado precisa estar sob uma guarda que a passagem saiba ler, como `if v bounds i { v[i] }` — açúcar sintático para `i >= 0 && i < len(v)`. Tudo que a passagem não conseguir provar seguro é um erro de compilação, não um panic em tempo de execução — `echo v[len(v) - 1]` sozinho já é rejeitado de cara, índice calculado e tudo. Este mesmo servidor se apoia nisso: o número do slide vindo da URL é verificado com exatamente essa guarda antes de indexar a lista de slides.\n\nUma Str é comprovada exatamente do mesmo jeito, com uma diferença que vale saber: uma string nunca tem um tamanho que o compilador conheça, porque não existe Str[3] para texto. Então até `s[1]` em um literal de string bem ali é recusado, e todo índice em uma delas ou está sob guarda ou veio de indexOf.", Examples: []slides_0_Example{slides_0_Example{Caption: "", CaptionPt: "", Code: "proc main(): void {\n\tv := [\"a\", \"b\", \"c\"]\n\n\tlast := len(v) - 1\n\tif v bounds last {\n\t\techo v[last]                 // fine — the guard covers it\n\t}\n\n\t// A Str carries no declared length, so even a literal index\n\t// into one is guarded — or comes from indexOf, which only\n\t// ever answers with a position the string really has.\n\ts := \"café\"\n\tif s bounds 3 {\n\t\techo s[3]\n\t}\n\tif indexOf(s, \"f\") is Result.Ok(at) {\n\t\techo s[at:]                  // no guard needed\n\t}\n}", CodePt: "proc main(): void {\n\tv := [\"a\", \"b\", \"c\"]\n\n\tlast := len(v) - 1\n\tif v bounds last {\n\t\techo v[last]                 // tudo bem — a guarda cobre isso\n\t}\n\n\t// Uma Str não carrega tamanho declarado, então até um índice\n\t// literal nela fica sob guarda — ou vem de indexOf, que só\n\t// responde com uma posição que a string realmente tem.\n\ts := \"café\"\n\tif s bounds 3 {\n\t\techo s[3]\n\t}\n\tif indexOf(s, \"f\") is Result.Ok(at) {\n\t\techo s[at:]                  // sem precisar de guarda\n\t}\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "A length is a promise", TitlePt: "Um tamanho é uma promessa", Explanation: "A declared static length is a promise, and it is kept everywhere: every value that lands in a Str[3] slot has to be a vector of exactly three — an initialiser, a later assignment, an argument, a field of a constructed value, a returned value — and a length the compiler cannot see is rejected too, which is why split can never fill one. Because the promise holds at every call site it is never lost, so a Str[3] can be indexed unguarded even after being reassigned, and so can a Str[3] parameter inside the callee. A field is the same: a Str[3] field is enforced wherever a value reaches it, construction included, so nothing that happens to the record around it can change how many it holds — assign a whole new record and the field is still exactly three.\n\nAn inferred length is weaker. mut v := [\"a\", \"b\", \"c\"] is a Str[3] observed rather than promised: writing through the name keeps it, but any rebinding costs it — even a rebinding to the same length, and even one inside a branch that may not run. A proof that holds only sometimes is not a proof. Declare the type when you want the promise enforced, and [dyn] when you want the freedom and will guard your indexes.\n\nWhat a guard proves is weaker again, and in the same way. if 2 < len(box.items) is proved about the value the field held at that moment, so replacing the field — or the record around it — costs it, even inside the very branch the guard opened. The two rules pull in opposite directions on the same expression and both are worth knowing: what the declaration says about a field survives everything, and what a guard found out about one survives only as long as the value it looked at.\n\nA length the compiler does know survives everything that cannot lose it. Concatenation adds the two lengths, map keeps its input's — a transform changes elements, not how many — and sort keeps it too, since a comparator moves elements rather than removing any. filter and filterMap deliberately claim nothing: what is knowable about them is a maximum, and a maximum can never put an index in range, because a filter that keeps nothing is always a possibility.", ExplanationPt: "Um tamanho estático declarado é uma promessa, e ela é mantida em todo lugar: todo valor que chega em um espaço Str[3] precisa ser um vetor de exatamente três — um inicializador, uma atribuição posterior, um argumento, um campo de um valor construído, um valor retornado — e um tamanho que o compilador não consegue ver também é recusado, e é por isso que split nunca consegue preencher um. Como a promessa vale em todo local de chamada, ela nunca se perde, então um Str[3] pode ser indexado sem guarda mesmo depois de ser reatribuído, e um parâmetro Str[3] também pode, dentro de quem foi chamado. Um campo é a mesma coisa: um campo Str[3] é exigido em todo lugar onde um valor chega nele, inclusive na construção, então nada que aconteça com o registro em volta pode mudar quantos ele guarda — atribua um registro inteiramente novo e o campo continua sendo exatamente três.\n\nUm tamanho inferido é mais fraco. mut v := [\"a\", \"b\", \"c\"] é um Str[3] observado, não prometido: escrever através do nome o mantém, mas qualquer reatribuição o custa — mesmo uma reatribuição para o mesmo tamanho, e mesmo uma dentro de um ramo que pode nem rodar. Uma prova que só vale às vezes não é uma prova. Declare o tipo quando quiser a promessa cobrada, e [dyn] quando quiser a liberdade e for guardar seus índices.\n\nO que uma guarda prova é mais fraco ainda, e da mesma maneira. if 2 < len(box.items) é provado sobre o valor que o campo guardava naquele instante, então substituir o campo — ou o registro em volta dele — custa a prova, mesmo dentro do próprio ramo que a guarda abriu. As duas regras puxam em direções opostas sobre a mesma expressão e vale conhecer ambas: o que a declaração diz sobre um campo sobrevive a tudo, e o que uma guarda descobriu sobre um campo sobrevive só enquanto durar o valor que ela olhou.\n\nUm tamanho que o compilador conhece sobrevive a tudo que não pode perdê-lo. A concatenação soma os dois tamanhos, map mantém o da sua entrada — uma transformação muda os elementos, não quantos são — e sort também mantém, já que um comparador move elementos em vez de remover algum. filter e filterMap deliberadamente não afirmam nada: o que se pode saber deles é um máximo, e um máximo nunca pode colocar um índice na faixa, porque um filtro que não guarda nada é sempre uma possibilidade.", Examples: []slides_0_Example{slides_0_Example{Caption: "A declared length is kept", CaptionPt: "Um tamanho declarado é mantido", Code: (((("proc main(): void {\n\tmut Str[3] v = [\"a\", \"b\", \"c\"]\n\tv = [\"x\", \"y\", \"z\"]   // still exactly three: the promise is kept\n\techo v[2]              // no guard needed, here or ever\n\n\t// v = [\"x\"]           // compile error: " + "`") + "v") + "` ") + "is declared Str[3]\n}"), CodePt: (((("proc main(): void {\n\tmut Str[3] v = [\"a\", \"b\", \"c\"]\n\tv = [\"x\", \"y\", \"z\"]   // ainda exatamente três: a promessa é mantida\n\techo v[2]              // sem guarda, aqui ou em qualquer lugar\n\n\t// v = [\"x\"]           // erro de compilação: " + "`") + "v") + "` ") + "é declarado Str[3]\n}")}, slides_0_Example{Caption: "An inferred one is not", CaptionPt: "Um inferido não é", Code: (((("proc main(): void {\n\tmut v := [\"a\", \"b\", \"c\"]   // Str[3], observed rather than promised\n\tv[0] = \"x\"                  // a write through the name keeps the length\n\techo v[2]\n\n\t// v = [\"x\", \"y\", \"z\"]      // ...but any rebinding costs the proof, even\n\t// echo v[2]                // to the same length: " + "`") + "v") + "` ") + "has no length now\n}"), CodePt: (((("proc main(): void {\n\tmut v := [\"a\", \"b\", \"c\"]   // Str[3], observado e não prometido\n\tv[0] = \"x\"                  // escrever através do nome mantém o tamanho\n\techo v[2]\n\n\t// v = [\"x\", \"y\", \"z\"]      // ...mas qualquer reatribuição custa a prova,\n\t// echo v[2]                // até para o mesmo tamanho: " + "`") + "v") + "` ") + "não tem tamanho\n}")}, slides_0_Example{Caption: "What a known length survives", CaptionPt: "O que um tamanho conhecido sobrevive", Code: "func twice(n: Int): Int { return n * 2 }\n\nproc main(): void {\n\tInt[2] a = [2, 1]\n\tInt[8] b = [1, 2, 3, 4, 5, 6, 7, 8]\n\n\tboth := a + b            // ten elements: the lengths add\n\techo both[9]              // no guard needed\n\n\tdoubled := map(a, twice)  // still two\n\tordered := sort(a)        // still two\n\techo \"{doubled[1]} {ordered[1]}\"\n\n\t// filter and filterMap claim nothing, so their results always need one.\n}", CodePt: "func twice(n: Int): Int { return n * 2 }\n\nproc main(): void {\n\tInt[2] a = [2, 1]\n\tInt[8] b = [1, 2, 3, 4, 5, 6, 7, 8]\n\n\tboth := a + b            // dez elementos: os tamanhos somam\n\techo both[9]              // sem precisar de guarda\n\n\tdoubled := map(a, twice)  // ainda dois\n\tordered := sort(a)        // ainda dois\n\techo \"{doubled[1]} {ordered[1]}\"\n\n\t// filter e filterMap não afirmam nada, então seus resultados sempre precisam.\n}"}, slides_0_Example{Caption: "A field's length is a promise too", CaptionPt: "O tamanho de um campo também é uma promessa", Code: "type Reading {\n\tsectors: Float[3]     // three, in every Reading there will ever be\n\tnotes:   Str[dyn]     // any number, in each one separately\n}\n\nfunc slower(was: Reading): Reading {\n\treturn Reading([was.sectors[0] + 0.1, was.sectors[1], was.sectors[2]], was.notes)\n}\n\nproc main(): void {\n\tmut Reading lap = Reading([31.4, 28.9, 33.2], [\"clean\"])\n\techo lap.sectors[2]        // no guard: the declaration promised three\n\n\tlap = slower(lap)          // a whole new record...\n\techo lap.sectors[2]        // ...and still three, so still no guard\n\n\t// The other way round for a [dyn] field, where only a guard can say:\n\tif lap.notes bounds 0 {\n\t\techo lap.notes[0]\n\t}\n}", CodePt: "type Reading {\n\tsectors: Float[3]     // três, em toda Reading que existir\n\tnotes:   Str[dyn]     // quantos forem, em cada uma separadamente\n}\n\nfunc slower(was: Reading): Reading {\n\treturn Reading([was.sectors[0] + 0.1, was.sectors[1], was.sectors[2]], was.notes)\n}\n\nproc main(): void {\n\tmut Reading lap = Reading([31.4, 28.9, 33.2], [\"limpa\"])\n\techo lap.sectors[2]        // sem guarda: a declaração prometeu três\n\n\tlap = slower(lap)          // um registro inteiramente novo...\n\techo lap.sectors[2]        // ...e ainda três, então ainda sem guarda\n\n\t// O contrário para um campo [dyn], onde só uma guarda pode dizer:\n\tif lap.notes bounds 0 {\n\t\techo lap.notes[0]\n\t}\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "Tables and CSV", TitlePt: "Tabelas e CSV", Explanation: "Table is an alias for Str[dyn][dyn]: a vector of rows of cells. It is what hive.file.csv yields, what hive.sql.raw answers with, and what hive.map.toTable builds. hive.file.csv(path, separator) reads a separated file — the separator is always written, since there are no optional parameters in Hive, so a comma is a choice like any other. Every reader answers with a Result, and naming one is what tells the build which reader to link in.", ExplanationPt: "Table é um alias para Str[dyn][dyn]: um vetor de linhas de células. É o que hive.file.csv produz, o que hive.sql.raw devolve, e o que hive.map.toTable constrói. hive.file.csv(caminho, separador) lê um arquivo separado — o separador é sempre escrito, já que não há parâmetros opcionais em Hive, então a vírgula é uma escolha como qualquer outra. Todo leitor devolve um Result, e nomear um é o que diz ao build qual leitor vincular.", Examples: []slides_0_Example{slides_0_Example{Caption: "", CaptionPt: "", Code: "proc main(): void {\n\thive.file.write(\"./tour.csv\", \"name,role\\nada,engineer\\ngrace,admiral\\n\")\n\n\tcsv := hive.file.csv(\"./tour.csv\", \",\")\n\tif csv is Result.Ok(table) {\n\t\tif len(table) > 1 {\n\t\t\techo table[1:]   // every row after the header\n\t\t}\n\t}\n}", CodePt: "proc main(): void {\n\thive.file.write(\"./tour.csv\", \"name,role\\nada,engineer\\ngrace,admiral\\n\")\n\n\tcsv := hive.file.csv(\"./tour.csv\", \",\")\n\tif csv is Result.Ok(table) {\n\t\tif len(table) > 1 {\n\t\t\techo table[1:]   // toda linha depois do cabeçalho\n\t\t}\n\t}\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "Result<T, E>", TitlePt: "Result<T, E>", Explanation: "Result is the language's one built-in generic union, and it is how every fallible operation reports itself: Result.Ok(value) or Result.Error(payload). Matching both variants with `is` is exhaustive, which lets a function written over a Result skip a dead else branch entirely.", ExplanationPt: "Result é a única união genérica nativa da linguagem, e é como toda operação falível se reporta: Result.Ok(valor) ou Result.Error(carga). Combinar as duas variantes com `is` é exaustivo, o que permite que uma função escrita sobre um Result dispense por completo um else morto.", Examples: []slides_0_Example{slides_0_Example{Caption: "", CaptionPt: "", Code: "func half(n: Int): Result<Int, Str> {\n\tif n % 2 == 0 { return Result.Ok(n / 2) }\n\treturn Result.Error(\"not even\")\n}\n\nproc main(): void {\n\tif half(4) is Result.Ok(v)      { echo v }\n\telse if half(4) is Result.Error(why) { echo why }\n}", CodePt: "func half(n: Int): Result<Int, Str> {\n\tif n % 2 == 0 { return Result.Ok(n / 2) }\n\treturn Result.Error(\"não é par\")\n}\n\nproc main(): void {\n\tif half(4) is Result.Ok(v)      { echo v }\n\telse if half(4) is Result.Error(why) { echo why }\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "Declared types", TitlePt: "Tipos declarados", Explanation: "A type with no variants is a struct; with variants, a tagged union. A field declared outside any variant is added to every variant. A value is built by calling the type or the variant: User(1, \"ada\"), Shape.Circle(5). There is no subtyping and no interface — a variant is a way of building and matching a union, not a type of its own.", ExplanationPt: "Um type sem variantes é um struct; com variantes, uma união com tag. Um campo declarado fora de qualquer variante é adicionado a todas as variantes. Um valor é construído chamando o type ou a variante: User(1, \"ada\"), Shape.Circle(5). Não existe subtipagem nem interface — uma variante é uma forma de construir e combinar uma união, não um tipo próprio.", Examples: []slides_0_Example{slides_0_Example{Caption: "A struct", CaptionPt: "Um struct", Code: "type User {\n\tid:   Int\n\tname: Str\n}\n\nproc main(): void {\n\tu := User(1, \"ada\")\n\techo u.name\n}", CodePt: "type User {\n\tid:   Int\n\tname: Str\n}\n\nproc main(): void {\n\tu := User(1, \"ada\")\n\techo u.name\n}"}, slides_0_Example{Caption: "A tagged union", CaptionPt: "Uma união com tag", Code: "type Shape {\n\tCircle    { radius: Int }\n\tRectangle { width: Int, height: Int }\n\tPoint\n}\n\nproc main(): void {\n\tp := Shape.Circle(5)\n\techo p\n}", CodePt: "type Shape {\n\tCircle    { radius: Int }\n\tRectangle { width: Int, height: Int }\n\tPoint\n}\n\nproc main(): void {\n\tp := Shape.Circle(5)\n\techo p\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "Pattern matching with is", TitlePt: "Casamento de padrões com is", Explanation: "`is` checks a value against a pattern and yields a Bool, narrowing it and binding names usable right away — in the rest of the condition after &&, and in the branch body. There is no match statement; every match is an ordinary if/else if chain. Four kinds of pattern exist: a variant, a Result, a vector matched positionally with an optional ...rest, and a string template whose {holes} bind the text between literal pieces.\n\nA hole may also say what it takes. {name} binds any text; {name is (regex)} binds only text the regex matches, and the two are one construct — so a route that wants a number can insist on one, and /users/nobody stops matching it. Nothing between those parentheses is escaped by the string, because a hole is recognised by that exact shape: {year is (\\d{4})} is four digits rather than an interpolation of 4. The regex is read at compile time, so a malformed one is a compile error naming what is wrong with it, never a surprise at runtime — and since the hole is already the name, a group inside it groups without capturing.\n\nA string pattern refuses a few things, all compile errors and all for the same reason: a pattern has to have one unambiguous reading. Two open holes may not sit side by side — with no literal text between them there is nothing to say where one ends and the next begins, though a hole that says what it takes ends itself and may sit anywhere. A hole has to be a plain binding name, so {a.b} or {len(x)} is refused. A pattern binds; it does not compute.", ExplanationPt: "`is` verifica um valor contra um padrão e produz um Bool, estreitando-o e vinculando nomes que já podem ser usados — no resto da condição depois de &&, e no corpo do ramo. Não existe instrução match; toda combinação é uma cadeia comum de if/else if. Existem quatro tipos de padrão: uma variante, um Result, um vetor combinado posicionalmente com um ...rest opcional, e um template de string cujos {buracos} vinculam o texto entre os trechos literais.\n\nUm buraco também pode dizer o que aceita. {nome} vincula qualquer texto; {nome is (regex)} vincula só o texto que a regex casa, e os dois são uma construção só — então uma rota que quer um número pode exigir um, e /users/ninguem deixa de casar com ela. Nada entre esses parênteses é escapado pela string, porque um buraco é reconhecido por aquela forma exata: {ano is (\\d{4})} são quatro dígitos, e não uma interpolação de 4. A regex é lida em tempo de compilação, então uma malformada é um erro de compilação que diz o que há de errado com ela, nunca uma surpresa em tempo de execução — e como o buraco já é o nome, um grupo dentro dele agrupa sem capturar.\n\nUm padrão de string recusa algumas coisas, todas erros de compilação e todas pelo mesmo motivo: um padrão precisa ter uma leitura só. Dois buracos abertos não podem ficar lado a lado — sem texto literal entre eles não há como dizer onde um termina e o outro começa, embora um buraco que diz o que aceita termine a si mesmo e possa ficar em qualquer lugar. Um buraco precisa ser um nome de vínculo simples, então {a.b} ou {len(x)} é recusado. Um padrão vincula; ele não calcula.", Examples: []slides_0_Example{slides_0_Example{Caption: "Variant patterns", CaptionPt: "Padrões de variante", Code: "type Shape {\n\tCircle    { radius: Int }\n\tRectangle { width: Int, height: Int }\n}\n\nproc main(): void {\n\tshape := Shape.Circle(4)\n\tif shape is Shape.Circle(r)            { echo \"circle, r={r}\" }\n\telse if shape is Shape.Rectangle(w, _) { echo \"rect, w={w}\" }\n}", CodePt: "type Shape {\n\tCircle    { radius: Int }\n\tRectangle { width: Int, height: Int }\n}\n\nproc main(): void {\n\tshape := Shape.Circle(4)\n\tif shape is Shape.Circle(r)            { echo \"círculo, r={r}\" }\n\telse if shape is Shape.Rectangle(w, _) { echo \"retângulo, w={w}\" }\n}"}, slides_0_Example{Caption: "Vector patterns", CaptionPt: "Padrões de vetor", Code: "proc main(): void {\n\tcommand := [\"move\", \"north\", \"2\"]\n\tif command is [\"move\", dir, ...steps] { echo \"{dir}, {len(steps)} more\" }\n\telse if command is [\"stop\"]           { echo \"halt\" }\n}", CodePt: "proc main(): void {\n\tcommand := [\"move\", \"north\", \"2\"]\n\tif command is [\"move\", dir, ...steps] { echo \"{dir}, {len(steps)} a mais\" }\n\telse if command is [\"stop\"]           { echo \"parar\" }\n}"}, slides_0_Example{Caption: "String patterns", CaptionPt: "Padrões de string", Code: "proc main(): void {\n\tpath := \"/users/42/posts/7\"\n\tif path is \"/users/{id}/posts/{postId}\" { echo \"{id} / {postId}\" }\n\n\t// Refused: two open holes with no literal text between them.\n\t// if path is \"/users/{id}{name}\" { }\n\t// Fine, though — the first says where it ends:\n\t// if path is \"/users/{id is (\\d+)}{name}\" { }\n\t//\n\t// Refused: a hole binds, it does not compute.\n\t// if path is \"/users/{len(path)}\" { }\n}", CodePt: "proc main(): void {\n\tpath := \"/users/42/posts/7\"\n\tif path is \"/users/{id}/posts/{postId}\" { echo \"{id} / {postId}\" }\n\n\t// Recusado: dois buracos abertos sem texto literal entre eles.\n\t// if path is \"/users/{id}{name}\" { }\n\t// Mas isto vale — o primeiro diz onde termina:\n\t// if path is \"/users/{id is (\\d+)}{name}\" { }\n\t//\n\t// Recusado: um buraco vincula, não calcula.\n\t// if path is \"/users/{len(path)}\" { }\n}"}, slides_0_Example{Caption: "A hole that says what it takes", CaptionPt: "Um buraco que diz o que aceita", Code: "func route(path: Str): Str {\n\t// Only digits reach id, so /users/nobody falls through\n\t// to the next branch instead of matching this one.\n\tif path is \"/users/{id is (\\d+)}\" {\n\t\treturn \"user #{id}\"\n\t}\n\t// The two kinds of hole mix freely — they are one construct.\n\tif path is \"/api/v{version is (\\d+)}/{rest}\" {\n\t\treturn \"v{version}, then {rest}\"\n\t}\n\t// Braces inside the parentheses are the regex's own: this is\n\t// four digits, not an interpolation.\n\tif path is \"/logs/{year is (\\d{4})}\" {\n\t\treturn \"logs for {year}\"\n\t}\n\treturn \"no match\"\n}\n\nproc main(): void {\n\techo route(\"/users/42\")        // user #42\n\techo route(\"/users/nobody\")    // no match\n\techo route(\"/api/v2/things\")   // v2, then things\n\techo route(\"/logs/2026\")       // logs for 2026\n}", CodePt: "func rota(caminho: Str): Str {\n\t// Só dígitos chegam em id, então /usuarios/ninguem cai para\n\t// o próximo ramo em vez de casar com este.\n\tif caminho is \"/usuarios/{id is (\\d+)}\" {\n\t\treturn \"usuário #{id}\"\n\t}\n\t// Os dois tipos de buraco se misturam à vontade — são uma\n\t// construção só.\n\tif caminho is \"/api/v{versao is (\\d+)}/{resto}\" {\n\t\treturn \"v{versao}, depois {resto}\"\n\t}\n\t// Chaves dentro dos parênteses são da própria regex: isto são\n\t// quatro dígitos, não uma interpolação.\n\tif caminho is \"/logs/{ano is (\\d{4})}\" {\n\t\treturn \"logs de {ano}\"\n\t}\n\treturn \"sem correspondência\"\n}\n\nproc main(): void {\n\techo rota(\"/usuarios/42\")        // usuário #42\n\techo rota(\"/usuarios/ninguem\")   // sem correspondência\n\techo rota(\"/api/v2/coisas\")      // v2, depois coisas\n\techo rota(\"/logs/2026\")          // logs de 2026\n}"}, slides_0_Example{Caption: "A malformed regex is a compile error", CaptionPt: "Uma regex malformada é um erro de compilação", Code: "proc main(): void {\n\ts := \"x\"\n\n\t// Each of these is refused before the program is built,\n\t// with a message naming what is wrong with the pattern.\n\n\t// if s is \"{a is (a{2,1})}\" { }   // {2,1} counts down\n\t// if s is \"{a is ((?=b)c)}\" { }   // no lookaround: a match\n\t//                                 // here never backtracks\n\t// if s is \"{a is ((b)\\1)}\" { }    // and no backreference\n\t// if s is \"{a is ((?<n>b))}\" { }  // the hole is already the name\n\n\techo s\n}", CodePt: "proc main(): void {\n\ts := \"x\"\n\n\t// Cada uma destas é recusada antes do programa ser construído,\n\t// com uma mensagem dizendo o que há de errado no padrão.\n\n\t// if s is \"{a is (a{2,1})}\" { }   // {2,1} conta para trás\n\t// if s is \"{a is ((?=b)c)}\" { }   // sem lookaround: um casamento\n\t//                                 // aqui nunca retrocede\n\t// if s is \"{a is ((b)\\1)}\" { }    // e sem retrorreferência\n\t// if s is \"{a is ((?<n>b))}\" { }  // o buraco já é o nome\n\n\techo s\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "func vs proc", TitlePt: "func vs proc", Explanation: "Both may perform I/O — echo, hive.file, hive.net are all fine in either. The difference is what each may touch: a func cannot declare a mutex parameter (v: mut T), so it can never write to storage its caller can see. It may call a proc — a mutex has to be handed a mut variable or a path into one, and the only mut variables in a func are its own, so what the proc writes through is the func's and not its caller's. A proc may do everything a func may, plus hold a mutex. Programs start at proc main(): void. Nothing about the declaration says how a call runs — that is decided entirely at the call site.", ExplanationPt: "Ambos podem fazer I/O — echo, hive.file, hive.net funcionam nos dois. A diferença é o que cada um pode tocar: um func não pode declarar um parâmetro mutex (v: mut T), então nunca pode escrever em um armazenamento que quem o chamou consiga ver. Ele pode chamar um proc — um mutex precisa receber uma variável mut ou um caminho para dentro de uma, e as únicas variáveis mut dentro de um func são as dele, então o que o proc escreve é do func e não de quem o chamou. Um proc pode fazer tudo que um func faz, além de guardar um mutex. Programas começam em proc main(): void. Nada na declaração diz como uma chamada roda — isso é decidido totalmente no local da chamada.", Examples: []slides_0_Example{slides_0_Example{Caption: "func: cannot touch the caller's storage", CaptionPt: "func: não pode tocar no armazenamento de quem chamou", Code: "func greet(name: Str): Str {\n\treturn \"hey {name}!\"\n}\n\nproc main(): void {\n\techo greet(\"ada\")\n}", CodePt: "func greet(name: Str): Str {\n\treturn \"olá, {name}!\"\n}\n\nproc main(): void {\n\techo greet(\"ada\")\n}"}, slides_0_Example{Caption: "proc: may hold a mutex", CaptionPt: "proc: pode guardar um mutex", Code: "proc grow(vec: mut Str[dyn], tag: Str): void {\n\tappend(vec, tag)\n}\n\nproc main(): void {\n\tmut Str[dyn] tags = [\"bee\"]\n\tgrow(tags, \"hive\")\n\techo tags\n}", CodePt: "proc grow(vec: mut Str[dyn], tag: Str): void {\n\tappend(vec, tag)\n}\n\nproc main(): void {\n\tmut Str[dyn] tags = [\"bee\"]\n\tgrow(tags, \"hive\")\n\techo tags\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "Named arguments", TitlePt: "Argumentos nomeados", Explanation: "Funcs, procs, queries and type constructors — the builtin ones included — all accept their arguments by name. A named argument may appear anywhere in the list; only the unnamed ones need to be in order, and they fill whichever parameters the named ones did not claim. A name has to exist and may not repeat, and once named arguments are used at all the call has to cover the full parameter list — so there is no way for one to read as a default that quietly went missing.\n\nThe tour you are reading is written that way itself: every screen of it is a Slide(title: …, titlePt: …, explanation: …) rather than five strings in a row whose meaning depends on counting them.", ExplanationPt: "Funcs, procs, queries e construtores de tipo — incluindo os nativos — todos aceitam seus argumentos por nome. Um argumento nomeado pode aparecer em qualquer lugar da lista; só os não nomeados precisam estar em ordem, e eles preenchem os parâmetros que os nomeados não reivindicaram. Um nome precisa existir e não pode se repetir, e uma vez que argumentos nomeados sejam usados, a chamada precisa cobrir a lista inteira de parâmetros — então nenhum deles pode ser lido como um valor padrão que sumiu sem avisar.\n\nO tour que você está lendo é escrito assim: cada tela dele é um Slide(title: …, titlePt: …, explanation: …) em vez de cinco strings em sequência cujo significado depende de contá-las.", Examples: []slides_0_Example{slides_0_Example{Caption: "", CaptionPt: "", Code: "type Colony {\n\tname:   Str\n\tapiary: Str\n\tframes: Int\n}\n\nfunc banner(text: Str, width: Int, fill: Str): Str {\n\treturn fill + \" \" + text + \" \" + fill + \" ({width})\"\n}\n\nproc main(): void {\n\techo banner(width: 40, text: \"Hive\", fill: \"~\")   // any order, once all are named\n\techo banner(\"Hive\", fill: \"~\", width: 40)          // the unnamed one goes first\n\n\tc := Colony(name: \"north\", apiary: \"orchard\", frames: 11)\n\techo c.frames\n}", CodePt: "type Colony {\n\tname:   Str\n\tapiary: Str\n\tframes: Int\n}\n\nfunc banner(text: Str, width: Int, fill: Str): Str {\n\treturn fill + \" \" + text + \" \" + fill + \" ({width})\"\n}\n\nproc main(): void {\n\techo banner(width: 40, text: \"Hive\", fill: \"~\")   // qualquer ordem, se todos nomeados\n\techo banner(\"Hive\", fill: \"~\", width: 40)          // o não nomeado vem primeiro\n\n\tc := Colony(name: \"north\", apiary: \"orchard\", frames: 11)\n\techo c.frames\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "First-class functions", TitlePt: "Funções de primeira classe", Explanation: "A callable is a value: a bare name on its own (f := takes) or a partial application with _ holes (handler(_, db)), each hole becoming a parameter in order. A func value may be used where a proc is expected, never the reverse. Three things cannot become values: a generic callable, one with a mutex parameter, and one with a statically-sized parameter beyond a single immutable binding.\n\nIts type is written like a declaration with the name dropped — func(Int): Int, proc(hive.net.HttpRequest): hive.net.HttpResponse — and that spelling is usable as a parameter, as a return, and as a variable's own type. The func/proc split is preserved through it: a func value fills a proc slot and not the other way round. mut may not appear in a function type at all, since a value has no call site to take a mutex from.", ExplanationPt: "Algo chamável é um valor: um nome sozinho (f := toma) ou uma aplicação parcial com buracos _ (handler(_, db)), cada buraco se tornando um parâmetro em ordem. Um valor func pode ser usado onde um proc é esperado, nunca o contrário. Três coisas não podem se tornar valores: um chamável genérico, um com parâmetro mutex, e um com parâmetro de tamanho estático além de uma única ligação imutável.\n\nSeu tipo se escreve como uma declaração sem o nome — func(Int): Int, proc(hive.net.HttpRequest): hive.net.HttpResponse — e essa grafia serve como parâmetro, como retorno e como tipo de uma variável. A divisão func/proc é preservada através dele: um valor func preenche um espaço de proc e não o contrário. mut não pode aparecer em um tipo de função, já que um valor não tem local de chamada de onde tomar um mutex.", Examples: []slides_0_Example{slides_0_Example{Caption: "A bare reference, and map", CaptionPt: "Uma referência solta, e map", Code: "func double(n: Int): Int { return n * 2 }\n\nproc main(): void {\n\tf := double\n\techo \"{f(21)} {map([1, 2, 3], double)}\"\n}", CodePt: "func double(n: Int): Int { return n * 2 }\n\nproc main(): void {\n\tf := double\n\techo \"{f(21)} {map([1, 2, 3], double)}\"\n}"}, slides_0_Example{Caption: "Partial application", CaptionPt: "Aplicação parcial", Code: "func add(a: Int, b: Int): Int { return a + b }\n\nproc main(): void {\n\taddFive := add(5, _)   // _ becomes the parameter\n\techo addFive(10)\n}", CodePt: "func add(a: Int, b: Int): Int { return a + b }\n\nproc main(): void {\n\taddFive := add(5, _)   // _ se torna o parâmetro\n\techo addFive(10)\n}"}, slides_0_Example{Caption: "Writing the type down", CaptionPt: "Escrevendo o tipo", Code: "func double(n: Int): Int { return n * 2 }\nfunc shout(s: Str): Str { return s + \"!\" }\n\nfunc applyTwice(f: func(Int): Int, n: Int): Int {   // as a parameter\n\treturn f(f(n))\n}\n\nfunc picked(loud: Bool): func(Str): Str {            // as a return\n\treturn shout\n}\n\nproc main(): void {\n\tfunc(Int): Int step = double                      // as a variable's type\n\techo applyTwice(step, 5)\n\techo picked(true)(\"hive\")\n}", CodePt: "func double(n: Int): Int { return n * 2 }\nfunc shout(s: Str): Str { return s + \"!\" }\n\nfunc applyTwice(f: func(Int): Int, n: Int): Int {   // como parâmetro\n\treturn f(f(n))\n}\n\nfunc picked(loud: Bool): func(Str): Str {            // como retorno\n\treturn shout\n}\n\nproc main(): void {\n\tfunc(Int): Int step = double                      // como tipo de variável\n\techo applyTwice(step, 5)\n\techo picked(true)(\"hive\")\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "Mutability and value semantics", TitlePt: "Mutabilidade e semântica de valor", Explanation: "Variables are immutable unless marked mut. Vectors, tables, maps and the declared types built from them are value types: two names for one value never observe each other's writes — unless both are mut, which is how sharing is opted into. Two mut names share the storage and not the names: writing through either reaches both, append included, but rebinding one hands that name storage of its own and leaves the other holding what they shared. A mutex parameter (v: mut T) is the one place storage crosses a call boundary on purpose, and only a proc may declare one; whether a call shares the caller's storage or a copy depends on whether the caller waits for it there or fires it with async — a call on a thread of its own is handed a copy of every argument, made on the caller's side before the thread starts.", ExplanationPt: "Variáveis são imutáveis a menos que marcadas com mut. Vetores, tabelas, maps e os tipos declarados construídos a partir deles são tipos de valor: dois nomes para um mesmo valor nunca observam as escritas um do outro — a menos que ambos sejam mut, que é como o compartilhamento é escolhido. Dois nomes mut compartilham o armazenamento, e não os nomes: escrever através de qualquer um alcança os dois, inclusive append, mas reatribuir um dá àquele nome um armazenamento próprio e deixa o outro com o que os dois compartilhavam. Um parâmetro mutex (v: mut T) é o único lugar em que o armazenamento cruza uma fronteira de chamada de propósito, e só um proc pode declarar um; se a chamada compartilha o armazenamento de quem chamou ou recebe uma cópia depende de quem chamou esperar por ela ali ou dispará-la com async — uma chamada em uma thread própria recebe uma cópia de cada argumento, feita do lado de quem chamou antes de a thread começar.", Examples: []slides_0_Example{slides_0_Example{Caption: "", CaptionPt: "", Code: "proc main(): void {\n\tmut Str[dyn] a = [\"x\", \"y\"]\n\tmut Str[dyn] b = a        // one storage, two names\n\tappend(b, \"z\")            // len(a) is now 3 too\n\techo a\n\n\tb = [\"replaced\"]          // rebinds b alone: a still holds the three\n\techo a\n}", CodePt: "proc main(): void {\n\tmut Str[dyn] a = [\"x\", \"y\"]\n\tmut Str[dyn] b = a        // um armazenamento, dois nomes\n\tappend(b, \"z\")            // len(a) também é 3 agora\n\techo a\n\n\tb = [\"replaced\"]          // reatribui só b: a ainda guarda os três\n\techo a\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "Concurrency", TitlePt: "Concorrência", Explanation: "Every call blocks its caller by default — there is no async func, no Future. What a call means is decided where it is written: f(x) waits; async f(x) fires and forgets; x := async f(x) starts it and waits only when x is read; await [f(a), f(b)] starts every call on its own thread and is one barrier resolving to a fully-typed vector. Whichever spelling starts it, a call on a thread of its own is handed a copy of every argument, worked out on the caller's side before the thread starts — so what the caller does next cannot change what the thread was given, and a mut parameter it writes through writes through that copy. hive.task.sleep(ms) parks only the calling thread, which is why two sleeps started together finish in about the longer one rather than the sum. Any wait can be bounded with `with timeout <ms>`, turning the result into a Result rather than a crash.\n\nThe await-all is the only await there is. Every call in its list starts on its own thread and the whole thing is one barrier, resolving to a vector as long and as typed as the list that made it — so its elements are indexable without a guard. Over calls that answer with void there is nothing to resolve to, so it is a statement instead: \"both of these, then carry on\". A vector holds one type, so calls that answer differently are given names instead — that is what the async binding above is for. await [] is a compile error, and a timeout on an await-all is one deadline across the whole barrier rather than one each.", ExplanationPt: "Toda chamada bloqueia quem a chamou por padrão — não existe async func, não existe Future. O que uma chamada significa é decidido onde ela é escrita: f(x) espera; async f(x) dispara e esquece; x := async f(x) a inicia e só espera quando x é lido; await [f(a), f(b)] inicia cada chamada em sua própria thread e é uma única barreira que se resolve em um vetor totalmente tipado. Seja qual for a grafia que a inicia, uma chamada em uma thread própria recebe uma cópia de cada argumento, resolvida do lado de quem chamou antes de a thread começar — então o que quem chamou fizer depois não muda o que a thread recebeu, e um parâmetro mut que ela escreve é essa cópia. hive.task.sleep(ms) estaciona apenas a thread que chamou, e é por isso que dois sleeps iniciados juntos terminam em cerca do mais longo, e não na soma. Qualquer espera pode ser limitada com `with timeout <ms>`, transformando o resultado em um Result em vez de uma falha.\n\nO await-all é o único await que existe. Cada chamada da sua lista começa em uma thread própria e o conjunto é uma única barreira, que se resolve em um vetor tão longo e tão tipado quanto a lista que o formou — então seus elementos são indexáveis sem guarda. Sobre chamadas que respondem void não há nada em que se resolver, então ele é uma instrução: \"as duas, e então siga\". Um vetor guarda um tipo só, então chamadas que respondem de formas diferentes recebem nomes em vez disso — é para isso que serve a ligação async acima. await [] é um erro de compilação, e um timeout sobre um await-all é um prazo único para a barreira inteira, e não um para cada.", Examples: []slides_0_Example{slides_0_Example{Caption: "Starting work and waiting for it", CaptionPt: "Iniciando um trabalho e esperando por ele", Code: "proc slow(label: Str, ms: Int): Str {\n\thive.task.sleep(ms)\n\treturn label\n}\n\nproc main(): void {\n\ta := async slow(\"a\", 30)      // starts now\n\tb := async slow(\"b\", 30)      // starts now, alongside\n\techo \"{a} and {b}\"             // waits for both, right here\n}", CodePt: "proc slow(label: Str, ms: Int): Str {\n\thive.task.sleep(ms)\n\treturn label\n}\n\nproc main(): void {\n\ta := async slow(\"a\", 30)      // inicia agora\n\tb := async slow(\"b\", 30)      // inicia agora, em paralelo\n\techo \"{a} and {b}\"             // espera pelos dois, aqui mesmo\n}"}, slides_0_Example{Caption: "Bounding a wait", CaptionPt: "Limitando uma espera", Code: "proc slow(label: Str, ms: Int): Str {\n\thive.task.sleep(ms)\n\treturn label\n}\n\nproc main(): void {\n\tif slow(\"c\", 300) with timeout 50 is Result.Error(err) {\n\t\techo \"gave up after {err.waited}ms\"\n\t}\n}", CodePt: "proc slow(label: Str, ms: Int): Str {\n\thive.task.sleep(ms)\n\treturn label\n}\n\nproc main(): void {\n\tif slow(\"c\", 300) with timeout 50 is Result.Error(err) {\n\t\techo \"desistiu depois de {err.waited}ms\"\n\t}\n}"}, slides_0_Example{Caption: "Many at once, as a value", CaptionPt: "Vários de uma vez, como valor", Code: "proc slow(label: Str, ms: Int): Str {\n\thive.task.sleep(ms)\n\treturn label\n}\n\nproc main(): void {\n\tboth := await [slow(\"a\", 40), slow(\"b\", 40)]   // one barrier, a Str[2]\n\techo both\n\techo both[1]                                    // a static length: no guard\n}", CodePt: "proc slow(label: Str, ms: Int): Str {\n\thive.task.sleep(ms)\n\treturn label\n}\n\nproc main(): void {\n\tboth := await [slow(\"a\", 40), slow(\"b\", 40)]   // uma barreira, um Str[2]\n\techo both\n\techo both[1]                                    // tamanho estático: sem guarda\n}"}, slides_0_Example{Caption: "Many at once, as a statement", CaptionPt: "Vários de uma vez, como instrução", Code: "proc note(text: Str): void {\n\thive.task.sleep(20)\n\techo text\n}\n\nproc main(): void {\n\t// Nothing to resolve to, so it is a statement: a batch of side effects\n\t// finished before the next line, each on its own thread.\n\tawait [note(\"first\"), note(\"second\")]\n\techo \"both notes are in\"\n}", CodePt: "proc note(text: Str): void {\n\thive.task.sleep(20)\n\techo text\n}\n\nproc main(): void {\n\t// Nada em que se resolver, então é uma instrução: um lote de efeitos\n\t// concluído antes da próxima linha, cada um em sua própria thread.\n\tawait [note(\"first\"), note(\"second\")]\n\techo \"os dois avisos chegaram\"\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "Generics", TitlePt: "Genéricos", Explanation: "A name in a signature that is neither a builtin nor a declared type is a type variable, and it makes the callable generic. A variable is pinned down by where it appears in the parameters — including inside a parameter's own function type. Nothing about it is dynamic at run time: every call site is resolved at compile time, and one concrete copy is emitted per distinct set of type arguments — first_Str, first_Int — with no boxing and no dispatch.\n\nA type declaration whose fields mention variables is generic in them too, in first-appearance order, and is written out where it is used: Box<Str>, Either<Str, Int>. Each instantiation gets its own clone, its own JSON codec and its own structural digest, so Either<Str, Int> and Either<Int, Str> are different types all the way down to the wire.", ExplanationPt: "Um nome em uma assinatura que não é nem uma função nativa nem um tipo declarado é uma variável de tipo, e ela torna o chamável genérico. Uma variável é fixada por onde ela aparece nos parâmetros — inclusive dentro do próprio tipo de função de um parâmetro. Nada nisso é dinâmico em tempo de execução: todo local de chamada é resolvido em tempo de compilação, e uma cópia concreta é emitida para cada conjunto distinto de argumentos de tipo — first_Str, first_Int — sem boxing e sem despacho.\n\nUma declaração de tipo cujos campos mencionam variáveis também é genérica nelas, na ordem em que aparecem pela primeira vez, e é escrita por extenso onde é usada: Box<Str>, Either<Str, Int>. Cada instanciação ganha seu próprio clone, seu próprio codec JSON e seu próprio digest estrutural, então Either<Str, Int> e Either<Int, Str> são tipos diferentes até o fim, inclusive na rede.", Examples: []slides_0_Example{slides_0_Example{Caption: "A generic callable", CaptionPt: "Um chamável genérico", Code: "func first(v: T[]): Result<T, Bool> {\n\tif len(v) > 0 {\n\t\treturn Result.Ok(v[0])\n\t}\n\treturn Result.Error(false)\n}\n\nproc main(): void {\n\ta := first([1, 2, 3])       // first_Int\n\tb := first([\"x\", \"y\"])      // first_Str, a separate copy\n\techo \"{a} {b}\"\n}", CodePt: "func first(v: T[]): Result<T, Bool> {\n\tif len(v) > 0 {\n\t\treturn Result.Ok(v[0])\n\t}\n\treturn Result.Error(false)\n}\n\nproc main(): void {\n\ta := first([1, 2, 3])       // first_Int\n\tb := first([\"x\", \"y\"])      // first_Str, uma cópia separada\n\techo \"{a} {b}\"\n}"}, slides_0_Example{Caption: "A generic type", CaptionPt: "Um tipo genérico", Code: "type Box {\n\titems: T[dyn]\n\tlabel: Str\n}\n\ntype Either {\n\tLeft  { left: A }\n\tRight { right: B }\n}\n\nproc main(): void {\n\tBox<Str> people = Box([\"ada\", \"grace\"], \"people\")\n\tBox<Int> counts = Box([1, 2, 3], \"counts\")\n\tEither<Str, Int> answer = Either.Right(42)\n\n\techo \"{people.label}: {people.items}\"\n\techo \"{counts.label}: {counts.items}\"\n\tif answer is Either.Right(n) { echo n }\n}", CodePt: "type Box {\n\titems: T[dyn]\n\tlabel: Str\n}\n\ntype Either {\n\tLeft  { left: A }\n\tRight { right: B }\n}\n\nproc main(): void {\n\tBox<Str> people = Box([\"ada\", \"grace\"], \"people\")\n\tBox<Int> counts = Box([1, 2, 3], \"counts\")\n\tEither<Str, Int> answer = Either.Right(42)\n\n\techo \"{people.label}: {people.items}\"\n\techo \"{counts.label}: {counts.items}\"\n\tif answer is Either.Right(n) { echo n }\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "Modules", TitlePt: "Módulos", Explanation: "import brings another file's declarations into scope, reached through a name — the file's own by default, or whatever `as` renames it to. The path itself says what kind of import it is: a bare path is a Hive file on disk, an https:// path is a file inside a git repository — pinned to a commit, a tag or a branch — and hive.* names a standard library module, reached the same way and adding nothing to the build unless it is actually used. A library module's default name is its own last segment, so import hive.crypto is already crypto and `as` is only for when you want some other name — and the alias is a spelling rather than an import: crypto.sha256 and hive.crypto.sha256 are the same call, both are always available, and neither changes what is linked into the build. A Hive module's .hive is never written, because the path names a module and the file is only where it lives; the path is relative to the importing file's own directory, not to wherever you happen to run the compiler from. Everything a module declares is visible — there is no pub/priv — and an import may not be named hive, since that name belongs to the standard library.\n\nNames carry no baggage across the boundary. A type or a callable only ever means what the module you are reading says it means, so two files may each declare their own Align and both stay distinct types; an imported one is constructed, annotated and matched through the same name its module is reached by. That is also what makes shadowing a builtin safe: a map declared in one file leaves every other file's bare map alone. And an imported callable is an ordinary value, so it is partially applicable and passable like any other.\n\nA remote import is an ordinary import with a repository in front of it. https://host/owner/repo is the repository and everything after it is the path inside it, so a revision goes where the repository ends — repo@a1b2c3, repo@v1.2.0, repo@main, a commit or a tag or a branch. The module is named after the file inside the repository rather than after the repository or its host, which is what makes moving a module out to one change nothing about how the code that uses it reads. Everything is fetched once, into a cache shared by every program on the machine that wants that commit, and nothing touches the network when the clone is already there. A remote module is also a different module from the same file read locally: two copies of one file are two modules, with types of their own.\n\nAn import that names no revision is pinned on first use. The commit it resolved to is written into a lock file beside the entrypoint — main.hive becomes main.hive-lock — and every later build reads that instead of asking the network what the latest is. Keep it in version control: it is what makes another machine build the same program. Deleting a line from it takes the latest of that repository again.\n\nImport cycles are refused, whether direct or round any number of steps, and the error prints the loop it found so you do not have to reconstruct it.", ExplanationPt: "import traz as declarações de outro arquivo para o escopo, alcançadas através de um nome — o próprio nome do arquivo por padrão, ou o que `as` renomear para. O próprio caminho diz que tipo de import é: um caminho simples é um arquivo Hive em disco, um caminho https:// é um arquivo dentro de um repositório git — fixado em um commit, uma tag ou um branch — e hive.* nomeia um módulo da biblioteca padrão, alcançado da mesma forma e não adicionando nada ao build a menos que seja de fato usado. O nome padrão de um módulo da biblioteca é o seu próprio último segmento, então import hive.crypto já é crypto e `as` só serve para quando você quer outro nome — e o alias é uma grafia, não um import: crypto.sha256 e hive.crypto.sha256 são a mesma chamada, as duas estão sempre disponíveis, e nenhuma muda o que entra no build. O .hive de um módulo Hive nunca é escrito, porque o caminho nomeia um módulo e o arquivo é só onde ele mora; o caminho é relativo ao diretório do próprio arquivo que importa, não a onde você por acaso roda o compilador. Tudo que um módulo declara é visível — não existe pub/priv — e um import não pode se chamar hive, já que esse nome pertence à biblioteca padrão.\n\nNomes não levam bagagem através da fronteira. Um tipo ou um chamável só significa o que o módulo que você está lendo diz que ele significa, então dois arquivos podem declarar cada um o seu Align e os dois continuam tipos distintos; um importado é construído, anotado e casado através do mesmo nome pelo qual seu módulo é alcançado. É isso também que torna seguro sombrear uma função nativa: um map declarado em um arquivo deixa em paz o map solto de todos os outros. E um chamável importado é um valor comum, então é parcialmente aplicável e passável como qualquer outro.\n\nUm import remoto é um import comum com um repositório na frente. https://host/dono/repo é o repositório e tudo depois disso é o caminho dentro dele, então uma revisão vai onde o repositório termina — repo@a1b2c3, repo@v1.2.0, repo@main, um commit ou uma tag ou um branch. O módulo é nomeado a partir do arquivo dentro do repositório e não do repositório ou do host dele, e é isso que faz com que mover um módulo para outro repositório não mude nada em como o código que o usa se lê. Tudo é buscado uma vez, para um cache compartilhado por todo programa da máquina que queira aquele commit, e nada toca a rede quando o clone já está lá. Um módulo remoto também é um módulo diferente do mesmo arquivo lido localmente: duas cópias de um arquivo são dois módulos, com tipos próprios.\n\nUm import que não nomeia revisão é fixado no primeiro uso. O commit para o qual ele resolveu é escrito em um arquivo de lock ao lado do entrypoint — main.hive vira main.hive-lock — e toda compilação seguinte lê aquilo em vez de perguntar à rede qual é o mais recente. Mantenha-o no controle de versão: é o que faz outra máquina construir o mesmo programa. Apagar uma linha dele volta a pegar o mais recente daquele repositório.\n\nCiclos de import são recusados, sejam diretos ou dando a volta em qualquer número de passos, e o erro imprime o laço que encontrou para você não ter que reconstruí-lo.", Examples: []slides_0_Example{slides_0_Example{Caption: "A library module, shortened", CaptionPt: "Um módulo da biblioteca, encurtado", Code: (((((((((((("import hive.crypto\nimport hive.net as web\n\nproc main(): void {\n\t// The default name is the module's last segment, so " + "`") + "crypto") + "` ") + "needs no\n\t// ") + "`") + "as") + "` ") + "at all. ") + "`") + "as") + "` ") + "is for when you want a different name than that.\n\techo crypto.sha256(\"hive\")\n\n\t// An alias is only a spelling: this is the very same call, and it works\n\t// in a program that imported nothing.\n\techo hive.crypto.sha256(\"hive\")\n\n\tif web.resolve(\"localhost\") is Result.Ok(addresses) {\n\t\techo addresses\n\t}\n}"), CodePt: (((((((((((("import hive.crypto\nimport hive.net as web\n\nproc main(): void {\n\t// O nome padrão é o último segmento do módulo, então " + "`") + "crypto") + "` ") + "não\n\t// precisa de ") + "`") + "as") + "` ") + "algum. ") + "`") + "as") + "` ") + "é para quando você quer outro nome.\n\techo crypto.sha256(\"hive\")\n\n\t// Um alias é só uma grafia: esta é exatamente a mesma chamada, e ela\n\t// funciona em um programa que não importou nada.\n\techo hive.crypto.sha256(\"hive\")\n\n\tif web.resolve(\"localhost\") is Result.Ok(addresses) {\n\t\techo addresses\n\t}\n}")}, slides_0_Example{Caption: "lib/text.hive", CaptionPt: "lib/text.hive", Code: "type Align {\n\tLeft\n\tRight\n}\n\nfunc pad(s: Str, width: Int, side: Align): Str {\n\tmut out := s\n\tfor ; len(out) < width; {\n\t\tif side is Align.Left { out = out + \".\" } else { out = \".\" + out }\n\t}\n\treturn out\n}\n\nfunc repeat(s: Str, times: Int): Str {\n\tmut out := \"\"\n\tfor i := 0; i < times; i++ { out = out + s }\n\treturn out\n}", CodePt: "type Align {\n\tLeft\n\tRight\n}\n\nfunc pad(s: Str, width: Int, side: Align): Str {\n\tmut out := s\n\tfor ; len(out) < width; {\n\t\tif side is Align.Left { out = out + \".\" } else { out = \".\" + out }\n\t}\n\treturn out\n}\n\nfunc repeat(s: Str, times: Int): Str {\n\tmut out := \"\"\n\tfor i := 0; i < times; i++ { out = out + s }\n\treturn out\n}"}, slides_0_Example{Caption: "main.hive, beside it", CaptionPt: "main.hive, ao lado dele", Code: "import ./lib/text\n\n// This file's Align and lib/text's Align are two different types: a name\n// only ever means what the module you read it in says it means.\ntype Align {\n\tCentre\n}\n\nproc main(): void {\n\tmine := Align.Centre()\n\tif mine is Align.Centre { echo \"ours\" }\n\n\techo text.pad(\"total\", 16, text.Align.Left())\n\n\t// An imported callable is an ordinary value.\n\trule := text.repeat(\"~\", _)\n\techo rule(16)\n}", CodePt: "import ./lib/text\n\n// O Align deste arquivo e o Align de lib/text são dois tipos diferentes: um\n// nome só significa o que o módulo em que você o lê diz que ele significa.\ntype Align {\n\tCentre\n}\n\nproc main(): void {\n\tmine := Align.Centre()\n\tif mine is Align.Centre { echo \"o nosso\" }\n\n\techo text.pad(\"total\", 16, text.Align.Left())\n\n\t// Um chamável importado é um valor comum.\n\trule := text.repeat(\"~\", _)\n\techo rule(16)\n}"}, slides_0_Example{Caption: "A module in a repository", CaptionPt: "Um módulo em um repositório", Code: (((("// The repository is " + "`") + "https://host/owner/repo") + "`") + "; everything after it is the\n// path inside it, and the .hive is left off exactly as it is on disk.\nimport https://github.com/owner/repo/src/text\n\n// A revision goes where the repository ends: a commit, a tag or a branch.\nimport https://github.com/owner/repo@v1.2.0/src/pricing\n\n// A Go file inside one, under the same rules — .go still compulsory.\nimport https://github.com/owner/repo@a1b2c3/go/slugs.go as slugs\n\nproc main(): void {\n\t// The name is the file's, not the repository's or the host's: moving a\n\t// module out to one changes nothing about how this line reads.\n\techo text.repeat(\"=\", 28)\n\techo pricing.line(\"Beeswax\", 450)\n\techo slugs.slugify(\"Hive Tool\")\n}"), CodePt: (((("// O repositório é " + "`") + "https://host/dono/repo") + "`") + "; tudo depois disso é o caminho\n// dentro dele, e o .hive fica de fora exatamente como fica em disco.\nimport https://github.com/owner/repo/src/text\n\n// Uma revisão vai onde o repositório termina: commit, tag ou branch.\nimport https://github.com/owner/repo@v1.2.0/src/pricing\n\n// Um arquivo Go dentro de um, sob as mesmas regras — .go ainda obrigatório.\nimport https://github.com/owner/repo@a1b2c3/go/slugs.go as slugs\n\nproc main(): void {\n\t// O nome é o do arquivo, não o do repositório nem o do host: mover um\n\t// módulo para um repositório não muda nada em como esta linha se lê.\n\techo text.repeat(\"=\", 28)\n\techo pricing.line(\"Beeswax\", 450)\n\techo slugs.slugify(\"Hive Tool\")\n}")}, slides_0_Example{Caption: "What the first build leaves behind", CaptionPt: "O que a primeira compilação deixa para trás", Code: "$ hive check main.hive\nNo problems found in main.hive (3s)\n\n$ cat main.hive-lock\n# Written by the Hive compiler: the commit each remote import was\n# resolved to. Keep this in version control — it is what makes another\n# machine build the same program. Delete a line to take the latest of\n# that repository again.\nhttps://github.com/owner/repo 4f2a9c1e8b7d6a5f4e3d2c1b0a9f8e7d6c5b4a39", CodePt: "$ hive check main.hive\nNo problems found in main.hive (3s)\n\n$ cat main.hive-lock\n# Written by the Hive compiler: the commit each remote import was\n# resolved to. Keep this in version control — it is what makes another\n# machine build the same program. Delete a line to take the latest of\n# that repository again.\nhttps://github.com/owner/repo 4f2a9c1e8b7d6a5f4e3d2c1b0a9f8e7d6c5b4a39"}, slides_0_Example{Caption: "A cycle, refused", CaptionPt: "Um ciclo, recusado", Code: "$ hive check main.hive\nlib/inventory.hive:1: this import forms a cycle:\n\n    lib/inventory.hive\n      -> lib/pricing.hive\n      -> lib/inventory.hive", CodePt: "$ hive check main.hive\nlib/inventory.hive:1: this import forms a cycle:\n\n    lib/inventory.hive\n      -> lib/pricing.hive\n      -> lib/inventory.hive"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "Importing a Go file", TitlePt: "Importando um arquivo Go", Explanation: "Hive is Go behind the curtain, so a Go file next to a Hive one can be called from it directly. The path ends in .go, which is compulsory — and it is the opposite of the rule for a Hive module, whose .hive is never written. That is deliberate: a path names a module and the file is only where it lives, but nothing about import ./lib/util should leave you wondering which of the two languages you are about to open.\n\nEvery value is deep-copied in both directions, and that is the whole point of the boundary. A Go function can sort the slice it was handed, keep a reference to it, or write through it later, and none of that can reach the Hive value it came from. It is also why an imported Go function arrives as a func rather than a proc.\n\nOnly what Go exports is reachable, which there means a capitalised name. Hive names callables in camelCase, so a name is reached with its first letter lowered — Grams is measures.grams — while a type keeps its PascalCase and a struct's fields lower their first letter the same way. The signatures are read by the Go toolchain rather than guessed at, and the boundary is narrow: string, int, float64, bool, []T, [][]string, a map with a scalar key, a struct the file exports, and (T, error), which arrives as Result<T, Str> since Go's way of reporting failure is Hive's. A pointer, a channel, an interface, a function value, a variadic parameter or a type from another package is refused, and the message names the parameter it could not take. A Go map has no order of its own, so one arriving in Hive has its keys sorted.", ExplanationPt: "Hive é Go por trás da cortina, então um arquivo Go ao lado de um arquivo Hive pode ser chamado diretamente dele. O caminho termina em .go, o que é obrigatório — e é o oposto da regra de um módulo Hive, cujo .hive nunca é escrito. Isso é de propósito: um caminho nomeia um módulo e o arquivo é só onde ele mora, mas nada em import ./lib/util deveria deixar você em dúvida sobre qual das duas linguagens está prestes a abrir.\n\nTodo valor é copiado profundamente nas duas direções, e esse é todo o sentido da fronteira. Uma função Go pode ordenar o slice que recebeu, guardar uma referência a ele, ou escrever através dele mais tarde, e nada disso pode alcançar o valor Hive de onde ele veio. É também por isso que uma função Go importada chega como um func e não como um proc.\n\nSó o que o Go exporta é alcançável, o que lá significa um nome com inicial maiúscula. Hive nomeia chamáveis em camelCase, então um nome é alcançado com a primeira letra minúscula — Grams é measures.grams — enquanto um tipo mantém seu PascalCase e os campos de um struct baixam a primeira letra da mesma forma. As assinaturas são lidas pela toolchain do Go em vez de adivinhadas, e a fronteira é estreita: string, int, float64, bool, []T, [][]string, um map com chave escalar, um struct que o arquivo exporta, e (T, error), que chega como Result<T, Str>, já que o jeito do Go de reportar falha é o mesmo do Hive. Um ponteiro, um canal, uma interface, um valor de função, um parâmetro variádico ou um tipo de outro pacote é recusado, e a mensagem nomeia o parâmetro que ela não conseguiu receber.", Examples: []slides_0_Example{slides_0_Example{Caption: "lib/measures.go", CaptionPt: "lib/measures.go", Code: "package measures\n\nimport (\n\t\"fmt\"\n\t\"strings\"\n)\n\n// Every field of this struct crosses the boundary, so the whole struct does.\ntype Weight struct {\n\tGrams int\n\tLabel string\n}\n\n// Exported, so Hive reaches it with its first letter lowered: measures.grams.\nfunc Grams(w Weight) int {\n\treturn w.Grams\n}\n\n// Go's way of reporting failure is Hive's: (T, error) is Result<T, Str>.\nfunc Parse(s string) (Weight, error) {\n\tif !strings.HasSuffix(s, \"g\") {\n\t\treturn Weight{}, fmt.Errorf(\"%q does not end in g\", s)\n\t}\n\treturn Weight{Grams: len(s) * 100, Label: s}, nil\n}", CodePt: "package measures\n\nimport (\n\t\"fmt\"\n\t\"strings\"\n)\n\n// Todo campo deste struct cruza a fronteira, então o struct inteiro cruza.\ntype Weight struct {\n\tGrams int\n\tLabel string\n}\n\n// Exportado, então o Hive o alcança com a inicial minúscula: measures.grams.\nfunc Grams(w Weight) int {\n\treturn w.Grams\n}\n\n// A forma do Go de reportar falha é a do Hive: (T, error) é Result<T, Str>.\nfunc Parse(s string) (Weight, error) {\n\tif !strings.HasSuffix(s, \"g\") {\n\t\treturn Weight{}, fmt.Errorf(\"%q does not end in g\", s)\n\t}\n\treturn Weight{Grams: len(s) * 100, Label: s}, nil\n}"}, slides_0_Example{Caption: "main.hive, beside it", CaptionPt: "main.hive, ao lado dele", Code: "import ./lib/measures.go\n\nproc main(): void {\n\ttool := measures.Weight(899, \"Hive tool\")\n\techo measures.grams(tool)\n\n\tparsed := measures.parse(\"450g\")\n\tif parsed is Result.Ok(w) {\n\t\techo \"{w.label}: {w.grams}\"\n\t} else if parsed is Result.Error(why) {\n\t\techo why\n\t}\n}", CodePt: "import ./lib/measures.go\n\nproc main(): void {\n\ttool := measures.Weight(899, \"Hive tool\")\n\techo measures.grams(tool)\n\n\tparsed := measures.parse(\"450g\")\n\tif parsed is Result.Ok(w) {\n\t\techo \"{w.label}: {w.grams}\"\n\t} else if parsed is Result.Error(why) {\n\t\techo why\n\t}\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "Maps", TitlePt: "Mapas", Explanation: "hive.map.Map<K, T> is the one collection that is not a vector. A key is compared and hashed whole, so it must be a Str, Int, Float, Bool, Atom, or a declared type whose every field is one of those. new() says only \"empty\", so it has to land somewhere already typed — a mut declaration, here. set and delete need that mut map; get answers by key with a Result, has asks the same question without the value, and keys/values walk it in the order keys were set, which is what makes echoing one reproducible from run to run. fromTable and toTable cross to and from a Table of two-cell rows, for a map whose keys and values are both Str.", ExplanationPt: "hive.map.Map<K, T> é a única coleção que não é um vetor. Uma chave é comparada e tem seu hash calculado por completo, então precisa ser uma Str, Int, Float, Bool, Atom, ou um tipo declarado cujos campos sejam todos um desses. new() diz apenas \"vazio\", então precisa chegar em algum lugar já tipado — uma declaração mut, aqui. set e delete precisam desse map mut; get responde por chave com um Result, has faz a mesma pergunta sem o valor, e keys/values percorrem na ordem em que as chaves foram definidas, o que faz um echo nele sair igual em toda execução. fromTable e toTable cruzam de e para uma Table de linhas de duas células, para um map cujas chaves e valores são ambos Str.", Examples: []slides_0_Example{slides_0_Example{Caption: "Setting, getting and walking", CaptionPt: "Definindo, obtendo e percorrendo", Code: "proc main(): void {\n\tmut hive.map.Map<Str, Int> counts = hive.map.new()\n\thive.map.set(counts, \"bees\", 12)\n\thive.map.set(counts, \"hives\", 3)\n\n\tif hive.map.get(counts, \"bees\") is Result.Ok(n) {\n\t\techo n\n\t}\n\techo hive.map.has(counts, \"wasps\")\n\thive.map.delete(counts, \"hives\")\n\techo hive.map.keys(counts)\n\techo hive.map.values(counts)\n}", CodePt: "proc main(): void {\n\tmut hive.map.Map<Str, Int> counts = hive.map.new()\n\thive.map.set(counts, \"bees\", 12)\n\thive.map.set(counts, \"hives\", 3)\n\n\tif hive.map.get(counts, \"bees\") is Result.Ok(n) {\n\t\techo n\n\t}\n\techo hive.map.has(counts, \"wasps\")\n\thive.map.delete(counts, \"hives\")\n\techo hive.map.keys(counts)\n\techo hive.map.values(counts)\n}"}, slides_0_Example{Caption: "Crossing to and from a Table", CaptionPt: "Cruzando de e para uma Table", Code: "proc main(): void {\n\tmut hive.map.Map<Str, Str> labels = hive.map.new()\n\thive.map.set(labels, \"bees\", \"many\")\n\techo hive.map.toTable(labels)\n\n\techo hive.map.get(hive.map.fromTable([[\"wasps\", \"few\"]]), \"wasps\")\n}", CodePt: "proc main(): void {\n\tmut hive.map.Map<Str, Str> labels = hive.map.new()\n\thive.map.set(labels, \"bees\", \"many\")\n\techo hive.map.toTable(labels)\n\n\techo hive.map.get(hive.map.fromTable([[\"wasps\", \"few\"]]), \"wasps\")\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "Conversions", TitlePt: "Conversões", Explanation: "hive.conv is pure, so it works in a func as well as a proc. ceil, floor and round turn a Float into an Int, rounding half away from zero; itf widens an Int to a Float, and its/fts render a number as a Str. sti and stf parse text back into a number and, since parsing can fail, answer with a Result whose ConversionError carries the offending input. There is no implicit numeric conversion anywhere else in the language, which is exactly why this module exists: an Int never quietly becomes a Float.", ExplanationPt: "hive.conv é puro, então funciona tanto em um func quanto em um proc. ceil, floor e round transformam um Float em um Int, arredondando o meio para longe do zero; itf amplia um Int para um Float, e its/fts renderizam um número como uma Str. sti e stf interpretam texto de volta em um número e, como interpretar pode falhar, respondem com um Result cujo ConversionError carrega a entrada culpada. Não existe conversão numérica implícita em nenhum outro lugar da linguagem, e é exatamente por isso que este módulo existe: um Int nunca se torna um Float silenciosamente.", Examples: []slides_0_Example{slides_0_Example{Caption: "Parsing text", CaptionPt: "Interpretando texto", Code: "proc main(): void {\n\tif hive.conv.sti(\"42\") is Result.Ok(n) {\n\t\techo n + 1\n\t}\n}", CodePt: "proc main(): void {\n\tif hive.conv.sti(\"42\") is Result.Ok(n) {\n\t\techo n + 1\n\t}\n}"}, slides_0_Example{Caption: "Rounding and widening", CaptionPt: "Arredondando e ampliando", Code: "proc main(): void {\n\thalf := hive.conv.itf(5) / 2.0   // widen first — Int and Float never mix\n\techo hive.conv.round(half)\n\techo hive.conv.fts(half)\n\techo hive.conv.its(hive.conv.ceil(half))\n}", CodePt: "proc main(): void {\n\thalf := hive.conv.itf(5) / 2.0   // amplia primeiro — Int e Float nunca se misturam\n\techo hive.conv.round(half)\n\techo hive.conv.fts(half)\n\techo hive.conv.its(hive.conv.ceil(half))\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "Math", TitlePt: "Matemática", Explanation: "hive.math is pure, and every one of its thirteen functions takes and answers with a Float — there is no integer twin, because Hive never widens a number behind your back. pi is the one constant; sin, cos, tan and their inverses asin, acos and atan2 cover angles; sqrt, hypot and abs cover distances; min, max and clamp cover bounds, with clamp defined as exactly min(max(v, low), high), so a range given backwards answers with the high bound. None of it reports an error: a value outside a function's domain simply answers with whatever non-finite Float the arithmetic already produces.", ExplanationPt: "hive.math é puro, e cada uma de suas treze funções recebe e responde com um Float — não existe uma versão inteira, porque Hive nunca amplia um número pelas suas costas. pi é a única constante; sin, cos, tan e suas inversas asin, acos e atan2 cobrem ângulos; sqrt, hypot e abs cobrem distâncias; min, max e clamp cobrem limites, com clamp definido como exatamente min(max(v, low), high), então um intervalo passado de trás para frente responde com o limite superior. Nada disso reporta um erro: um valor fora do domínio de uma função simplesmente responde com o Float não finito que a aritmética já produz.", Examples: []slides_0_Example{slides_0_Example{Caption: "", CaptionPt: "", Code: "proc main(): void {\n\tradius := 3.0\n\tarea := hive.math.pi() * radius ** 2.0\n\techo area\n\n\techo hive.math.sqrt(hive.math.hypot(3.0, 4.0))\n\techo hive.math.max(hive.math.min(2.0, 5.0), 1.0)\n\techo hive.math.clamp(120.0, 0.0, 100.0)\n}", CodePt: "proc main(): void {\n\tradius := 3.0\n\tarea := hive.math.pi() * radius ** 2.0\n\techo area\n\n\techo hive.math.sqrt(hive.math.hypot(3.0, 4.0))\n\techo hive.math.max(hive.math.min(2.0, 5.0), 1.0)\n\techo hive.math.clamp(120.0, 0.0, 100.0)\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "JSON", TitlePt: "JSON", Explanation: "hive.json treats a Hive type declaration as the JSON schema itself. Neither of the two calls that do the work names a format: encode(value, codec) is a builtin, T.decode(text, codec) is the decoder a declaration derives, and what says JSON is the codec handed to them — hive.json.codec(). That is what lets another format be another codec rather than a change to the language: hive.crypto.jwtCodec(secret), hive.net.queryParamsCodec() and hive.term.codec() are three more, and they go in these very same two places.\n\nA codec is an ordinary value — held in a variable, passed to a function, kept in a field — because its type already says which format it is: hive.codec.Codec<E> names the error decoding in it fails with, and every module with a codec owns its own, so this one is a Codec<hive.json.JsonError>. The encoder is still written out of the declaration wherever the codec is used, which is why none of this needs reflection. Decoding answers with a Result<T, E> whose Error carries the exact path that failed. Encoding derives from the static type, so it cannot fail: there is no Result to check on the way out. T.Variant.decode reads one variant's payload without the key the union is wrapped in, for a document whose variant you already know.\n\nA field may be annotated, and every annotation is about the codecs rather than about the field: -- JSON as <name>; is the key the field is read from and written as, in both directions, so a round trip agrees with itself; -- URL as <name>; and -- Flag as <name>; say the same for a query string and a command-line flag; and -- Default as <literal>; is what to read where the key is absent or null — which is what turns a required field into an optional one. None of them reaches the three calls below.\n\nThree more calls need no type at all. flatten(text) flattens any document into [path, value] rows, which get looks up by a dotted path, and table(text) reads a JSON array of flat objects as a headered Table.", ExplanationPt: "hive.json trata uma declaração de tipo Hive como o próprio schema JSON. Nenhuma das duas chamadas que fazem o trabalho menciona um formato: encode(value, codec) é um builtin, T.decode(text, codec) é o decodificador que a declaração deriva, e quem diz JSON é o codec passado a elas — hive.json.codec(). É isso que permite que outro formato seja outro codec, e não uma mudança na linguagem: hive.crypto.jwtCodec(secret), hive.net.queryParamsCodec() e hive.term.codec() são mais três, e vão exatamente nesses mesmos dois lugares.\n\nUm codec é um valor comum — guardado numa variável, passado a uma função, mantido num campo — porque o seu tipo já diz qual formato ele é: hive.codec.Codec<E> nomeia o erro com que decodificar nele falha, e cada módulo com um codec tem o seu, então este é um Codec<hive.json.JsonError>. O codificador continua sendo escrito a partir da declaração onde quer que o codec seja usado, que é o motivo de nada disso precisar de reflexão. Decodificar responde com um Result<T, E> cujo Error traz o caminho exato que falhou. Codificar sai do tipo estático, então não tem como falhar: não existe Result para conferir na saída. T.Variant.decode lê o conteúdo de uma variante sem a chave que envolve a união, para um documento cuja variante você já conhece.\n\nUm campo pode ser anotado, e toda anotação é sobre os codecs, e não sobre o campo: -- JSON as <nome>; é a chave pela qual o campo é lido e escrito, nas duas direções, então uma ida e volta concorda consigo mesma; -- URL as <nome>; e -- Flag as <nome>; dizem o mesmo para uma query string e uma flag de linha de comando; e -- Default as <literal>; é o que ler quando a chave está ausente ou é null — que é o que transforma um campo obrigatório em opcional. Nenhuma delas alcança as três chamadas abaixo.\n\nOutras três chamadas não precisam de tipo nenhum. flatten(text) achata qualquer documento em linhas [caminho, valor], que get consulta por um caminho com pontos, e table(text) lê um array JSON de objetos sem aninhamento como uma Table com cabeçalho.", Examples: []slides_0_Example{slides_0_Example{Caption: "The declaration is the schema, and the codec is the format", CaptionPt: "A declaração é o schema, e o codec é o formato", Code: "type Greeting { name: Str }\n\nproc main(): void {\n\tbody := encode(Greeting(\"ada\"), hive.json.codec())\n\techo body\n\n\tparsed := Greeting.decode(body, hive.json.codec())\n\tif parsed is Result.Ok(g) {\n\t\techo \"hello, {g.name}!\"\n\t}\n}", CodePt: "type Greeting { name: Str }\n\nproc main(): void {\n\tbody := encode(Greeting(\"ada\"), hive.json.codec())\n\techo body\n\n\tparsed := Greeting.decode(body, hive.json.codec())\n\tif parsed is Result.Ok(g) {\n\t\techo \"olá, {g.name}!\"\n\t}\n}"}, slides_0_Example{Caption: "A document that does not fit says exactly where", CaptionPt: "Um documento que não encaixa diz exatamente onde", Code: "type Greeting { name: Str  times: Int }\n\nproc main(): void {\n\tif Greeting.decode(\"\\{\\\"name\\\":\\\"ada\\\"}\", hive.json.codec()) is Result.Error(why) {\n\t\techo \"{why.path}: expected {why.expected}, found {why.found}\"\n\t}\n}", CodePt: "type Greeting { name: Str  times: Int }\n\nproc main(): void {\n\tif Greeting.decode(\"\\{\\\"name\\\":\\\"ada\\\"}\", hive.json.codec()) is Result.Error(why) {\n\t\techo \"{why.path}: esperava {why.expected}, encontrou {why.found}\"\n\t}\n}"}, slides_0_Example{Caption: "The annotations steer both halves", CaptionPt: "As anotações guiam as duas metades", Code: "type User {\n\tname: Str -- JSON as username;\n\tage: Int\n\tfirstName: Str -- Default as \"\";\n}\n\nproc main(): void {\n\t// The key is username in both directions, so a round trip agrees.\n\techo encode(User(\"ada\", 36, \"Ada\"), hive.json.codec())\n\n\t// firstName is absent here, and the Default is what turns a\n\t// required field into an optional one.\n\tif User.decode(\"\\{\\\"username\\\":\\\"ada\\\",\\\"age\\\":36}\", hive.json.codec()) is Result.Ok(u) {\n\t\techo \"[{u.firstName}]\"\n\t}\n}", CodePt: "type User {\n\tname: Str -- JSON as username;\n\tage: Int\n\tfirstName: Str -- Default as \"\";\n}\n\nproc main(): void {\n\t// A chave é username nas duas direções, então a ida e volta concorda.\n\techo encode(User(\"ada\", 36, \"Ada\"), hive.json.codec())\n\n\t// firstName está ausente aqui, e o Default é o que transforma um\n\t// campo obrigatório em opcional.\n\tif User.decode(\"\\{\\\"username\\\":\\\"ada\\\",\\\"age\\\":36}\", hive.json.codec()) is Result.Ok(u) {\n\t\techo \"[{u.firstName}]\"\n\t}\n}"}, slides_0_Example{Caption: "A document with no declaration behind it", CaptionPt: "Um documento sem declaração por trás", Code: "proc main(): void {\n\t// Any shape at all, as [path, value] rows.\n\tif hive.json.flatten(\"\\{\\\"keys\\\":\\{\\\"layout\\\":\\\"us\\\"}}\") is Result.Ok(rows) {\n\t\tif hive.json.get(rows, \"keys.layout\") is Result.Ok(layout) {\n\t\t\techo layout\n\t\t}\n\t}\n\n\t// One shape in particular: a list of records, as a headered Table.\n\tif hive.json.table(\"[\\{\\\"city\\\":\\\"Belém\\\",\\\"pop\\\":1506}]\") is Result.Ok(t) {\n\t\techo row(t, \"Belém\")\n\t}\n}", CodePt: "proc main(): void {\n\t// Qualquer formato, em linhas [caminho, valor].\n\tif hive.json.flatten(\"\\{\\\"keys\\\":\\{\\\"layout\\\":\\\"us\\\"}}\") is Result.Ok(rows) {\n\t\tif hive.json.get(rows, \"keys.layout\") is Result.Ok(layout) {\n\t\t\techo layout\n\t\t}\n\t}\n\n\t// Um formato específico: uma lista de registros, como Table com cabeçalho.\n\tif hive.json.table(\"[\\{\\\"city\\\":\\\"Belém\\\",\\\"pop\\\":1506}]\") is Result.Ok(t) {\n\t\techo row(t, \"Belém\")\n\t}\n}"}, slides_0_Example{Caption: "A codec is a value", CaptionPt: "Um codec é um valor", Code: "type Greeting { name: Str }\n\n// Generic in the codec's error type, so one function writes every format.\nfunc written(codec: hive.codec.Codec<E>): Str {\n\treturn encode(Greeting(\"ada\"), codec)\n}\n\nproc main(): void {\n\tasJson := hive.json.codec()\n\techo written(asJson)\n\techo written(hive.net.queryParamsCodec())\n\techo written(hive.term.codec())\n}", CodePt: "type Greeting { name: Str }\n\n// Genérica no tipo de erro do codec, então uma função só escreve qualquer formato.\nfunc written(codec: hive.codec.Codec<E>): Str {\n\treturn encode(Greeting(\"ada\"), codec)\n}\n\nproc main(): void {\n\tasJson := hive.json.codec()\n\techo written(asJson)\n\techo written(hive.net.queryParamsCodec())\n\techo written(hive.term.codec())\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "Files", TitlePt: "Arquivos", Explanation: "hive.file is general filesystem access, and the table readers — csv, xlsx, ods — live there too. Contents move as a Str that holds bytes rather than validated text, so a binary file survives a read/write round trip untouched. read and lines pull a file in, write and append push one out, and writeSecret is write leaving the file readable by the account that wrote it and nobody else — mode 0600 on Linux and macOS, a protected single-account access list on Windows, settled before any of the secret is in it. delete, copy and move do what they say; exists and size ask without reading, and list and makeDir cover a directory. Everything fallible answers with a Result<_, hive.file.FileError> whose reason is a short tag: NotFound, Permission, Exists or Io.", ExplanationPt: "hive.file é acesso geral ao sistema de arquivos, e os leitores de tabela — csv, xlsx, ods — também moram lá. Conteúdos se movem como uma Str que guarda bytes em vez de texto validado, então um arquivo binário sobrevive intacto a uma ida e volta de leitura/escrita. read e lines trazem um arquivo para dentro, write e append escrevem de volta, e writeSecret é o write que deixa o arquivo legível só para a conta que o escreveu — modo 0600 no Linux e no macOS, uma lista de acesso de uma conta só no Windows, resolvido antes de qualquer pedaço do segredo chegar nele. delete, copy e move fazem o que dizem; exists e size perguntam sem ler, e list e makeDir cobrem um diretório. Tudo que é falível responde com um Result<_, hive.file.FileError> cujo reason é uma tag curta: NotFound, Permission, Exists ou Io.", Examples: []slides_0_Example{slides_0_Example{Caption: "Writing and reading", CaptionPt: "Escrevendo e lendo", Code: "proc main(): void {\n\thive.file.makeDir(\"./out\")\n\thive.file.write(\"./out/report.txt\", \"done\")\n\thive.file.append(\"./out/report.txt\", \"\\nand done again\")\n\n\tif hive.file.read(\"./out/report.txt\") is Result.Ok(text) {\n\t\techo text\n\t}\n}", CodePt: "proc main(): void {\n\thive.file.makeDir(\"./out\")\n\thive.file.write(\"./out/report.txt\", \"concluído\")\n\thive.file.append(\"./out/report.txt\", \"\\ne concluído outra vez\")\n\n\tif hive.file.read(\"./out/report.txt\") is Result.Ok(text) {\n\t\techo text\n\t}\n}"}, slides_0_Example{Caption: "Asking about a file", CaptionPt: "Perguntando sobre um arquivo", Code: "proc main(): void {\n\thive.file.makeDir(\"./out\")\n\thive.file.write(\"./out/report.txt\", \"done\")\n\n\techo hive.file.exists(\"./out/report.txt\")\n\tif hive.file.size(\"./out/report.txt\") is Result.Ok(n) {\n\t\techo n\n\t}\n\tif hive.file.list(\"./out\") is Result.Ok(names) {\n\t\techo names\n\t}\n}", CodePt: "proc main(): void {\n\thive.file.makeDir(\"./out\")\n\thive.file.write(\"./out/report.txt\", \"concluído\")\n\n\techo hive.file.exists(\"./out/report.txt\")\n\tif hive.file.size(\"./out/report.txt\") is Result.Ok(n) {\n\t\techo n\n\t}\n\tif hive.file.list(\"./out\") is Result.Ok(names) {\n\t\techo names\n\t}\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "Spreadsheets", TitlePt: "Planilhas", Explanation: "A table is read by naming its reader. hive.file.xlsx and hive.file.ods each read a whole workbook and answer with a Result<Table[dyn], hive.file.TableError> — one Table per sheet, in document order, so an empty sheet is an empty Table rather than a missing one. Both readers are dependency-free. An xlsx cell arrives as the file stores it, with one exception: a date is kept internally as a day count, so the cell's own number format is consulted to catch exactly those and render them as a real date. An ods stores real dates already, so nothing there needs undoing.", ExplanationPt: "Uma tabela é lida nomeando seu leitor. hive.file.xlsx e hive.file.ods leem cada um uma planilha inteira e respondem com um Result<Table[dyn], hive.file.TableError> — uma Table por aba, na ordem do documento, então uma aba vazia é uma Table vazia em vez de uma ausente. Os dois leitores não têm dependências. Uma célula de xlsx chega como o arquivo a guarda, com uma exceção: uma data é guardada internamente como uma contagem de dias, então o formato de número da própria célula é consultado para pegar exatamente essas e renderizá-las como uma data de verdade. Um ods já guarda datas de verdade, então nada ali precisa ser desfeito.", Examples: []slides_0_Example{slides_0_Example{Caption: "", CaptionPt: "", Code: "proc main(): void {\n\tsheets := hive.file.xlsx(\"./ledger.xlsx\")\n\tif sheets is Result.Ok(tables) {\n\t\tif tables bounds 0 {\n\t\t\techo len(tables[0])\n\t\t}\n\t} else if sheets is Result.Error(err) {\n\t\techo \"no workbook yet: {err.message}\"\n\t}\n}", CodePt: "proc main(): void {\n\tsheets := hive.file.xlsx(\"./ledger.xlsx\")\n\tif sheets is Result.Ok(tables) {\n\t\tif tables bounds 0 {\n\t\t\techo len(tables[0])\n\t\t}\n\t} else if sheets is Result.Error(err) {\n\t\techo \"ainda sem planilha: {err.message}\"\n\t}\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "Terminal and processes", TitlePt: "Terminal e processos", Explanation: "hive.term is line-oriented terminal I/O: print writes to stdout, status writes to stderr — what a program is doing, as against what it was asked for — isTerminal says whether standard error is a person or a pipe, and read/readSecret block for a line of input, the second with the terminal's echo turned off. args() reads the command line, and exit(code) ends the program on the spot.\n\nRunning another program is three calls, and the first two differ in who is talking to the terminal: exec hands back everything the command wrote and turns a nonzero exit into a Result.Error, while attach gives the command this program's own terminal and answers with its exit status. Neither ever touches a shell, so nothing in an argument can be read as one — the arguments are a vector rather than a line of text, and that is the whole of the safety story.\n\nexecWith is the third, and it is exec with an environment overlay: a Map<Str, Str> whose pairs are set for the command on top of the environment this program was started with. An overlay is deliberately all it offers. A replaced environment is a different and much sharper tool — a command with no PATH finds no programs and one with no HOME finds no caches — and nothing that wants to set one variable wants those as well. So an empty map is exactly exec, byte for byte, and a name the environment already holds is replaced rather than joined by a second. This is what hive build --target is built on, and it is the only thing in the compiler that needs an environment at all.\n\ncodec() reads command-line flags into a type of your own through T.decode, and encode writes one back. A flag always opens with --: a field is --name value or --name=value, named by the field itself or by -- Flag as <name>; written without its dashes, so --dry-run is dryRun: Bool -- Flag as dry-run;. A Bool standing alone is true, and a vector is its flag repeated. T.decode reads one Str, split the way a shell splits a line so a quoted value stays one word — so join the arguments first, quoting again any that holds a space, since args() hands them over with the shell's quotes already gone.", ExplanationPt: "hive.term é I/O de terminal orientado a linhas: print escreve no stdout e status escreve no stderr — o que o programa está fazendo, e não o que pediram a ele. isTerminal diz se a saída de erro é uma pessoa ou um pipe, e read/readSecret esperam por uma linha de entrada, o segundo com o eco do terminal desligado. args() lê a linha de comando, e exit(code) encerra o programa na hora.\n\nRodar outro programa são três chamadas, e a diferença entre as duas primeiras é quem fala com o terminal: exec devolve tudo que o comando escreveu e transforma uma saída diferente de zero em Result.Error, enquanto attach entrega ao comando o terminal do próprio programa e responde com o status de saída dele. Nenhuma das duas passa por um shell, então nada dentro de um argumento pode ser lido como shell — os argumentos são um vetor, não uma linha de texto, e a segurança se resume a isso.\n\nexecWith é a terceira: é o exec com uma camada por cima do ambiente, um Map<Str, Str> cujos pares valem para o comando somados ao ambiente com que este programa foi iniciado. É só isso que ela oferece, e de propósito. Trocar o ambiente inteiro é uma ferramenta bem mais afiada — um comando sem PATH não acha programa nenhum, e um sem HOME não acha cache nenhum — e quem só quer definir uma variável não quer isso junto. Um map vazio, portanto, é exatamente o exec, byte a byte, e um nome que o ambiente já tem é substituído em vez de ganhar um segundo ao lado. É nisso que o hive build --target se apoia, e é a única coisa no compilador que precisa mexer com ambiente.\n\ncodec() lê flags de linha de comando para um tipo seu através de T.decode, e encode escreve um de volta. Uma flag sempre começa com --: um campo é --nome valor ou --nome=valor, com o nome do próprio campo ou o de -- Flag as <nome>; escrito sem os traços, então --dry-run é dryRun: Bool -- Flag as dry-run;. Um Bool sozinho é true, e um vetor é a sua flag repetida. T.decode lê uma única Str, dividida do jeito que um shell divide uma linha para que um valor entre aspas continue sendo uma palavra só — então junte os argumentos antes, pondo de novo entre aspas os que têm espaço, já que args() os entrega com as aspas do shell já removidas.", Examples: []slides_0_Example{slides_0_Example{Caption: "Running another program", CaptionPt: "Rodando outro programa", Code: "proc main(): void {\n\tresult := hive.term.exec(\"hostname\", [], \"\")\n\tif result is Result.Ok(output) {\n\t\techo \"this machine is called: {output}\"\n\t} else if result is Result.Error(why) {\n\t\techo \"could not run hostname: {why.message}\"\n\t}\n}", CodePt: "proc main(): void {\n\tresult := hive.term.exec(\"hostname\", [], \"\")\n\tif result is Result.Ok(output) {\n\t\techo \"esta máquina se chama: {output}\"\n\t} else if result is Result.Error(why) {\n\t\techo \"não foi possível rodar hostname: {why.message}\"\n\t}\n}"}, slides_0_Example{Caption: "Running one with an environment", CaptionPt: "Rodando um com um ambiente", Code: (((("proc main(): void {\n\tmut hive.map.Map<Str, Str> asked = hive.map.new()\n\thive.map.set(asked, \"GOOS\", \"linux\")\n\thive.map.set(asked, \"GOARCH\", \"arm64\")\n\n\t// An overlay, not a replacement: these two on top of everything this\n\t// program was started with, so " + "`") + "go") + "` ") + "is still found on the PATH.\n\tresult := hive.term.execWith(\"go\", [\"env\", \"GOOS\", \"GOARCH\"], \"\", asked)\n\tif result is Result.Ok(output) {\n\t\techo output\n\t} else if result is Result.Error(why) {\n\t\techo \"no Go on this machine: {why.message}\"\n\t}\n}"), CodePt: (((("proc main(): void {\n\tmut hive.map.Map<Str, Str> asked = hive.map.new()\n\thive.map.set(asked, \"GOOS\", \"linux\")\n\thive.map.set(asked, \"GOARCH\", \"arm64\")\n\n\t// Uma sobreposição, não uma substituição: estas duas por cima de tudo\n\t// com que este programa foi iniciado, então " + "`") + "go") + "` ") + "continua no PATH.\n\tresult := hive.term.execWith(\"go\", [\"env\", \"GOOS\", \"GOARCH\"], \"\", asked)\n\tif result is Result.Ok(output) {\n\t\techo output\n\t} else if result is Result.Error(why) {\n\t\techo \"sem Go nesta máquina: {why.message}\"\n\t}\n}")}, slides_0_Example{Caption: "A secret, read and then kept", CaptionPt: "Um segredo, lido e depois guardado", Code: "proc main(): void {\n\techo \"API token:\"\n\ttoken := hive.term.readSecret()\n\n\t// Nobody but this account can read the file, and that is true\n\t// before the first byte of the token is in it.\n\tif hive.file.writeSecret(\"./token.txt\", token) is Result.Error(why) {\n\t\techo \"could not keep it: {why.message}\"\n\t}\n}", CodePt: "proc main(): void {\n\techo \"Token da API:\"\n\ttoken := hive.term.readSecret()\n\n\t// Ninguém além desta conta consegue ler o arquivo, e isso já vale\n\t// antes do primeiro byte do token entrar nele.\n\tif hive.file.writeSecret(\"./token.txt\", token) is Result.Error(why) {\n\t\techo \"não deu para guardar: {why.message}\"\n\t}\n}"}, slides_0_Example{Caption: "Asking about the terminal itself", CaptionPt: "Perguntando sobre o próprio terminal", Code: "proc main(): void {\n\techo hive.term.isTerminal()\n\techo hive.term.args()\n}", CodePt: "proc main(): void {\n\techo hive.term.isTerminal()\n\techo hive.term.args()\n}"}, slides_0_Example{Caption: "Flags into a type", CaptionPt: "Flags para um tipo", Code: "type Options {\n\tname: Str\n\tdryRun: Bool -- Flag as dry-run; Default as false;\n\tcount: Int -- Default as 10;\n}\n\nproc main(): void {\n\t// hive.term.args() joined into one line, a value with a space quoted again.\n\tline := \"--name 'Ada Lovelace' --dry-run --count=3\"\n\tif Options.decode(line, hive.term.codec()) is Result.Ok(o) {\n\t\techo \"{o.name} {o.dryRun} {o.count}\"\n\t}\n\n\t// A flag with no Default has to be there.\n\tif Options.decode(\"--dry-run\", hive.term.codec()) is Result.Error(why) {\n\t\techo \"{why.path}: expected {why.expected}, found {why.found}\"\n\t}\n\n\techo encode(Options(\"Ada Lovelace\", true, 3), hive.term.codec())\n}", CodePt: "type Options {\n\tname: Str\n\tdryRun: Bool -- Flag as dry-run; Default as false;\n\tcount: Int -- Default as 10;\n}\n\nproc main(): void {\n\t// hive.term.args() juntado em uma linha, com aspas de novo no valor que tem espaço.\n\tline := \"--name 'Ada Lovelace' --dry-run --count=3\"\n\tif Options.decode(line, hive.term.codec()) is Result.Ok(o) {\n\t\techo \"{o.name} {o.dryRun} {o.count}\"\n\t}\n\n\t// Uma flag sem Default precisa estar lá.\n\tif Options.decode(\"--dry-run\", hive.term.codec()) is Result.Error(why) {\n\t\techo \"{why.path}: esperava {why.expected}, encontrou {why.found}\"\n\t}\n\n\techo encode(Options(\"Ada Lovelace\", true, 3), hive.term.codec())\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "Time", TitlePt: "Tempo", Explanation: "Times in Hive are plain Ints — Unix seconds — so they need no type of their own to pass around, store or compare. now() reads the clock; timezone() and timezoneOffset() say where the program thinks it is; format renders a time in local time with a strftime-style template — %Y, %m, %d, %H, %M, %S and the rest. An escape it doesn't recognise passes straight through rather than failing.\n\ndateFrom(year, month, day) and dateTimeFrom(year, month, day, hour, minute, second) build a stamp for a date you name, in local time, and a field out of range carries into the next — dateFrom(2026, 3, 0) is February's last day. year(), month(), day(), weekday() (1 Monday to 7 Sunday), lastDayOfMonth(), hour(), minute(), second() and millisecond() each read one field of now.", ExplanationPt: "Horários em Hive são Ints simples — segundos Unix — então não precisam de um tipo próprio para circular, ser guardados ou comparados. now() lê o relógio; timezone() e timezoneOffset() dizem onde o programa pensa que está; format renderiza um horário em hora local com um template no estilo strftime — %Y, %m, %d, %H, %M, %S e o resto. Um escape que ele não reconhece passa direto em vez de falhar.\n\ndateFrom(ano, mês, dia) e dateTimeFrom(ano, mês, dia, hora, minuto, segundo) constroem o carimbo de uma data que você nomeia, em hora local, e um campo fora do intervalo transborda para o seguinte — dateFrom(2026, 3, 0) é o último dia de fevereiro. year(), month(), day(), weekday() (1 segunda a 7 domingo), lastDayOfMonth(), hour(), minute(), second() e millisecond() leem, cada um, um campo de agora.", Examples: []slides_0_Example{slides_0_Example{Caption: "The clock", CaptionPt: "O relógio", Code: "proc main(): void {\n\tstamp := hive.time.now()\n\techo hive.time.format(stamp, \"%Y-%m-%d %H:%M\")\n\techo hive.time.timezone()\n\techo hive.time.timezoneOffset()\n}", CodePt: "proc main(): void {\n\tstamp := hive.time.now()\n\techo hive.time.format(stamp, \"%Y-%m-%d %H:%M\")\n\techo hive.time.timezone()\n\techo hive.time.timezoneOffset()\n}"}, slides_0_Example{Caption: "A date you name", CaptionPt: "Uma data que você nomeia", Code: "proc main(): void {\n\tday := hive.time.dateFrom(2026, 9, 14)\n\techo hive.time.format(day, \"%Y-%m-%d %A\")\n\n\t// Day 0 of March is the last day of February.\n\techo hive.time.format(hive.time.dateFrom(2026, 3, 0), \"%Y-%m-%d\")\n\n\t// This month, from its first day to its last.\n\tyear := hive.time.year()\n\tmonth := hive.time.month()\n\tfirst := hive.time.dateFrom(year, month, 1)\n\tlast := hive.time.dateFrom(year, month, hive.time.lastDayOfMonth())\n\techo hive.time.format(first, \"%d/%m\") + \" to \" + hive.time.format(last, \"%d/%m\")\n}", CodePt: "proc main(): void {\n\tday := hive.time.dateFrom(2026, 9, 14)\n\techo hive.time.format(day, \"%Y-%m-%d %A\")\n\n\t// O dia 0 de março é o último dia de fevereiro.\n\techo hive.time.format(hive.time.dateFrom(2026, 3, 0), \"%Y-%m-%d\")\n\n\t// Este mês, do primeiro ao último dia.\n\tyear := hive.time.year()\n\tmonth := hive.time.month()\n\tfirst := hive.time.dateFrom(year, month, 1)\n\tlast := hive.time.dateFrom(year, month, hive.time.lastDayOfMonth())\n\techo hive.time.format(first, \"%d/%m\") + \" até \" + hive.time.format(last, \"%d/%m\")\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "Environment variables", TitlePt: "Variáveis de ambiente", Explanation: "hive.env.get resolves a name in one order: the .env file beside the program, then the .env in the parent folder, then the OS environment — and answers with a Result, since a variable that was never set is an ordinary outcome rather than a crash. The .env file is read once, on the first get: blank lines and # comments are ignored, an export prefix is allowed and stripped, and a quoted value has its quotes removed.", ExplanationPt: "hive.env.get resolve um nome em uma ordem: o arquivo .env ao lado do programa, depois o .env na pasta pai, depois o ambiente do sistema operacional — e responde com um Result, já que uma variável que nunca foi definida é um resultado comum, não uma falha. O arquivo .env é lido uma vez, no primeiro get: linhas vazias e comentários com # são ignorados, um prefixo export é permitido e removido, e um valor entre aspas tem as aspas removidas.", Examples: []slides_0_Example{slides_0_Example{Caption: "", CaptionPt: "", Code: "proc main(): void {\n\tresult := hive.env.get(\"DATABASE_URL\")\n\tif result is Result.Ok(url) {\n\t\techo \"DATABASE_URL is {url}\"\n\t} else if result is Result.Error(why) {\n\t\techo \"DATABASE_URL is not set: {why.message}\"\n\t}\n}", CodePt: "proc main(): void {\n\tresult := hive.env.get(\"DATABASE_URL\")\n\tif result is Result.Ok(url) {\n\t\techo \"DATABASE_URL é {url}\"\n\t} else if result is Result.Error(why) {\n\t\techo \"DATABASE_URL não está definida: {why.message}\"\n\t}\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "Networking: HTTP", TitlePt: "Rede: HTTP", Explanation: "hive.net's HTTP corner needs no external dependency in either direction. httpServe(port, handler) starts a server that blocks forever, calling a proc(HttpRequest): HttpResponse once per request on a thread of its own — exactly the shape of the handler serving this very page. httpRequest performs a client request and answers with a Result, since getting no response at all — a bad URL, a refused connection, a timeout — is a Result.Error rather than an empty HttpResponse. resolve and localAddress round out the module: the first looks up every address behind a name, the second is the address another machine would reach this one on.\n\nurlEncode(text) percent-encodes every byte but letters, digits, -, ., _ and ~, so the answer is safe in a path segment and a query alike. queryParamsCodec() is a codec for the query string itself, without its leading ?: encode writes a type's fields as key=value pairs and a vector as its key repeated, T.decode reads them back, and -- URL as <name>; names a parameter the way JSON as names a key. A query string is flat, so only a type without variants whose fields are values, or vectors of values, travels as one — anything else is a compile error at the call.", ExplanationPt: "O canto HTTP de hive.net não precisa de dependência externa em nenhuma das direções. httpServe(port, handler) inicia um servidor que bloqueia para sempre, chamando um proc(HttpRequest): HttpResponse uma vez por requisição, cada uma em sua própria thread — exatamente a forma do handler que está servindo esta própria página. httpRequest realiza uma requisição de cliente e responde com um Result, já que não obter resposta alguma — uma URL inválida, uma conexão recusada, um timeout — é um Result.Error em vez de um HttpResponse vazio. resolve e localAddress completam o módulo: o primeiro busca todos os endereços atrás de um nome, o segundo é o endereço pelo qual outra máquina alcançaria esta.\n\nurlEncode(texto) codifica em porcentagem todo byte que não seja letra, dígito, -, ., _ ou ~, então a resposta é segura tanto num trecho de caminho quanto numa query. queryParamsCodec() é um codec para a própria query string, sem o ? do começo: encode escreve os campos de um tipo como pares chave=valor e um vetor como a sua chave repetida, T.decode lê de volta, e -- URL as <nome>; nomeia um parâmetro do jeito que JSON as nomeia uma chave. Uma query string é plana, então só um tipo sem variantes cujos campos sejam valores, ou vetores de valores, viaja como uma — qualquer outra coisa é erro de compilação na chamada.", Examples: []slides_0_Example{slides_0_Example{Caption: "A server", CaptionPt: "Um servidor", Code: "proc handle(request: hive.net.HttpRequest): hive.net.HttpResponse {\n\theaders := [[\"Content-Type\", \"text/plain\"]]\n\treturn hive.net.HttpResponse(200, headers, \"hello, {request.url}!\\n\")\n}\n\nproc main(): void {\n\thive.net.httpServe(8080, handle)\n}", CodePt: "proc handle(request: hive.net.HttpRequest): hive.net.HttpResponse {\n\theaders := [[\"Content-Type\", \"text/plain\"]]\n\treturn hive.net.HttpResponse(200, headers, \"olá, {request.url}!\\n\")\n}\n\nproc main(): void {\n\thive.net.httpServe(8080, handle)\n}"}, slides_0_Example{Caption: "Query strings", CaptionPt: "Query strings", Code: "type Search {\n\tterm: Str -- URL as q;\n\tpage: Int -- Default as 1;\n\ttags: Str[dyn] -- URL as tag;\n}\n\nproc main(): void {\n\techo hive.net.urlEncode(\"rust & go\")\n\n\tparams := encode(Search(\"rust & go\", 2, [\"web\", \"cli\"]), hive.net.queryParamsCodec())\n\techo \"https://example.com/search?{params}\"\n\n\tif Search.decode(\"q=hive+lang&tag=web\", hive.net.queryParamsCodec()) is Result.Ok(s) {\n\t\techo \"{s.term}, page {s.page}\"\n\t}\n}", CodePt: "type Search {\n\tterm: Str -- URL as q;\n\tpage: Int -- Default as 1;\n\ttags: Str[dyn] -- URL as tag;\n}\n\nproc main(): void {\n\techo hive.net.urlEncode(\"rust & go\")\n\n\tparams := encode(Search(\"rust & go\", 2, [\"web\", \"cli\"]), hive.net.queryParamsCodec())\n\techo \"https://example.com/search?{params}\"\n\n\tif Search.decode(\"q=hive+lang&tag=web\", hive.net.queryParamsCodec()) is Result.Ok(s) {\n\t\techo \"{s.term}, página {s.page}\"\n\t}\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "Networking: WebSockets", TitlePt: "Rede: WebSockets", Explanation: "hive.net's WebSocket support is RFC 6455 end to end: the handshake, frame headers, masking, ping/pong and fragmentation are the runtime's business, and a program only ever sees whole messages as a Str. wsServe(port, handler) serves connections the same shape httpServe serves requests, calling a proc(WsConnection): void once per connection; wsConnect dials one as a client, and wsRequest reads back the HttpRequest that opened it. wsClose ends a connection from this side; a peer that hangs up on its own answers as \"Closed\" — the ordinary end of a conversation, not a failure to report.", ExplanationPt: "O suporte a WebSocket de hive.net é RFC 6455 do início ao fim: o handshake, os cabeçalhos de frame, a mascaração, o ping/pong e a fragmentação são responsabilidade do runtime, e um programa só vê mensagens inteiras como uma Str. wsServe(port, handler) serve conexões da mesma forma que httpServe serve requisições, chamando um proc(WsConnection): void uma vez por conexão; wsConnect disca uma como cliente, e wsRequest lê de volta o HttpRequest que a abriu. wsClose termina uma conexão deste lado; um par que desliga por conta própria responde como \"Closed\" — o fim comum de uma conversa, não uma falha a ser reportada.", Examples: []slides_0_Example{slides_0_Example{Caption: "", CaptionPt: "", Code: "proc handle(conn: hive.net.WsConnection): void {\n\tif hive.net.wsReceive(conn) is Result.Ok(msg) {\n\t\thive.net.wsSend(conn, \"echo: {msg}\")\n\t}\n}\n\nproc main(): void {\n\thive.net.wsServe(8081, handle)\n}", CodePt: "proc handle(conn: hive.net.WsConnection): void {\n\tif hive.net.wsReceive(conn) is Result.Ok(msg) {\n\t\thive.net.wsSend(conn, \"eco: {msg}\")\n\t}\n}\n\nproc main(): void {\n\thive.net.wsServe(8081, handle)\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "Networking: raw TCP", TitlePt: "Rede: TCP bruto", Explanation: "Raw TCP is a stream, not a queue of messages, so hive.net's socket calls move bytes rather than whole messages. socketConnect dials out and socketServe(port, handler) listens, calling a proc(SocketConnection): void once per connection; socketReceive(conn, bytes) does a short read — fewer bytes than asked for is normal, not an error — while socketReceiveLine reads up to the next newline and drops it. socketPeer names who is on the other end, and socketClose ends the stream. A SocketError's reason is Connect, Closed, Send or Receive.", ExplanationPt: "TCP bruto é um fluxo, não uma fila de mensagens, então as chamadas de socket de hive.net movem bytes em vez de mensagens inteiras. socketConnect disca para fora e socketServe(port, handler) escuta, chamando um proc(SocketConnection): void uma vez por conexão; socketReceive(conn, bytes) faz uma leitura curta — receber menos bytes do que foi pedido é normal, não um erro — enquanto socketReceiveLine lê até a próxima nova linha e a descarta. socketPeer nomeia quem está do outro lado, e socketClose termina o fluxo. O reason de um SocketError é Connect, Closed, Send ou Receive.", Examples: []slides_0_Example{slides_0_Example{Caption: "", CaptionPt: "", Code: "proc handle(conn: hive.net.SocketConnection): void {\n\tif hive.net.socketReceiveLine(conn) is Result.Ok(line) {\n\t\thive.net.socketSend(conn, \"you said: {line}\\n\")\n\t}\n\thive.net.socketClose(conn)\n}\n\nproc main(): void {\n\thive.net.socketServe(9000, handle)\n}", CodePt: "proc handle(conn: hive.net.SocketConnection): void {\n\tif hive.net.socketReceiveLine(conn) is Result.Ok(line) {\n\t\thive.net.socketSend(conn, \"você disse: {line}\\n\")\n\t}\n\thive.net.socketClose(conn)\n}\n\nproc main(): void {\n\thive.net.socketServe(9000, handle)\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "Cryptography", TitlePt: "Criptografia", Explanation: "hive.crypto is pure, so it works in a func as well as a proc. sha256 and sha512 hash, and hmacSha256 authenticates with a key. encrypt and decrypt seal a string under a password with AES-256-GCM, using a fresh salt and nonce every call so the same text never encrypts alike twice; base64Encode and base64Decode move between text and base64, and randomHex draws bytes at random. HS256 JSON Web Tokens are covered by jwtCodec, which is a codec like hive.json.codec() and goes in the same two places: encode(claims, hive.crypto.jwtCodec(secret)) signs a value of your own type into a token, and Claims.decode(token, hive.crypto.jwtCodec(secret)) checks the signature and the exp/nbf claims before reading them back. Only HS256 is accepted, closing off the classic algorithm-confusion attack outright. A token that does not check out is a hive.crypto.JwtError, just as claims of the wrong shape are, its found opening with the reason — so a forged token and claims of the wrong shape are one failure to handle rather than two. jwtDecode reads the claims without verifying anything, for inspection only, and jwtHeader reads just alg/typ/kid, which is what a program rotating keys needs before it can name the secret.", ExplanationPt: "hive.crypto é puro, então funciona tanto em um func quanto em um proc. sha256 e sha512 calculam hash, e hmacSha256 autentica com uma chave. encrypt e decrypt selam uma string sob uma senha com AES-256-GCM, usando um salt e um nonce novos em cada chamada, então o mesmo texto nunca é criptografado da mesma forma duas vezes; base64Encode e base64Decode movem entre texto e base64, e randomHex sorteia bytes aleatórios. JSON Web Tokens HS256 são cobertos por jwtCodec, que é um codec como hive.json.codec() e vai nos mesmos dois lugares: encode(claims, hive.crypto.jwtCodec(secret)) assina um valor de um tipo seu dentro de um token, e Claims.decode(token, hive.crypto.jwtCodec(secret)) verifica a assinatura e as claims exp/nbf antes de lê-las de volta. Apenas HS256 é aceito, eliminando de cara o clássico ataque de confusão de algoritmo. Um token que não confere é um hive.crypto.JwtError, assim como claims com formato errado, com o found começando pelo motivo — então um token forjado e claims com formato errado são uma falha só, e não duas. jwtDecode lê as claims sem verificar nada, apenas para inspeção, e jwtHeader lê apenas alg/typ/kid, que é o que um programa que rotaciona chaves precisa antes de poder nomear o segredo.", Examples: []slides_0_Example{slides_0_Example{Caption: "Hashing", CaptionPt: "Calculando hash", Code: "proc main(): void {\n\techo hive.crypto.sha256(\"hive\")\n\techo hive.crypto.sha512(\"hive\")\n\techo hive.crypto.hmacSha256(\"hive\", \"secret\")\n}", CodePt: "proc main(): void {\n\techo hive.crypto.sha256(\"hive\")\n\techo hive.crypto.sha512(\"hive\")\n\techo hive.crypto.hmacSha256(\"hive\", \"secret\")\n}"}, slides_0_Example{Caption: "Encryption", CaptionPt: "Cifrando e decifrando", Code: "proc main(): void {\n\tsealed := hive.crypto.encrypt(\"a secret\", \"password\")\n\tif hive.crypto.decrypt(sealed, \"password\") is Result.Ok(plain) {\n\t\techo plain\n\t}\n}", CodePt: "proc main(): void {\n\tsealed := hive.crypto.encrypt(\"um segredo\", \"password\")\n\tif hive.crypto.decrypt(sealed, \"password\") is Result.Ok(plain) {\n\t\techo plain\n\t}\n}"}, slides_0_Example{Caption: "Encoding and random bytes", CaptionPt: "Codificação e bytes aleatórios", Code: "proc main(): void {\n\tencoded := hive.crypto.base64Encode(\"hive\")\n\tif hive.crypto.base64Decode(encoded) is Result.Ok(decoded) {\n\t\techo decoded\n\t}\n\techo hive.crypto.randomHex(8)\n}", CodePt: "proc main(): void {\n\tencoded := hive.crypto.base64Encode(\"hive\")\n\tif hive.crypto.base64Decode(encoded) is Result.Ok(decoded) {\n\t\techo decoded\n\t}\n\techo hive.crypto.randomHex(8)\n}"}, slides_0_Example{Caption: "JWTs", CaptionPt: "JWTs", Code: "type Claims { user: Str }\n\nproc main(): void {\n\ttoken := encode(Claims(\"ada\"), hive.crypto.jwtCodec(\"secret\"))\n\n\t// Checks the signature and the times, then reads the claims.\n\tif Claims.decode(token, hive.crypto.jwtCodec(\"secret\")) is Result.Ok(claims) {\n\t\techo claims.user\n\t}\n\techo hive.crypto.jwtHeader(token)\n}", CodePt: "type Claims { user: Str }\n\nproc main(): void {\n\ttoken := encode(Claims(\"ada\"), hive.crypto.jwtCodec(\"secret\"))\n\n\t// Confere a assinatura e os prazos, e então lê as claims.\n\tif Claims.decode(token, hive.crypto.jwtCodec(\"secret\")) is Result.Ok(claims) {\n\t\techo claims.user\n\t}\n\techo hive.crypto.jwtHeader(token)\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "SQL", TitlePt: "SQL", Explanation: "A query declaration is a func whose body is inline SQL and whose return type describes its rows. Columns are matched to fields by name, not position, so reordering a SELECT can never silently remap them, and values are always bound as parameters — never spliced into the text — so nothing a caller supplies can change what the statement means. connect opens a single connection and pool a whole pool of them, both answering with a Result<SqlConnection, SqlError>; close lets one go. SQL assembled at run time, rather than declared as a query, goes through hive.sql.raw instead — untyped by construction, and easy to grep for.\n\nThe return type does more than name the row: it decides the shape of the whole result, so you can read that off the declaration without looking at the SQL at all. A declared row type comes back as Result<Row[dyn], SqlError>. A single scalar vector — Str[dyn], Int[dyn] — needs no row type at all and comes back as that column. And void marks a statement rather than a query, coming back as the number of rows it touched.\n\nBecause columns are matched by name, name them — and the compiler holds you to it. A column whose name differs from its field needs an alias (SELECT u.name AS author), a row type holds scalars only, and SELECT * against a declared row type is a compile error. It says neither how many columns come back nor what they are called, so nothing can line them up against the fields; and what it stands for changes the day somebody adds a column to the table, which would turn a query that compiled and worked into rows that no longer fit, in a program nobody edited. The rule is about the result and only the result, so count(*), a * 2, a star inside a subquery and the select list of a void statement all still compile — and so does SELECT * into a Table, which is rows of cells and has no field names to disagree with.", ExplanationPt: "Uma declaração query é um func cujo corpo é SQL inline e cujo tipo de retorno descreve suas linhas. Colunas são associadas a campos pelo nome, não pela posição, então reordenar um SELECT nunca pode remapeá-las silenciosamente, e valores são sempre vinculados como parâmetros — nunca inseridos diretamente no texto — então nada que quem chama fornecer pode mudar o que a instrução significa. connect abre uma única conexão e pool abre um pool inteiro delas, ambos respondendo com um Result<SqlConnection, SqlError>; close libera uma. SQL montado em tempo de execução, em vez de declarado como uma query, passa por hive.sql.raw em vez disso — sem tipo por construção, e fácil de encontrar com grep.\n\nO tipo de retorno faz mais do que nomear a linha: ele decide o formato do resultado inteiro, então dá para ler isso na declaração sem olhar o SQL. Um tipo de linha declarado volta como Result<Row[dyn], SqlError>. Um vetor de um escalar só — Str[dyn], Int[dyn] — não precisa de tipo de linha algum e volta como aquela coluna. E void marca uma instrução em vez de uma consulta, voltando como o número de linhas que ela tocou.\n\nComo as colunas são associadas pelo nome, nomeie-as — e o compilador exige isso. Uma coluna cujo nome difere do campo precisa de um alias (SELECT u.name AS author), um tipo de linha guarda apenas escalares, e SELECT * contra um tipo de linha declarado é um erro de compilação. Ele não diz nem quantas colunas voltam nem como se chamam, então nada consegue alinhá-las aos campos; e o que ele representa muda no dia em que alguém adiciona uma coluna à tabela, o que transformaria uma consulta que compilava e funcionava em linhas que não cabem mais, num programa que ninguém editou. A regra é sobre o resultado e somente sobre o resultado, então count(*), a * 2, uma estrela dentro de uma subconsulta e a lista de seleção de uma instrução void continuam compilando — e SELECT * para uma Table também, que é linhas de células e não tem nomes de campo com que discordar.", Examples: []slides_0_Example{slides_0_Example{Caption: "A row type", CaptionPt: "Um tipo de linha", Code: "type User { id: Int, name: Str }\n\nquery findUser(name: Str): User[dyn] {\n\tSELECT id, name FROM users WHERE name = {name}\n}\n\nproc main(): void {\n\tpooled := hive.sql.pool(hive.sql.DatabaseDriver.SQLite(), \"file::memory:?cache=shared\", 1, 1)\n\tif pooled is Result.Ok(db) {\n\t\thive.sql.raw(db, \"CREATE TABLE users (id INTEGER, name TEXT)\")\n\t\thive.sql.raw(db, \"INSERT INTO users (id, name) VALUES (1, 'ada')\")\n\n\t\tfound := hive.sql.run(db, findUser(\"ada\"))\n\t\tif found is Result.Ok(users) {\n\t\t\techo users\n\t\t}\n\t\thive.sql.close(db)\n\t} else if pooled is Result.Error(err) {\n\t\techo \"could not open the database: {err.message}\"\n\t}\n}", CodePt: "type User { id: Int, name: Str }\n\nquery findUser(name: Str): User[dyn] {\n\tSELECT id, name FROM users WHERE name = {name}\n}\n\nproc main(): void {\n\tpooled := hive.sql.pool(hive.sql.DatabaseDriver.SQLite(), \"file::memory:?cache=shared\", 1, 1)\n\tif pooled is Result.Ok(db) {\n\t\thive.sql.raw(db, \"CREATE TABLE users (id INTEGER, name TEXT)\")\n\t\thive.sql.raw(db, \"INSERT INTO users (id, name) VALUES (1, 'ada')\")\n\n\t\tfound := hive.sql.run(db, findUser(\"ada\"))\n\t\tif found is Result.Ok(users) {\n\t\t\techo users\n\t\t}\n\t\thive.sql.close(db)\n\t} else if pooled is Result.Error(err) {\n\t\techo \"não foi possível abrir o banco de dados: {err.message}\"\n\t}\n}"}, slides_0_Example{Caption: "One column, and no rows at all", CaptionPt: "Uma coluna, e nenhuma linha", Code: "query insertUser(id: Int, name: Str): void {\n\tINSERT INTO users (id, name) VALUES ({id}, {name})\n}\n\nquery userNames(): Str[dyn] {\n\tSELECT name FROM users ORDER BY name\n}\n\nquery deleteUser(id: Int): void {\n\tDELETE FROM users WHERE id = {id}\n}\n\nproc main(): void {\n\topened := hive.sql.pool(hive.sql.DatabaseDriver.SQLite(), \"file::memory:?cache=shared\", 1, 1)\n\tif opened is Result.Ok(db) {\n\t\thive.sql.raw(db, \"CREATE TABLE users (id INTEGER, name TEXT)\")\n\t\thive.sql.run(db, insertUser(1, \"ada\"))\n\t\thive.sql.run(db, insertUser(2, \"grace\"))\n\n\t\tif hive.sql.run(db, userNames()) is Result.Ok(names) {\n\t\t\techo names                      // a Str[dyn]: the column itself\n\t\t}\n\t\tif hive.sql.run(db, deleteUser(1)) is Result.Ok(rows) {\n\t\t\techo \"deleted {rows} row\"       // an Int: what the statement touched\n\t\t}\n\t\thive.sql.close(db)\n\t}\n}", CodePt: "query insertUser(id: Int, name: Str): void {\n\tINSERT INTO users (id, name) VALUES ({id}, {name})\n}\n\nquery userNames(): Str[dyn] {\n\tSELECT name FROM users ORDER BY name\n}\n\nquery deleteUser(id: Int): void {\n\tDELETE FROM users WHERE id = {id}\n}\n\nproc main(): void {\n\topened := hive.sql.pool(hive.sql.DatabaseDriver.SQLite(), \"file::memory:?cache=shared\", 1, 1)\n\tif opened is Result.Ok(db) {\n\t\thive.sql.raw(db, \"CREATE TABLE users (id INTEGER, name TEXT)\")\n\t\thive.sql.run(db, insertUser(1, \"ada\"))\n\t\thive.sql.run(db, insertUser(2, \"grace\"))\n\n\t\tif hive.sql.run(db, userNames()) is Result.Ok(names) {\n\t\t\techo names                      // um Str[dyn]: a coluna em si\n\t\t}\n\t\tif hive.sql.run(db, deleteUser(1)) is Result.Ok(rows) {\n\t\t\techo \"apagou {rows} linha\"      // um Int: o que a instrução tocou\n\t\t}\n\t\thive.sql.close(db)\n\t}\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "Optional filters: WHERE { }", TitlePt: "Filtros opcionais: WHERE { }", Explanation: "A search screen with four boxes on it is four optional predicates, and assembling that as text is where SQL injection and \"WHERE 1 = 1\" both come from. A WHERE block is the declaration that replaces it: it ANDs together the predicates whose conditions hold, and a nested or { } or and { } flips the connective for the group inside it.\n\nA group that contributes nothing disappears rather than leaving a dangling connective, a group contributing more than one predicate is parenthesised, and when no predicate holds at all there is no WHERE clause in the statement — so there is no 1 = 1 to write and none to read back in a log. Every branch's text is fixed at compile time; the only thing decided while the program runs is which branches are taken. The values still travel as bound parameters, exactly as they do in a plain query.\n\nTwo things a where block deliberately cannot do. A column name or a sort direction can never be a parameter — ORDER BY {col} would order every row by one constant string — so make that choice a variant type and dispatch to one query per ordering. And SQL you genuinely do assemble yourself goes through hive.sql.raw instead, which is untyped by construction and greppable by design.", ExplanationPt: "Uma tela de busca com quatro campos é quatro predicados opcionais, e montar isso como texto é de onde vêm tanto a injeção de SQL quanto o \"WHERE 1 = 1\". Um bloco WHERE é a declaração que substitui isso: ele junta com AND os predicados cujas condições valem, e um or { } ou and { } aninhado inverte o conectivo do grupo dentro dele.\n\nUm grupo que não contribui com nada desaparece em vez de deixar um conectivo solto, um grupo que contribui com mais de um predicado é parentetizado, e quando nenhum predicado vale, não existe cláusula WHERE alguma na instrução — então não há 1 = 1 para escrever nem para ler depois em um log. O texto de cada ramo é fixado em tempo de compilação; a única coisa decidida enquanto o programa roda é quais ramos são tomados. Os valores continuam viajando como parâmetros vinculados, exatamente como em uma query comum.\n\nDuas coisas que um bloco where deliberadamente não faz. Um nome de coluna ou uma direção de ordenação nunca pode ser um parâmetro — ORDER BY {col} ordenaria toda linha por uma string constante — então faça dessa escolha um tipo com variantes e despache para uma query por ordenação. E o SQL que você de fato monta na mão passa por hive.sql.raw, que é sem tipo por construção e fácil de encontrar com grep.", Examples: []slides_0_Example{slides_0_Example{Caption: "", CaptionPt: "", Code: "type Colony {\n\tname:   Str\n\tapiary: Str\n\tframes: Int\n}\n\nquery findColonies(apiary: Str, minFrames: Int, small: Bool, huge: Bool): Colony[dyn] {\n\tSELECT name, apiary, frames FROM colonies\n\tWHERE {\n\t\tif apiary != \"\"  { apiary = {apiary} }\n\t\tif minFrames > 0 { frames >= {minFrames} }\n\t\tor {\n\t\t\tif small { frames < 4 }\n\t\t\tif huge  { frames > 10 }\n\t\t}\n\t}\n\tORDER BY name\n}\n\nproc main(): void {\n\topened := hive.sql.pool(hive.sql.DatabaseDriver.SQLite(), \"file::memory:?cache=shared\", 1, 1)\n\tif opened is Result.Ok(db) {\n\t\thive.sql.raw(db, \"CREATE TABLE colonies (name TEXT, apiary TEXT, frames INTEGER)\")\n\t\thive.sql.raw(db, \"INSERT INTO colonies VALUES ('north', 'orchard', 12)\")\n\t\thive.sql.raw(db, \"INSERT INTO colonies VALUES ('south', 'orchard', 2)\")\n\t\thive.sql.raw(db, \"INSERT INTO colonies VALUES ('east', 'meadow', 7)\")\n\n\t\t// Everything off: no predicate holds, so there is no WHERE at all.\n\t\tif hive.sql.run(db, findColonies(\"\", 0, false, false)) is Result.Ok(all) {\n\t\t\techo len(all)\n\t\t}\n\t\t// apiary AND frames >= 5 AND (frames > 10).\n\t\tif hive.sql.run(db, findColonies(\"orchard\", 5, false, true)) is Result.Ok(some) {\n\t\t\techo some\n\t\t}\n\t\thive.sql.close(db)\n\t}\n}", CodePt: "type Colony {\n\tname:   Str\n\tapiary: Str\n\tframes: Int\n}\n\nquery findColonies(apiary: Str, minFrames: Int, small: Bool, huge: Bool): Colony[dyn] {\n\tSELECT name, apiary, frames FROM colonies\n\tWHERE {\n\t\tif apiary != \"\"  { apiary = {apiary} }\n\t\tif minFrames > 0 { frames >= {minFrames} }\n\t\tor {\n\t\t\tif small { frames < 4 }\n\t\t\tif huge  { frames > 10 }\n\t\t}\n\t}\n\tORDER BY name\n}\n\nproc main(): void {\n\topened := hive.sql.pool(hive.sql.DatabaseDriver.SQLite(), \"file::memory:?cache=shared\", 1, 1)\n\tif opened is Result.Ok(db) {\n\t\thive.sql.raw(db, \"CREATE TABLE colonies (name TEXT, apiary TEXT, frames INTEGER)\")\n\t\thive.sql.raw(db, \"INSERT INTO colonies VALUES ('north', 'orchard', 12)\")\n\t\thive.sql.raw(db, \"INSERT INTO colonies VALUES ('south', 'orchard', 2)\")\n\t\thive.sql.raw(db, \"INSERT INTO colonies VALUES ('east', 'meadow', 7)\")\n\n\t\t// Tudo desligado: nenhum predicado vale, então não há WHERE algum.\n\t\tif hive.sql.run(db, findColonies(\"\", 0, false, false)) is Result.Ok(all) {\n\t\t\techo len(all)\n\t\t}\n\t\t// apiary AND frames >= 5 AND (frames > 10).\n\t\tif hive.sql.run(db, findColonies(\"orchard\", 5, false, true)) is Result.Ok(some) {\n\t\t\techo some\n\t\t}\n\t\thive.sql.close(db)\n\t}\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "The in-memory trap", TitlePt: "A pegadinha do banco em memória", Explanation: "A plain :memory: SQLite database belongs to the connection, not to the process. Each connection gets a private, empty database that vanishes when it closes — and combined with a pool, that has a sharp edge. One query at a time works fine. Eight at once does not: the pool opens further connections, each one lands on a database of its own, and the queries that went to the new ones come back \"no such table\". A program that passes every test single-threaded starts failing the moment requests overlap, which is exactly what happens behind an HTTP server, since it runs each request on a thread of its own.\n\nThere are two ways out, and only one of them is a real answer. pool(…, 1, 1) holds the pool to a single connection, which works but serialises every query in the program through it. \"file::memory:?cache=shared\" is the one to use: the connections share a single in-memory database, so the pool can be as wide as the work is. None of this is Hive's rule — it is SQLite's — but it is the thing about an in-memory database worth knowing before production teaches it to you.", ExplanationPt: "Um banco SQLite :memory: simples pertence à conexão, não ao processo. Cada conexão recebe um banco privado e vazio que desaparece quando ela fecha — e combinado com um pool, isso tem uma aresta afiada. Uma query por vez funciona bem. Oito de uma vez não: o pool abre mais conexões, cada uma cai em um banco próprio, e as queries que foram para as novas voltam com \"no such table\". Um programa que passa em todo teste em thread única começa a falhar no instante em que as requisições se sobrepõem, que é exatamente o que acontece atrás de um servidor HTTP, já que ele roda cada requisição em uma thread própria.\n\nExistem duas saídas, e só uma delas é uma resposta de verdade. pool(…, 1, 1) segura o pool em uma única conexão, o que funciona mas serializa toda query do programa por ela. \"file::memory:?cache=shared\" é a que se deve usar: as conexões compartilham um único banco em memória, então o pool pode ser tão largo quanto o trabalho for. Nada disso é regra do Hive — é do SQLite — mas é o que vale saber sobre um banco em memória antes que a produção lhe ensine.", Examples: []slides_0_Example{slides_0_Example{Caption: "A row type", CaptionPt: "Um tipo de linha", Code: "type User { id: Int, name: Str }\n\nquery findUser(name: Str): User[dyn] {\n\tSELECT id, name FROM users WHERE name = {name}\n}\n\nproc lookUp(db: hive.sql.SqlConnection, name: Str): Str {\n\tif hive.sql.run(db, findUser(name)) is Result.Ok(rows) {\n\t\tif rows bounds 0 {\n\t\t\treturn rows[0].name\n\t\t}\n\t}\n\treturn \"?\"\n}\n\nproc main(): void {\n\t// A plain \":memory:\" would give each of these eight connections a\n\t// private, empty database of its own. The shared cache is what makes\n\t// them all the same one.\n\topened := hive.sql.pool(hive.sql.DatabaseDriver.SQLite(), \"file::memory:?cache=shared\", 8, 1)\n\tif opened is Result.Ok(db) {\n\t\thive.sql.raw(db, \"CREATE TABLE users (id INTEGER, name TEXT)\")\n\t\thive.sql.raw(db, \"INSERT INTO users (id, name) VALUES (1, 'ada'), (2, 'grace')\")\n\n\t\techo await [lookUp(db, \"ada\"), lookUp(db, \"grace\")]\n\t\thive.sql.close(db)\n\t}\n}", CodePt: "type User { id: Int, name: Str }\n\nquery findUser(name: Str): User[dyn] {\n\tSELECT id, name FROM users WHERE name = {name}\n}\n\nproc lookUp(db: hive.sql.SqlConnection, name: Str): Str {\n\tif hive.sql.run(db, findUser(name)) is Result.Ok(rows) {\n\t\tif rows bounds 0 {\n\t\t\treturn rows[0].name\n\t\t}\n\t}\n\treturn \"?\"\n}\n\nproc main(): void {\n\t// Um \":memory:\" simples daria a cada uma destas oito conexões um banco\n\t// privado e vazio, só dela. O cache compartilhado é o que faz todas\n\t// elas serem o mesmo banco.\n\topened := hive.sql.pool(hive.sql.DatabaseDriver.SQLite(), \"file::memory:?cache=shared\", 8, 1)\n\tif opened is Result.Ok(db) {\n\t\thive.sql.raw(db, \"CREATE TABLE users (id INTEGER, name TEXT)\")\n\t\thive.sql.raw(db, \"INSERT INTO users (id, name) VALUES (1, 'ada'), (2, 'grace')\")\n\n\t\techo await [lookUp(db, \"ada\"), lookUp(db, \"grace\")]\n\t\thive.sql.close(db)\n\t}\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "The UI module", TitlePt: "O módulo de UI", Explanation: "hive.ui builds an interface as a value: a View is a tree of widgets, and every one of them takes the same two things, its attributes and then its own payload. row and column lay out children; text, image, icon and link show something; button, input, textarea, checkbox and select ask for something; table shows a Table and canvas draws on a plane, and spacer, spinner, none, overlay and inset round out the set. Attributes are just as closed a set: gap, pad, width, height, grow, align, justify and scroll lay out; size, heading, tone and background style; disabled, busy, placeholder, hint and kind describe state; and on, onDismiss, onInput, onSubmit, onChoose, onToggle, onPick and onSort carry an event, for a window rather than this kind of page. Align, Justify, TextSize, Tone, Axis, InputKind and Icon are the enumerations behind them, reached like any other library type, and each is a closed set: a Tone is Normal, Muted, Good, Warn or Danger, or an exact colour through HEX and RGBA; an InputKind is Text, Number, Date, Password or Search; and an Icon is one of eighteen — Search, Close, Check, Plus, Minus, Up, Down, Left, Right, Warning, Info, Star, Heart, Reply, Repeat, Trash, User and Menu — so a name that is not one of them is a compile error rather than a missing glyph.\n\nhtml(view) renders a tree as an HTML fragment, and page(title, view) as a whole document with a stylesheet already built in, light and dark alike — which is exactly what an httpServe handler can answer a request with directly, and what every screen in this tour but its code samples is. The same tree, shown instead by window(title, view, update, state), becomes a live page: update is a fold over its events, the same shape a hive.syslink handler folds over a mailbox, so a window has an address and needs no mutex of its own.\n\nA window is a real window, and not a tab. What opens is an application-mode browser window — no address bar, no tabs — running against a profile of its own, so it is its own process rather than something inside somebody's browser: none of their session, extensions or history is in it, and quitting a browser does not close it. Closing the window ends the program. A machine with no Chromium-family browser falls back to an ordinary tab, which is the one case where the window belongs to a browser.\n\nIts icon is assets/icon.png, beside the entrypoint: a PNG of at most 256×256, embedded into the executable by the build. Nothing in the source names it — what a window shows and what the file carries have to be one image, so there is one place to put it — and a program that ships none shows Hive's own mark rather than a browser's blank sheet. On Windows a built windowed program carries no console and carries that icon as a resource, so what opens is the window, and the file itself shows the icon in a file manager and on a pinned shortcut. A typeface arrives the same way: assets/font.woff2, beside the entrypoint, and the window is set in it. Nothing declares that one either — the rule is always written into the page, so a program that ships the file is set in its own face and one that does not falls through to the system's, with nothing to configure in either case.\n\nThat address is also why there is no command type. Every language built this way needs an answer to \"the update wants to fetch something, and must not block the screen while it does\", and most of them invent a type for it — a Cmd, a task, an effect the runtime interprets. Hive needs none, because it already has two things that add up to one: update is a proc, so it may act, and hive.syslink.self(envelope) is the window's own address, so work started with async can send its answer back as an ordinary message.\n\nAnd because a view is a value rather than a template, an interface is testable without a browser. html is the same renderer a served page uses, so a test can render a view and read the markup that comes out — which is the cheapest place there is to check that a disabled button really is disabled, and that somebody else's text cannot smuggle a script tag into your page.\n\nTwo widgets are missing from all of that, and each has a slide to itself: canvas, which draws on a plane and which a served page draws exactly as a window does, and scene, which draws in three dimensions and which only a window can draw.", ExplanationPt: "hive.ui constrói uma interface como um valor: uma View é uma árvore de widgets, e cada um deles recebe as mesmas duas coisas, seus atributos e depois sua própria carga. row e column organizam filhos; text, image, icon e link mostram algo; button, input, textarea, checkbox e select pedem algo; table mostra uma Table e canvas desenha sobre um plano, e spacer, spinner, none, overlay e inset completam o conjunto. Os atributos são um conjunto igualmente fechado: gap, pad, width, height, grow, align, justify e scroll organizam; size, heading, tone e background estilizam; disabled, busy, placeholder, hint e kind descrevem estado; e on, onDismiss, onInput, onSubmit, onChoose, onToggle, onPick e onSort carregam um evento, para uma window e não para este tipo de página. Align, Justify, TextSize, Tone, Axis, InputKind e Icon são as enumerações por trás deles, acessadas como qualquer outro tipo da biblioteca, e cada uma é um conjunto fechado: um Tone é Normal, Muted, Good, Warn ou Danger, ou uma cor exata via HEX e RGBA; um InputKind é Text, Number, Date, Password ou Search; e um Icon é um entre dezoito — Search, Close, Check, Plus, Minus, Up, Down, Left, Right, Warning, Info, Star, Heart, Reply, Repeat, Trash, User e Menu — então um nome que não esteja nessa lista é erro de compilação, e não um ícone faltando.\n\nhtml(view) renderiza uma árvore como um fragmento HTML, e page(title, view) como um documento inteiro com uma stylesheet já embutida, claro e escuro igualmente — que é exatamente o que um handler de httpServe pode usar para responder a uma requisição diretamente, e o que toda tela deste tour, exceto suas amostras de código, é. A mesma árvore, mostrada em vez disso por window(title, view, update, state), se torna uma página viva: update é um fold sobre seus eventos, a mesma forma que um handler de hive.syslink faz fold sobre uma caixa de mensagens, então uma window tem um endereço e não precisa de mutex próprio.\n\nUma window é uma janela de verdade, e não uma aba. O que abre é uma janela de navegador em modo aplicativo — sem barra de endereço e sem abas — rodando sobre um perfil só dela, então é um processo próprio e não algo dentro do navegador de alguém: nada da sessão, das extensões ou do histórico da pessoa está ali, e fechar o navegador não fecha a janela. Fechar a janela encerra o programa. Uma máquina sem nenhum navegador da família Chromium cai para uma aba comum, que é o único caso em que a janela pertence a um navegador.\n\nO ícone dela é o assets/icon.png, ao lado do arquivo de entrada: um PNG de no máximo 256×256, embutido no executável pelo build. Nada no código-fonte o nomeia — o que a janela mostra e o que o arquivo carrega têm de ser a mesma imagem, então há um lugar só para ela — e um programa que não traz nenhuma mostra a marca do próprio Hive, em vez da folha em branco de um navegador. No Windows, um programa de janela compilado não carrega console e leva esse ícone como recurso, então o que abre é a janela, e o próprio arquivo mostra o ícone no gerenciador de arquivos e num atalho fixado. Uma fonte chega do mesmo jeito: assets/font.woff2, ao lado do arquivo de entrada, e a janela é composta nela. Nada declara essa também — a regra é sempre escrita na página, então um programa que traz o arquivo aparece na fonte dele e um que não traz cai para a do sistema, sem nada para configurar em nenhum dos dois casos.\n\nEsse endereço é também por que não existe um tipo de comando. Toda linguagem construída assim precisa de uma resposta para \"o update quer buscar algo, e não pode travar a tela enquanto busca\", e a maioria delas inventa um tipo para isso — um Cmd, uma task, um efeito que o runtime interpreta. Hive não precisa de nenhum, porque já tem duas coisas que somadas dão uma: update é um proc, então pode agir, e hive.syslink.self(envelope) é o endereço da própria window, então o trabalho iniciado com async pode mandar sua resposta de volta como uma mensagem comum.\n\nE como uma view é um valor e não um template, uma interface pode ser testada sem navegador. html é o mesmo renderizador que uma página servida usa, então um teste pode renderizar uma view e ler o markup que sai — que é o lugar mais barato que existe para conferir que um botão desabilitado está mesmo desabilitado, e que o texto de outra pessoa não consegue contrabandear uma tag de script para dentro da sua página.\n\nDois widgets ficaram de fora de tudo isso, e cada um tem um slide só para si: canvas, que desenha sobre um plano e que uma página servida desenha exatamente como uma window desenha, e scene, que desenha em três dimensões e que somente uma window consegue desenhar.", Examples: []slides_0_Example{slides_0_Example{Caption: "A page, served directly", CaptionPt: "Uma página, servida diretamente", Code: "proc main(): void {\n\tview := hive.ui.column([hive.ui.pad(24), hive.ui.gap(12)], [\n\t\thive.ui.text([hive.ui.size(hive.ui.TextSize.Title()), hive.ui.heading(1)], \"Hive Tour\"),\n\t\thive.ui.row([hive.ui.gap(8)], [\n\t\t\thive.ui.input([hive.ui.placeholder(\"your name\")], \"\"),\n\t\t\thive.ui.button([hive.ui.tone(hive.ui.Tone.Good())], \"Say hi\"),\n\t\t]),\n\t\thive.ui.link([hive.ui.background(hive.ui.Tone.Good())], \"/1\", \"Start →\"),\n\t])\n\n\tbody := hive.ui.page(\"Hive\", view)   // a whole document, served directly\n\techo len(body)\n}", CodePt: "proc main(): void {\n\tview := hive.ui.column([hive.ui.pad(24), hive.ui.gap(12)], [\n\t\thive.ui.text([hive.ui.size(hive.ui.TextSize.Title()), hive.ui.heading(1)], \"Tour do Hive\"),\n\t\thive.ui.row([hive.ui.gap(8)], [\n\t\t\thive.ui.input([hive.ui.placeholder(\"seu nome\")], \"\"),\n\t\t\thive.ui.button([hive.ui.tone(hive.ui.Tone.Good())], \"Dizer oi\"),\n\t\t]),\n\t\thive.ui.link([hive.ui.background(hive.ui.Tone.Good())], \"/1\", \"Começar →\"),\n\t])\n\n\tbody := hive.ui.page(\"Hive\", view)   // um documento inteiro, servido diretamente\n\techo len(body)\n}"}, slides_0_Example{Caption: "A window, with no command type", CaptionPt: "Uma window, sem tipo de comando", Code: "import hive.ui\n\ntype Model {\n\trows:    Str[dyn]\n\tloading: Bool\n}\n\ntype Msg {\n\tRefresh\n\tLoaded { rows: Str[dyn] }\n}\n\nfunc view(model: Model): ui.View {\n\tmut ui.View[dyn] kids = [ui.button([ui.on(Msg.Refresh())], \"Refresh\")]\n\tif model.loading {\n\t\tappend(kids, ui.spinner([]))\n\t}\n\tfor each row in model.rows {\n\t\tappend(kids, ui.text([], row))\n\t}\n\treturn ui.column([ui.pad(24), ui.gap(8)], kids)\n}\n\nproc update(model: Model, msg: Msg, from: hive.syslink.Envelope): Model {\n\tif msg is Msg.Refresh {\n\t\tasync reload(hive.syslink.self(from))   // off it goes; nothing to hold\n\t\treturn Model(model.rows, true)           // ...and the spinner starts now\n\t} else if msg is Msg.Loaded(rows) {\n\t\treturn Model(rows, false)                // it comes back as a message\n\t}\n\treturn model\n}\n\nproc reload(window: hive.syslink.Address): void {\n\thive.task.sleep(400)\n\tasync window(Msg.Loaded([\"north\", \"south\"]))\n}\n\nproc main(): void {\n\tui.window(\"Colonies\", view, update, Model([], false))\n}", CodePt: "import hive.ui\n\ntype Model {\n\trows:    Str[dyn]\n\tloading: Bool\n}\n\ntype Msg {\n\tRefresh\n\tLoaded { rows: Str[dyn] }\n}\n\nfunc view(model: Model): ui.View {\n\tmut ui.View[dyn] kids = [ui.button([ui.on(Msg.Refresh())], \"Atualizar\")]\n\tif model.loading {\n\t\tappend(kids, ui.spinner([]))\n\t}\n\tfor each row in model.rows {\n\t\tappend(kids, ui.text([], row))\n\t}\n\treturn ui.column([ui.pad(24), ui.gap(8)], kids)\n}\n\nproc update(model: Model, msg: Msg, from: hive.syslink.Envelope): Model {\n\tif msg is Msg.Refresh {\n\t\tasync reload(hive.syslink.self(from))   // lá vai; nada a segurar\n\t\treturn Model(model.rows, true)           // ...e o spinner começa agora\n\t} else if msg is Msg.Loaded(rows) {\n\t\treturn Model(rows, false)                // volta como uma mensagem\n\t}\n\treturn model\n}\n\nproc reload(window: hive.syslink.Address): void {\n\thive.task.sleep(400)\n\tasync window(Msg.Loaded([\"north\", \"south\"]))\n}\n\nproc main(): void {\n\tui.window(\"Colônias\", view, update, Model([], false))\n}"}, slides_0_Example{Caption: "An interface you can test", CaptionPt: "Uma interface que dá para testar", Code: "import hive.ui\n\ntype Model {\n\tposts: Str[dyn]\n\tdraft: Str\n}\n\nfunc view(model: Model): ui.View {\n\tmut ui.View[dyn] kids = [\n\t\tui.input([ui.placeholder(\"say something\")], model.draft),\n\t\tui.button([ui.disabled(model.draft == \"\")], \"Post\"),\n\t]\n\tfor each post in model.posts {\n\t\tappend(kids, ui.text([], post))\n\t}\n\treturn ui.column([ui.pad(24), ui.gap(8)], kids)\n}\n\ntest \"the post button is disabled until something is typed\" {\n\tempty := ui.html(view(Model([], \"\")))\n\tassert indexOf(empty, \"disabled\") is Result.Ok(_)\n\n\ttyped := ui.html(view(Model([], \"hi\")))\n\tassert indexOf(typed, \"disabled\") is Result.Error(_)\n}\n\ntest \"a post body cannot smuggle markup into the page\" {\n\tpage := ui.html(view(Model([\"<script>steal()</script>\"], \"\")))\n\tassert indexOf(page, \"<script>\") is Result.Error(_)\n}", CodePt: "import hive.ui\n\ntype Model {\n\tposts: Str[dyn]\n\tdraft: Str\n}\n\nfunc view(model: Model): ui.View {\n\tmut ui.View[dyn] kids = [\n\t\tui.input([ui.placeholder(\"diga algo\")], model.draft),\n\t\tui.button([ui.disabled(model.draft == \"\")], \"Publicar\"),\n\t]\n\tfor each post in model.posts {\n\t\tappend(kids, ui.text([], post))\n\t}\n\treturn ui.column([ui.pad(24), ui.gap(8)], kids)\n}\n\ntest \"o botão fica desabilitado até algo ser digitado\" {\n\tempty := ui.html(view(Model([], \"\")))\n\tassert indexOf(empty, \"disabled\") is Result.Ok(_)\n\n\ttyped := ui.html(view(Model([], \"oi\")))\n\tassert indexOf(typed, \"disabled\") is Result.Error(_)\n}\n\ntest \"o corpo de um post não contrabandeia markup para a página\" {\n\tpage := ui.html(view(Model([\"<script>steal()</script>\"], \"\")))\n\tassert indexOf(page, \"<script>\") is Result.Error(_)\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "Drawing on a canvas", TitlePt: "Desenhando em um canvas", Explanation: "canvas(attrs, marks) is a widget like any other and a Mark is a value like any other, so what a canvas holds is what is on it right now. There is no drawing context, no frame to schedule and nothing to clear: a chart is rebuilt from the state exactly the way the rest of the page is, and the same tree a window swaps in is what page writes out — so a chart drawn for a program is a chart served by a web server with nothing changed.\n\nThere are five marks, and each one is here for the reason a shape is: it cannot be made out of the others. dot is a filled circle, bar a rectangle, path a line through a run of points, patch those same numbers filled, and words text placed on the plane. A ring is a dot with no fill, a grid is a vector of paths, an arrow is a path — and none of those needed a name of its own.\n\nEverything on a canvas is said in the program's own units. region(x, y, width, height) says which part of the plane the box shows, and a map of a circuit written in metres draws a car nine metres long nine long. paint(Tone) is a mark's colour and thick(Float) is how wide a path is drawn, in the region's units too — so a map zoomed in has thicker roads, which is what zooming in means. The box may be any shape; what is drawn keeps its own and is centred in it.\n\nTwo placements are worth stating outright, because both are the sort of thing a program discovers by being half a width out. A bar is placed by its middle, like everything else on a canvas — one mark placed by its corner would be the one that is always wrong. And words are centred on the place they are given, with their height in the region's units rather than in points, because a label on a plan is as big as the thing it labels.", ExplanationPt: "canvas(attrs, marks) é um widget como qualquer outro e um Mark é um valor como qualquer outro, então o que um canvas guarda é o que está nele agora. Não existe contexto de desenho, não existe frame para agendar e não há nada para limpar: um gráfico é reconstruído a partir do estado exatamente como o resto da página é, e a mesma árvore que uma window troca é a que page escreve — então um gráfico desenhado para um programa é um gráfico servido por um servidor web sem mudar nada.\n\nExistem cinco marks, e cada um está aqui pelo mesmo motivo que uma forma está: não dá para fazê-lo a partir dos outros. dot é um círculo preenchido, bar é um retângulo, path é uma linha através de uma sequência de pontos, patch são esses mesmos números preenchidos, e words é texto posicionado no plano. Um anel é um dot sem preenchimento, uma grade é um vetor de paths, uma seta é um path — e nenhum deles precisou de um nome próprio.\n\nTudo em um canvas é dito nas unidades do próprio programa. region(x, y, largura, altura) diz qual parte do plano a caixa mostra, e um mapa de um circuito escrito em metros desenha um carro de nove metros com nove de comprimento. paint(Tone) é a cor de um mark e thick(Float) é a espessura com que um path é desenhado, também nas unidades da região — então um mapa com zoom tem estradas mais grossas, que é o que dar zoom significa. A caixa pode ter qualquer formato; o que é desenhado mantém o seu e fica centralizado nela.\n\nDuas colocações valem ser ditas explicitamente, porque as duas são o tipo de coisa que um programa descobre estando meia largura fora do lugar. Um bar é posicionado pelo seu meio, como tudo mais em um canvas — um mark posicionado pelo canto seria o único sempre errado. E words fica centralizado no lugar que recebe, com a sua altura nas unidades da região e não em pontos, porque um rótulo em uma planta é do tamanho da coisa que ele rotula.", Examples: []slides_0_Example{slides_0_Example{Caption: "A chart, in the program's own units", CaptionPt: "Um gráfico, nas unidades do próprio programa", Code: "import hive.ui\n\n// Twelve across and a hundred down, so a month of 68 kilos is drawn 68\n// long and nothing has to be turned into pixels first.\nfunc chart(harvest: Int[]): ui.View {\n\tmut ui.Mark[dyn] marks = []\n\tfor i := 0; i < len(harvest); i++ {\n\t\tif harvest bounds i {\n\t\t\ttall := hive.conv.itf(harvest[i])\n\t\t\t// A bar is placed from its middle, which is why the floor is\n\t\t\t// 100 and the middle of one standing on it is half a bar up.\n\t\t\tappend(marks, ui.bar([ui.paint(ui.Tone.Good())],\n\t\t\t\thive.conv.itf(i) + 0.5, 100.0 - tall / 2.0, 0.7, tall))\n\t\t}\n\t}\n\tappend(marks, ui.words([ui.paint(ui.Tone.Muted())], 6.0, 8.0, 7.0, \"kg per month\"))\n\treturn ui.canvas([\n\t\tui.width(480),\n\t\tui.height(200),\n\t\tui.region(0.0, 0.0, 12.0, 100.0),\n\t], marks)\n}\n\nproc main(): void {\n\t// The same tree a window would swap in is what a page writes out.\n\techo len(ui.page(\"Harvest\", chart([31, 44, 58, 72, 68, 51])))\n}", CodePt: "import hive.ui\n\n// Doze de largura e cem de altura, então um mês de 68 quilos é desenhado\n// com 68 de comprimento e nada precisa virar pixel primeiro.\nfunc chart(harvest: Int[]): ui.View {\n\tmut ui.Mark[dyn] marks = []\n\tfor i := 0; i < len(harvest); i++ {\n\t\tif harvest bounds i {\n\t\t\ttall := hive.conv.itf(harvest[i])\n\t\t\t// Um bar é posicionado pelo seu meio, e é por isso que o chão\n\t\t\t// é 100 e o meio de um em pé sobre ele fica meia barra acima.\n\t\t\tappend(marks, ui.bar([ui.paint(ui.Tone.Good())],\n\t\t\t\thive.conv.itf(i) + 0.5, 100.0 - tall / 2.0, 0.7, tall))\n\t\t}\n\t}\n\tappend(marks, ui.words([ui.paint(ui.Tone.Muted())], 6.0, 8.0, 7.0, \"kg por mês\"))\n\treturn ui.canvas([\n\t\tui.width(480),\n\t\tui.height(200),\n\t\tui.region(0.0, 0.0, 12.0, 100.0),\n\t], marks)\n}\n\nproc main(): void {\n\t// A mesma árvore que uma window trocaria é a que uma página escreve.\n\techo len(ui.page(\"Colheita\", chart([31, 44, 58, 72, 68, 51])))\n}"}, slides_0_Example{Caption: "A line, a ring and a word", CaptionPt: "Uma linha, um anel e uma palavra", Code: "import hive.ui\n\nproc main(): void {\n\t// A line through five places — x, y, x, y — and a ring on the last of\n\t// them, which is a dot and so needed no name of its own.\n\ttrace := ui.path([ui.thick(0.15), ui.paint(ui.Tone.Good())],\n\t\t[0.0, 2.0, 1.0, 5.0, 2.0, 4.0, 3.0, 8.0, 4.0, 7.0])\n\ttip := ui.dot([ui.paint(ui.Tone.Good())], 4.0, 7.0, 0.2)\n\n\t// Words are centred on the place, and their height is in region units\n\t// too — so this one is a twelfth as tall as the plane it sits on.\n\tlabel := ui.words([ui.paint(ui.Tone.Muted())], 4.0, 8.4, 0.8, \"now\")\n\n\tview := ui.canvas([\n\t\tui.width(360),\n\t\tui.height(200),\n\t\tui.region(-0.5, 0.0, 5.5, 10.0),\n\t], [trace, tip, label])\n\n\techo ui.html(view)\n}", CodePt: "import hive.ui\n\nproc main(): void {\n\t// Uma linha através de cinco lugares — x, y, x, y — e um anel sobre o\n\t// último deles, que é um dot e por isso não precisou de nome próprio.\n\ttrace := ui.path([ui.thick(0.15), ui.paint(ui.Tone.Good())],\n\t\t[0.0, 2.0, 1.0, 5.0, 2.0, 4.0, 3.0, 8.0, 4.0, 7.0])\n\ttip := ui.dot([ui.paint(ui.Tone.Good())], 4.0, 7.0, 0.2)\n\n\t// words fica centralizado no lugar, e sua altura também está nas\n\t// unidades da região — então esta tem um doze avos do plano em que está.\n\tlabel := ui.words([ui.paint(ui.Tone.Muted())], 4.0, 8.4, 0.8, \"agora\")\n\n\tview := ui.canvas([\n\t\tui.width(360),\n\t\tui.height(200),\n\t\tui.region(-0.5, 0.0, 5.5, 10.0),\n\t], [trace, tip, label])\n\n\techo ui.html(view)\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "Three dimensions", TitlePt: "Três dimensões", Explanation: "A scene is the one widget only a window can draw, and it is a module of its own — hive.ui.scene — because it is the one thing in the standard library that carries a download: a pinned three.js, fetched on the first build that needs it and cached ever after. A window that draws no scene links none of it. A scene is a value like every other view: a camera, a sky, and a vector of shapes, rebuilt by an ordinary func from the state as often as there are frames. Nothing here is a handle — no mesh to keep, no material to allocate, no canvas to hold — so a program answers what is there right now, and never what has changed.\n\nbox, sphere, cylinder, cone, roof, ground, label, sprite and line are the shapes, and each takes its dimensions as its payload rather than as attributes, because they are what the shape is: a sphere without a radius is not a sphere with a default one. cone takes both of its radii rather than only the base — a cone in the world is hardly ever the perfect one, a track marker is snub-nosed and a nose cone ends in a blade, and a shape that could only be the perfect one gets composed around rather than used. Nought at the top is the perfect one; a top equal to the bottom is a cylinder. roof is a pitched one — a ridge down its length, a slope each side and a gable closing each end — the shape a box and a cone between them cannot make and half the built world is. ground already lies flat, so a floor costs no rotation and turn still means everywhere what it means anywhere; label always faces the viewer, because one you can read from one side only is not a label; sprite is a picture that does the same, naming a file in the program's own assets/ and standing at a size given in metres — its place is its foot rather than its middle, because what a sprite is for is something standing on the ground and one centred on its position is buried to the waist in it, and paint tints the image rather than replacing it, so one drawing made white serves every colour a scene asks of it; and line takes no position at all, since a line is between two places and has no third to call its own.\n\nat, turn and paint place and colour a shape. turn is a heading rather than three world axes: yaw first, then pitch about the shape's own lateral axis and roll about its own length, so a thing turned to face somewhere and then pitched tips nose-up the way it is facing. surface says what it is made of, which is a different question from what colour it is, and the set reads from the ground up: what you drive on — Turf, Gravel, Tarmac, Kerb — then what things are built of — Concrete, Brick, Panes, Planks, Staves, Bark, Leaves, Crowd, Mesh — then what they are finished in: Speckle, Metal, Carbon, Rubber, Glass and Gloss. Each is drawn over whatever colour the shape already is — so one pattern serves a red kerb and a blue one, and Gloss adds no pattern at all. Panes is the exception: it paints its glass a colour of its own rather than a shade of the shape's, because a window is a different material from the wall, and it lands one in every couple of metres of surface. Where a shape says nothing its kind decides: a ground is Turf, a cylinder or a cone is Staves, a sphere is Speckle, and everything else is Planks. And a colour carrying an alpha is translucent — Tone.RGBA(r, g, b, a), or an eight-digit Tone.HEX — drawn see-through and writing no depth, which is how a ghost, a tinted screen or the plume behind something gets drawn.\n\neye, aim, lens, fog, background, grain, bits, grab and crosshair go on the scene itself rather than on a shape: where the camera is and where it looks, how wide the lens is, how far you can see, the sky the fog fades into, and whether the window should hold the mouse. shadows(false) turns the sun's shadows off, and it is the one thing about the lighting a scene may say — a machine that cannot hold its frame rate can sell them before it sells anything that changes what is there. grain(px) and bits(n) are one decision in two halves: how few pixels the world is drawn into before it is blown back up unfiltered, and how few values each of its colour channels may take. grain is the short edge, and what it buys is a picture genuinely drawn small rather than a full-size one blurred — a wall a pixel wide is a pixel wide, with an edge on it. bits is per channel, and the quantisation is dithered rather than rounded, so a gradient comes out woven instead of banded; it is applied after the frame is encoded for the screen, because a display's bit depth is a fact about what it displays and not about the arithmetic behind it. Five bits is the fifteen-bit colour most handhelds had. Insets are drawn into the same buffer, so a second look at the world is grained with the first, and a scene that says neither is drawn at the size of its box in the colour the screen has. Lighting is deliberately not something a scene describes — the rig is fixed, chosen so that a solid painted a colour comes out that colour with a shaded side — so what makes a scene dark is its palette rather than a setting turned down. The events are a scene's own: onFrame reports how many milliseconds the last frame took, onRate how many hertz the screen turned out to run at, onKeyDown and onKeyUp a key's name, onLook how far the mouse moved, onGrab whether the window has the pointer, and onPad a gamepad.\n\nA gamepad is the one input a browser will not tell you about: there is no event for a stick moving, only a snapshot you have to ask for. So the runtime reads the pads once a frame and posts what changed — which makes a button on a pad the same kind of thing as a key going down, rather than a second way of hearing about input. onPad reports three things: which pad, which control on it, and where that control now is. One shape covers the whole device, because a button is a control that is only ever 0 or 1. leftX, leftY, rightX and rightY are the sticks; lt and rt the triggers; a, b, x, y, lb, rb, up, down, left, right, back, start, guide, lstick and rstick the buttons; and anything past those is reported by number. here is the pad itself: 1 when it arrives, 2 when it arrives with a layout the browser could not name, and 0 when it goes — and a pad being unplugged lets go of everything it was holding first, so a car whose controller ran flat mid-corner does not keep turning. A dead zone is applied before the event is sent, and nothing is sent until a control has actually moved, because a stick at rest is never quite at rest.\n\ninset is the one widget that draws in three dimensions without being a scene: a box on the page showing the same world from wherever its own eye, aim and lens are standing — a rear-view mirror, a replay box, a security camera. It holds a camera and no world, which is the whole of what makes it affordable: a second scene would be a second description of everything, sent every frame, while an inset is a second pass over the shapes the scene already handed over. One window draws one scene and any number of insets.\n\nA scene holds one more shape than the nine above, and that one is heard rather than drawn: it has the slide after this one to itself.\n\nA served page renders a scene's box empty — there is no socket for frames to travel down — so the examples below are shown as code only: open one in a window to actually see it.", ExplanationPt: "Uma scene é o único widget que somente uma window consegue desenhar, e é um módulo à parte — hive.ui.scene — porque é a única coisa na biblioteca padrão que carrega um download: um three.js fixado, buscado na primeira compilação que precisa dele e guardado em cache dali em diante. Uma window que não desenha nenhuma scene não linka nada disso. Uma scene é um valor como qualquer outra view: uma câmera, um céu e um vetor de formas, reconstruído por um func comum a partir do estado tantas vezes quantos forem os frames. Nada aqui é um handle — nenhuma malha para guardar, nenhum material para alocar, nenhum canvas para segurar — então um programa responde o que está ali agora, e nunca o que mudou.\n\nbox, sphere, cylinder, cone, roof, ground, label, sprite e line são as formas, e cada uma recebe suas dimensões como carga e não como atributos, porque elas são o que a forma é: uma esfera sem raio não é uma esfera com um raio padrão. cone recebe os seus dois raios em vez de só o da base — um cone no mundo quase nunca é o perfeito, um cone de sinalização é rombudo e o bico de um carro termina em lâmina, e uma forma que só pudesse ser a perfeita acaba sendo contornada em vez de usada. Zero no topo é a perfeita; um topo igual à base é um cilindro. roof é um telhado de duas águas — uma cumeeira ao longo dele, um caimento de cada lado e uma empena fechando cada ponta — a forma que um box e um cone juntos não fazem e que metade do mundo construído tem. ground já está deitado, então um chão não custa rotação nenhuma e turn continua significando em todo lugar o que significa em qualquer lugar; label sempre encara quem olha, porque uma que só dá para ler de um lado não é uma label; sprite é uma imagem que faz o mesmo, nomeando um arquivo dentro do assets/ do próprio programa e ficando de pé num tamanho dado em metros — o lugar dela é o pé, e não o meio, porque uma sprite serve para algo que está em pé no chão e uma centrada na própria posição fica enterrada até a cintura; e paint tinge a imagem em vez de substituí-la, então um único desenho feito em branco serve a todas as cores que uma scene pedir dele; e line não recebe posição alguma, já que uma linha fica entre dois lugares e não tem um terceiro para chamar de seu.\n\nat, turn e paint posicionam e colorem uma forma. turn é uma direção, e não três eixos do mundo: primeiro a guinada, depois a inclinação em torno do eixo lateral da própria forma e o rolamento em torno do comprimento dela — então algo virado para um lado e depois inclinado levanta o nariz na direção para onde está virado. surface diz do que ela é feita, que é uma pergunta diferente de qual é a cor dela, e o conjunto se lê do chão para cima: aquilo em que se anda — Turf, Gravel, Tarmac, Kerb — depois aquilo de que as coisas são feitas — Concrete, Brick, Panes, Planks, Staves, Bark, Leaves, Crowd, Mesh — e depois o acabamento delas: Speckle, Metal, Carbon, Rubber, Glass e Gloss. Cada uma é desenhada por cima da cor que a forma já tem — então um padrão só serve para um zebrado vermelho e para um azul, e Gloss não acrescenta padrão nenhum. Panes é a exceção: pinta o vidro com uma cor própria, e não com um tom da cor da forma, porque uma janela é de um material diferente do da parede — e aparece uma a cada dois metros de superfície. Onde a forma não diz nada, o tipo dela decide: um ground é Turf, um cylinder ou um cone é Staves, uma sphere é Speckle, e todo o resto é Planks. E uma cor que carrega alpha é translúcida — Tone.RGBA(r, g, b, a), ou um Tone.HEX de oito dígitos — desenhada vazada e sem escrever profundidade, que é como se desenha um fantasma, um vidro colorido ou o rastro atrás de alguma coisa.\n\neye, aim, lens, fog, background, grain, bits, grab e crosshair vão na própria scene, e não em uma forma: onde a câmera está e para onde olha, quão aberta é a lente, até onde se enxerga, o céu no qual a névoa se dissolve, e se a window deve segurar o mouse. shadows(false) desliga as sombras do sol, e é a única coisa que uma scene tem a dizer sobre a iluminação — uma máquina que não segura a taxa de quadros pode vender as sombras antes de vender qualquer coisa que mude o que está ali. grain(px) e bits(n) são uma decisão em duas metades: em quantos pixels o mundo é desenhado antes de ser ampliado de volta sem filtro, e quantos valores cada um dos canais de cor dele pode assumir. grain é o lado menor, e o que ele compra é uma imagem de fato desenhada pequena, e não uma de tamanho inteiro borrada — uma parede de um pixel de largura tem um pixel de largura, com uma borda nele. bits é por canal, e a quantização é pontilhada em vez de arredondada, então um degradê sai tramado e não em faixas; ela é aplicada depois de o quadro ser codificado para a tela, porque a profundidade de cor de um monitor é um fato sobre o que ele mostra e não sobre a aritmética por trás disso. Cinco bits é a cor de quinze bits que a maioria dos portáteis tinha. Insets são desenhados no mesmo buffer, então uma segunda olhada no mundo tem o mesmo grão da primeira, e uma scene que não diz nenhum dos dois é desenhada no tamanho da caixa dela, com a cor que a tela tem. Iluminação deliberadamente não é algo que uma scene descreve — o conjunto é fixo, escolhido para que um sólido pintado de uma cor saia daquela cor com um lado sombreado — então o que deixa uma scene escura é a paleta dela, e não uma configuração abaixada. Os eventos são só da scene: onFrame reporta quantos milissegundos o último frame levou, onRate a quantos hertz a tela acabou rodando, onKeyDown e onKeyUp o nome de uma tecla, onLook o quanto o mouse andou, onGrab se a window está com o ponteiro, e onPad um gamepad.\n\nUm gamepad é a única entrada sobre a qual um navegador não avisa: não existe evento para um analógico se mexer, só um retrato que você precisa pedir. Então o runtime lê os controles uma vez por frame e publica o que mudou — o que faz de um botão de controle a mesma coisa que uma tecla sendo pressionada, em vez de um segundo jeito de ouvir sobre entrada. onPad reporta três coisas: qual controle, qual comando dele, e onde esse comando está agora. Uma forma só cobre o aparelho inteiro, porque um botão é um comando que só é 0 ou 1. leftX, leftY, rightX e rightY são os analógicos; lt e rt os gatilhos; a, b, x, y, lb, rb, up, down, left, right, back, start, guide, lstick e rstick os botões; e qualquer coisa além disso é reportada por número. here é o próprio controle: 1 quando ele chega, 2 quando chega com um layout que o navegador não soube nomear, e 0 quando ele vai embora — e um controle desconectado solta antes tudo que estava segurando, então um carro cujo controle ficou sem bateria no meio da curva não continua virando. Uma zona morta é aplicada antes de o evento ser enviado, e nada é enviado até um comando ter de fato se mexido, porque um analógico parado nunca está totalmente parado.\n\ninset é o único widget que desenha em três dimensões sem ser uma scene: uma caixa na página mostrando o mesmo mundo de onde quer que estejam o eye, o aim e o lens dela — um retrovisor, uma caixa de replay, uma câmera de segurança. Ela guarda uma câmera e nenhum mundo, e é só isso que a torna barata: uma segunda scene seria uma segunda descrição de tudo, enviada a cada frame, enquanto um inset é uma segunda passada sobre as formas que a scene já entregou. Uma window desenha uma scene e quantos insets quiser.\n\nUma scene guarda mais uma forma além das nove acima, e essa é ouvida em vez de desenhada: ela tem o slide seguinte só para si.\n\nUma página servida renderiza a caixa de uma scene vazia — não há socket por onde os frames viajem — então os exemplos abaixo são mostrados apenas como código: abra um deles em uma window para realmente ver.", Examples: []slides_0_Example{slides_0_Example{Caption: "A 3D scene, in a window", CaptionPt: "Uma cena 3D, em uma window", Code: "type Msg { Tick }\n\nfunc view(model: Int): hive.ui.View {\n\tshapes := [\n\t\thive.ui.ground([hive.ui.paint(hive.ui.Tone.HEX(\"#2d5c34\"))], 20.0, 20.0),\n\t\thive.ui.box(\n\t\t\t[\n\t\t\t\thive.ui.at(0.0, 0.5, -3.0),\n\t\t\t\thive.ui.paint(hive.ui.Tone.HEX(\"#b5651d\")),\n\t\t\t\thive.ui.surface(hive.ui.Surface.Planks()),\n\t\t\t],\n\t\t\t1.0, 1.0, 1.0,\n\t\t),\n\t\thive.ui.sphere([hive.ui.at(2.0, 1.0, -4.0), hive.ui.paint(hive.ui.Tone.HEX(\"#e0c341\"))], 1.0),\n\t\t// A cone takes both radii: nought at the top is the pointed one.\n\t\thive.ui.cone(\n\t\t\t[\n\t\t\t\thive.ui.at(-2.0, 0.4, -3.0),\n\t\t\t\thive.ui.paint(hive.ui.Tone.HEX(\"#ff5a1f\")),\n\t\t\t\thive.ui.surface(hive.ui.Surface.Gloss()),\n\t\t\t],\n\t\t\t0.0, 0.4, 0.8,\n\t\t),\n\t\t// And a colour with an alpha is drawn see-through.\n\t\thive.ui.box(\n\t\t\t[hive.ui.at(0.0, 1.2, -1.5), hive.ui.paint(hive.ui.Tone.RGBA(120, 200, 255, 96))],\n\t\t\t3.0, 2.0, 0.2,\n\t\t),\n\t]\n\treturn hive.ui.scene(\n\t\t[hive.ui.grow(1), hive.ui.eye(0.0, 1.6, 4.0), hive.ui.aim(0.0, 0.0), hive.ui.lens(70), hive.ui.fog(60)],\n\t\tshapes,\n\t)\n}\n\nproc update(model: Int, msg: Msg, envelope: hive.syslink.Envelope): Int {\n\treturn model\n}\n\nproc main(): void {\n\thive.ui.window(\"A 3D scene\", view, update, 0)\n}", CodePt: "type Msg { Tick }\n\nfunc view(model: Int): hive.ui.View {\n\tshapes := [\n\t\thive.ui.ground([hive.ui.paint(hive.ui.Tone.HEX(\"#2d5c34\"))], 20.0, 20.0),\n\t\thive.ui.box(\n\t\t\t[\n\t\t\t\thive.ui.at(0.0, 0.5, -3.0),\n\t\t\t\thive.ui.paint(hive.ui.Tone.HEX(\"#b5651d\")),\n\t\t\t\thive.ui.surface(hive.ui.Surface.Planks()),\n\t\t\t],\n\t\t\t1.0, 1.0, 1.0,\n\t\t),\n\t\thive.ui.sphere([hive.ui.at(2.0, 1.0, -4.0), hive.ui.paint(hive.ui.Tone.HEX(\"#e0c341\"))], 1.0),\n\t\t// Um cone recebe os dois raios: zero no topo é o pontudo.\n\t\thive.ui.cone(\n\t\t\t[\n\t\t\t\thive.ui.at(-2.0, 0.4, -3.0),\n\t\t\t\thive.ui.paint(hive.ui.Tone.HEX(\"#ff5a1f\")),\n\t\t\t\thive.ui.surface(hive.ui.Surface.Gloss()),\n\t\t\t],\n\t\t\t0.0, 0.4, 0.8,\n\t\t),\n\t\t// E uma cor com alpha é desenhada translúcida.\n\t\thive.ui.box(\n\t\t\t[hive.ui.at(0.0, 1.2, -1.5), hive.ui.paint(hive.ui.Tone.RGBA(120, 200, 255, 96))],\n\t\t\t3.0, 2.0, 0.2,\n\t\t),\n\t]\n\treturn hive.ui.scene(\n\t\t[hive.ui.grow(1), hive.ui.eye(0.0, 1.6, 4.0), hive.ui.aim(0.0, 0.0), hive.ui.lens(70), hive.ui.fog(60)],\n\t\tshapes,\n\t)\n}\n\nproc update(model: Int, msg: Msg, envelope: hive.syslink.Envelope): Int {\n\treturn model\n}\n\nproc main(): void {\n\thive.ui.window(\"Uma cena 3D\", view, update, 0)\n}"}, slides_0_Example{Caption: "Animating it, and reacting to a frame", CaptionPt: "Animando, e reagindo a um frame", Code: "type Msg {\n\tTick { ms: Int }\n}\n\nfunc view(angle: Float): hive.ui.View {\n\treturn hive.ui.scene(\n\t\t[\n\t\t\thive.ui.grow(1),\n\t\t\thive.ui.eye(0.0, 1.5, 5.0),\n\t\t\thive.ui.aim(0.0, 0.0),\n\t\t\thive.ui.onFrame(Msg.Tick(_)),\n\t\t],\n\t\t[\n\t\t\thive.ui.ground([hive.ui.paint(hive.ui.Tone.Muted())], 10.0, 10.0),\n\t\t\thive.ui.box(\n\t\t\t\t[hive.ui.at(0.0, 1.0, 0.0), hive.ui.turn(0.0, angle, 0.0), hive.ui.paint(hive.ui.Tone.Good())],\n\t\t\t\t1.0, 1.0, 1.0,\n\t\t\t),\n\t\t],\n\t)\n}\n\n// A scene's onFrame reports how many milliseconds the last frame took, so\n// the turn this box makes is in real seconds rather than in frames — the\n// same box spins at the same speed at thirty frames a second or sixty.\nproc spin(angle: Float, msg: Msg, envelope: hive.syslink.Envelope): Float {\n\tif msg is Msg.Tick(ms) {\n\t\treturn angle + hive.conv.itf(ms) * 0.002\n\t}\n\treturn angle\n}\n\nproc main(): void {\n\thive.ui.window(\"A spinning box\", view, spin, 0.0)\n}", CodePt: "type Msg {\n\tTick { ms: Int }\n}\n\nfunc view(angle: Float): hive.ui.View {\n\treturn hive.ui.scene(\n\t\t[\n\t\t\thive.ui.grow(1),\n\t\t\thive.ui.eye(0.0, 1.5, 5.0),\n\t\t\thive.ui.aim(0.0, 0.0),\n\t\t\thive.ui.onFrame(Msg.Tick(_)),\n\t\t],\n\t\t[\n\t\t\thive.ui.ground([hive.ui.paint(hive.ui.Tone.Muted())], 10.0, 10.0),\n\t\t\thive.ui.box(\n\t\t\t\t[hive.ui.at(0.0, 1.0, 0.0), hive.ui.turn(0.0, angle, 0.0), hive.ui.paint(hive.ui.Tone.Good())],\n\t\t\t\t1.0, 1.0, 1.0,\n\t\t\t),\n\t\t],\n\t)\n}\n\n// O onFrame de uma scene reporta quantos milissegundos o último frame levou,\n// então o giro que esta caixa faz é em segundos reais, não em frames — a\n// mesma caixa gira na mesma velocidade a trinta quadros por segundo ou a sessenta.\nproc spin(angle: Float, msg: Msg, envelope: hive.syslink.Envelope): Float {\n\tif msg is Msg.Tick(ms) {\n\t\treturn angle + hive.conv.itf(ms) * 0.002\n\t}\n\treturn angle\n}\n\nproc main(): void {\n\thive.ui.window(\"Uma caixa girando\", view, spin, 0.0)\n}"}, slides_0_Example{Caption: "Driving it with a gamepad", CaptionPt: "Dirigindo com um gamepad", Code: "type Msg {\n\tTick { ms: Int }\n\tPadded { pad: Int, control: Str, at: Float }\n}\n\ntype Kart {\n\tx:        Float\n\tz:        Float\n\tsteer:    Float\n\tthrottle: Float\n}\n\nfunc view(kart: Kart): hive.ui.View {\n\treturn hive.ui.scene(\n\t\t[\n\t\t\thive.ui.grow(1),\n\t\t\thive.ui.eye(kart.x, 4.0, kart.z + 9.0),\n\t\t\thive.ui.aim(0.0, -0.25),\n\t\t\thive.ui.lens(70),\n\t\t\thive.ui.onFrame(Msg.Tick(_)),\n\t\t\thive.ui.onPad(Msg.Padded(_, _, _)),\n\t\t],\n\t\t[\n\t\t\thive.ui.ground(\n\t\t\t\t[hive.ui.paint(hive.ui.Tone.HEX(\"#61686f\")), hive.ui.surface(hive.ui.Surface.Tarmac())],\n\t\t\t\t120.0, 120.0,\n\t\t\t),\n\t\t\thive.ui.box(\n\t\t\t\t[\n\t\t\t\t\thive.ui.at(kart.x, 0.4, kart.z),\n\t\t\t\t\thive.ui.paint(hive.ui.Tone.HEX(\"#00d2be\")),\n\t\t\t\t\thive.ui.surface(hive.ui.Surface.Gloss()),\n\t\t\t\t],\n\t\t\t\t1.6, 0.6, 3.4,\n\t\t\t),\n\t\t],\n\t)\n}\n\n// A stick and a trigger arrive through the same hole and are told apart by\n// name, because a button is a control that is only ever 0 or 1. Nothing is\n// held on to here that the pad did not say: onPad reports what *changed*,\n// so the model remembers the last position of each control it cares about.\nproc drive(kart: Kart, msg: Msg, envelope: hive.syslink.Envelope): Kart {\n\tmut Kart it = kart\n\tif msg is Msg.Padded(pad, control, at) {\n\t\tif control == \"leftX\" { it.steer = at }\n\t\tif control == \"rt\" { it.throttle = at }\n\t\treturn it\n\t}\n\tif msg is Msg.Tick(ms) {\n\t\tstep := hive.conv.itf(ms) / 1000.0\n\t\tit.x = it.x + it.steer * it.throttle * 16.0 * step\n\t\tit.z = it.z - it.throttle * 16.0 * step\n\t}\n\treturn it\n}\n\nproc main(): void {\n\thive.ui.window(\"A pad\", view, drive, Kart(0.0, 0.0, 0.0, 0.0))\n}", CodePt: "type Msg {\n\tTick { ms: Int }\n\tPadded { pad: Int, control: Str, at: Float }\n}\n\ntype Kart {\n\tx:        Float\n\tz:        Float\n\tsteer:    Float\n\tthrottle: Float\n}\n\nfunc view(kart: Kart): hive.ui.View {\n\treturn hive.ui.scene(\n\t\t[\n\t\t\thive.ui.grow(1),\n\t\t\thive.ui.eye(kart.x, 4.0, kart.z + 9.0),\n\t\t\thive.ui.aim(0.0, -0.25),\n\t\t\thive.ui.lens(70),\n\t\t\thive.ui.onFrame(Msg.Tick(_)),\n\t\t\thive.ui.onPad(Msg.Padded(_, _, _)),\n\t\t],\n\t\t[\n\t\t\thive.ui.ground(\n\t\t\t\t[hive.ui.paint(hive.ui.Tone.HEX(\"#61686f\")), hive.ui.surface(hive.ui.Surface.Tarmac())],\n\t\t\t\t120.0, 120.0,\n\t\t\t),\n\t\t\thive.ui.box(\n\t\t\t\t[\n\t\t\t\t\thive.ui.at(kart.x, 0.4, kart.z),\n\t\t\t\t\thive.ui.paint(hive.ui.Tone.HEX(\"#00d2be\")),\n\t\t\t\t\thive.ui.surface(hive.ui.Surface.Gloss()),\n\t\t\t\t],\n\t\t\t\t1.6, 0.6, 3.4,\n\t\t\t),\n\t\t],\n\t)\n}\n\n// Um analógico e um gatilho chegam pelo mesmo buraco e são distinguidos pelo\n// nome, porque um botão é um comando que só é 0 ou 1. Nada é guardado aqui\n// que o controle não tenha dito: onPad reporta o que *mudou*, então o modelo\n// lembra a última posição de cada comando que lhe interessa.\nproc drive(kart: Kart, msg: Msg, envelope: hive.syslink.Envelope): Kart {\n\tmut Kart it = kart\n\tif msg is Msg.Padded(pad, control, at) {\n\t\tif control == \"leftX\" { it.steer = at }\n\t\tif control == \"rt\" { it.throttle = at }\n\t\treturn it\n\t}\n\tif msg is Msg.Tick(ms) {\n\t\tstep := hive.conv.itf(ms) / 1000.0\n\t\tit.x = it.x + it.steer * it.throttle * 16.0 * step\n\t\tit.z = it.z - it.throttle * 16.0 * step\n\t}\n\treturn it\n}\n\nproc main(): void {\n\thive.ui.window(\"Um controle\", view, drive, Kart(0.0, 0.0, 0.0, 0.0))\n}"}}, Download: false, Shot: "/shot/three-dimensions.png"}, slides_0_Slide{Title: "A sound is a shape", TitlePt: "Um som é uma forma", Explanation: "sound(attrs, name) is a scene's eighth shape, and the one that is heard from where it stands instead of drawn where it stands. It takes at like every other shape and three of its own: voice(Voice) is which noise it is, pitch(Float) how high and volume(Float) how loud. Both of those are multiples of that voice at rest rather than hertz and decibels — what an engine sounds like at 1.0 is the renderer's business, and a program says twice as fast and half as loud, which is what it actually knows.\n\nA sound is listed beside the solids for one reason worth more than the oddity costs: a noise in a world has a place in it, and a place is what every other member of that set already knows how to carry. So it is positioned by the same at, written into the same frame, and arrives at the page with the very camera that has to hear it — where a module of its own would be a second description of where things are.\n\nThe payload is a name, and the name is the shape's identity between frames. This is the one place audio cannot follow the rule the solids follow. A box is matched to whatever was in its slot last frame, because a view built by the same code lists the same things in the same order — and a box rebuilt anyway is a box nobody can tell was rebuilt. An oscillator rebuilt anyway is a click, sixty of them a second, and a slot cannot promise otherwise, because a scene is free to stop drawing the car in front and every sound after it would shift up a place and become a different engine. So a name that is in this frame and was in the last one carries on, a name that has just arrived starts, and a name that has gone fades out.\n\nWhich of those a voice actually does belongs to the voice rather than to the program. Engine, Tyres, Brakes, Wind, Crowd and Music are sustained: they last as long as the scene goes on naming them. Impact, Chime and Talk are struck: they fire once, when the name arrives, and are deaf to everything after it — because a crash is a thing that happened rather than a thing that is happening, and a world of values has no other way to say so. Naming \"hit-3\" for as long as a car knows it was hit is one impact; letting the name go and bringing it back is a second one.\n\nSounds are positioned and the listener is the scene's own eye and aim, so what is heard on the left is what is drawn on the left, and a car goes past your ear as well as your eye. Music and Talk are the exceptions, having nowhere to stand, so neither is ever panned — and Talk needs to be both struck and unpanned, because a struck voice is positioned once and never moved, so a spoken line pinned to a place is one the listener then walks away from. And nothing is downloaded for any of it: every voice is oscillators and filtered noise, for the same reason a Surface is a drawn pattern rather than an image — a built Hive program is one file. That buys something as well as costing something, since a voice made of a frequency can be bent, and an engine that is a recording of one speed played faster is an engine you can hear looping.", ExplanationPt: "sound(attrs, nome) é a oitava forma de uma scene, e a única que é ouvida de onde está em vez de desenhada onde está. Ela recebe at como toda outra forma e três atributos próprios: voice(Voice) é que barulho ela é, pitch(Float) quão agudo e volume(Float) quão alto. Os dois são múltiplos daquela voz em repouso, e não hertz e decibéis — como um motor soa em 1.0 é assunto do renderizador, e um programa diz duas vezes mais rápido e metade do volume, que é o que ele de fato sabe.\n\nUm som está listado ao lado dos sólidos por um motivo que vale mais do que a estranheza custa: um barulho em um mundo tem um lugar nele, e um lugar é o que todo outro membro daquele conjunto já sabe carregar. Então ele é posicionado pelo mesmo at, escrito no mesmo frame, e chega à página com a mesma câmera que precisa ouvi-lo — onde um módulo próprio seria uma segunda descrição de onde as coisas estão.\n\nA carga é um nome, e o nome é a identidade da forma entre frames. Este é o único lugar em que o áudio não pode seguir a regra que os sólidos seguem. Uma box é casada com o que estava no lugar dela no frame anterior, porque uma view construída pelo mesmo código lista as mesmas coisas na mesma ordem — e uma box reconstruída mesmo assim é uma box que ninguém percebe que foi reconstruída. Um oscilador reconstruído mesmo assim é um clique, sessenta deles por segundo, e uma posição não pode prometer o contrário, porque uma scene é livre para parar de desenhar o carro da frente e todo som depois dele subiria uma posição e viraria outro motor. Então um nome que está neste frame e estava no anterior continua, um nome que acabou de chegar começa, e um nome que sumiu se apaga.\n\nQual dessas coisas uma voz de fato faz pertence à voz, não ao programa. Engine, Tyres, Brakes, Wind, Crowd e Music são sustentadas: duram enquanto a scene continuar as nomeando. Impact, Chime e Talk são percutidas: disparam uma vez, quando o nome chega, e ficam surdas a tudo depois disso — porque uma batida é algo que aconteceu e não algo que está acontecendo, e um mundo de valores não tem outro jeito de dizer isso. Nomear \"hit-3\" enquanto um carro souber que bateu é um impacto só; soltar o nome e trazê-lo de volta é um segundo.\n\nSons são posicionados e quem escuta é o próprio eye e aim da scene, então o que se ouve à esquerda é o que está desenhado à esquerda, e um carro passa pelo seu ouvido tanto quanto pelo seu olho. Music e Talk são as exceções, por não terem onde ficar, então nenhuma delas é panoramizada — e Talk precisa ser percutida e sem posição ao mesmo tempo, porque uma voz percutida é posicionada uma vez e nunca mais movida, então uma fala presa a um lugar é uma fala da qual quem ouve depois se afasta. E nada é baixado para nada disso: toda voz é osciladores e ruído filtrado, pelo mesmo motivo que uma Surface é um padrão desenhado e não uma imagem — um programa Hive construído é um arquivo só. Isso compra algo além de custar algo, já que uma voz feita de frequência pode ser dobrada, e um motor que é a gravação de uma rotação tocada mais rápido é um motor que dá para ouvir dando a volta.", Examples: []slides_0_Example{slides_0_Example{Caption: "Sustained and struck, side by side", CaptionPt: "Sustentada e percutida, lado a lado", Code: "import hive.ui\n\ntype Msg { Tick }\n\ntype Car {\n\tseat:  Int\n\tx:     Float\n\tz:     Float\n\tspeed: Float\n\tknock: Bool\n}\n\n// A func like every other shape-maker, and worth noticing that a sound is\n// exactly as much of a value as a box is: sixty of these are built a second\n// and thrown away, while the voice on the other end of the name goes on\n// running the whole time.\nfunc noises(cars: Car[]): ui.Shape[dyn] {\n\tmut ui.Shape[dyn] out = []\n\tfor each car in cars {\n\t\t// Sustained: it lasts as long as the scene goes on naming it, and\n\t\t// bends with the speed rather than being restarted at a new one.\n\t\tappend(out, ui.sound([\n\t\t\tui.at(car.x, 0.6, car.z),\n\t\t\tui.voice(ui.Voice.Engine()),\n\t\t\tui.pitch(0.7 + car.speed / 90.0),\n\t\t\tui.volume(0.6),\n\t\t], \"engine-{car.seat}\"))\n\n\t\t// Struck: it fires when the name arrives and is deaf to everything\n\t\t// after it. Letting the name go and bringing it back is a second one.\n\t\tif car.knock {\n\t\t\tappend(out, ui.sound([\n\t\t\t\tui.at(car.x, 0.6, car.z),\n\t\t\t\tui.voice(ui.Voice.Impact()),\n\t\t\t\tui.pitch(1.0),\n\t\t\t\tui.volume(0.8),\n\t\t\t], \"hit-{car.seat}\"))\n\t\t}\n\t}\n\treturn out\n}\n\nfunc view(cars: Car[dyn]): ui.View {\n\tsolids := [ui.ground([ui.paint(ui.Tone.HEX(\"#3a3f45\"))], 400.0, 400.0)]\n\treturn ui.scene([\n\t\tui.grow(1),\n\t\tui.eye(0.0, 1.4, 6.0),\n\t\tui.aim(0.0, 0.0),\n\t\tui.background(ui.Tone.HEX(\"#8ec5ff\")),\n\t], solids + noises(cars))\n}\n\nproc update(cars: Car[dyn], msg: Msg, envelope: hive.syslink.Envelope): Car[dyn] {\n\treturn cars\n}\n\nproc main(): void {\n\tCar[dyn] grid = [Car(1, -2.0, 0.0, 40.0, false), Car(2, 2.0, -6.0, 62.0, true)]\n\tui.window(\"Sound\", view, update, grid)\n}", CodePt: "import hive.ui\n\ntype Msg { Tick }\n\ntype Car {\n\tseat:  Int\n\tx:     Float\n\tz:     Float\n\tspeed: Float\n\tknock: Bool\n}\n\n// Um func como todo outro construtor de formas deste arquivo, e vale notar\n// que um som é exatamente tão valor quanto uma box: sessenta destes são\n// construídos por segundo e jogados fora, enquanto a voz do outro lado do\n// nome segue rodando o tempo todo.\nfunc noises(cars: Car[]): ui.Shape[dyn] {\n\tmut ui.Shape[dyn] out = []\n\tfor each car in cars {\n\t\t// Sustentada: dura enquanto a scene continuar a nomeando, e dobra\n\t\t// com a velocidade em vez de ser reiniciada em uma nova.\n\t\tappend(out, ui.sound([\n\t\t\tui.at(car.x, 0.6, car.z),\n\t\t\tui.voice(ui.Voice.Engine()),\n\t\t\tui.pitch(0.7 + car.speed / 90.0),\n\t\t\tui.volume(0.6),\n\t\t], \"engine-{car.seat}\"))\n\n\t\t// Percutida: dispara quando o nome chega e fica surda a tudo depois\n\t\t// disso. Soltar o nome e trazê-lo de volta é um segundo impacto.\n\t\tif car.knock {\n\t\t\tappend(out, ui.sound([\n\t\t\t\tui.at(car.x, 0.6, car.z),\n\t\t\t\tui.voice(ui.Voice.Impact()),\n\t\t\t\tui.pitch(1.0),\n\t\t\t\tui.volume(0.8),\n\t\t\t], \"hit-{car.seat}\"))\n\t\t}\n\t}\n\treturn out\n}\n\nfunc view(cars: Car[dyn]): ui.View {\n\tsolids := [ui.ground([ui.paint(ui.Tone.HEX(\"#3a3f45\"))], 400.0, 400.0)]\n\treturn ui.scene([\n\t\tui.grow(1),\n\t\tui.eye(0.0, 1.4, 6.0),\n\t\tui.aim(0.0, 0.0),\n\t\tui.background(ui.Tone.HEX(\"#8ec5ff\")),\n\t], solids + noises(cars))\n}\n\nproc update(cars: Car[dyn], msg: Msg, envelope: hive.syslink.Envelope): Car[dyn] {\n\treturn cars\n}\n\nproc main(): void {\n\tCar[dyn] grid = [Car(1, -2.0, 0.0, 40.0, false), Car(2, 2.0, -6.0, 62.0, true)]\n\tui.window(\"Som\", view, update, grid)\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "Testing", TitlePt: "Testes", Explanation: "A test is a top-level declaration, named in prose because the name is documentation rather than something anything calls. `hive test entrypoint.hive` runs every test the entrypoint reaches, with no framework and no assertion vocabulary beyond the assert the language already has — and reports coverage, including which declarations were never exercised, on every run without a separate flag. This file has no main at all — a file holding only tests needs none — so it runs with `hive test`, not `hive run`.", ExplanationPt: "Um test é uma declaração de nível superior, nomeada em prosa porque o nome é documentação, não algo que alguma coisa chama. `hive test entrypoint.hive` roda todo test que o entrypoint alcança, sem framework e sem vocabulário de asserção além do assert que a linguagem já tem — e reporta cobertura, incluindo quais declarações nunca foram exercitadas, em toda execução sem uma flag separada. Este arquivo não tem main algum — um arquivo só com tests não precisa de um — então ele roda com `hive test`, não com `hive run`.", Examples: []slides_0_Example{slides_0_Example{Caption: "", CaptionPt: "", Code: "type Item {\n\tname:  Str\n\tprice: Int\n}\n\nfunc total(items: Item[]): Int {\n\tmut sum := 0\n\tfor each item in items {\n\t\tsum = sum + item.price\n\t}\n\treturn sum\n}\n\ntest \"an empty cart costs nothing\" {\n\tItem[0] empty = []\n\tassert total(empty) == 0\n}\n\ntest \"two items add up\" {\n\tassert total([Item(\"wax\", 5), Item(\"comb\", 3)]) == 8\n}", CodePt: "type Item {\n\tname:  Str\n\tprice: Int\n}\n\nfunc total(items: Item[]): Int {\n\tmut sum := 0\n\tfor each item in items {\n\t\tsum = sum + item.price\n\t}\n\treturn sum\n}\n\ntest \"um carrinho vazio não custa nada\" {\n\tItem[0] empty = []\n\tassert total(empty) == 0\n}\n\ntest \"dois itens se somam\" {\n\tassert total([Item(\"wax\", 5), Item(\"comb\", 3)]) == 8\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "Distributed actors", TitlePt: "Atores distribuídos", Explanation: "hive.syslink gives a program addressable services, in this process or on another machine, reached by the identical statement either way. spawn starts one and register publishes it under an atom; at reaches a service on this node and on reaches one on another, both without any I/O. A service's handler is a fold over its mailbox — proc(State, Message, Envelope): State — so there is no mutex anywhere: the fold is the mutex, and answer inside it is how a deferred reply is sent. self reads a handler's own address, monitor asks to be told if another address stops, and stop ends one outright. A crash inside one service kills only that service and leaves the rest of the node running.\n\nMissing a reply on one branch is the easiest mistake there is to make in service code, and waiting out a timeout is a miserable way to be told about it: it points at the network when the problem is a missing line. So answer is a no-op when the sender used async and nothing is waiting — one handler serves both shapes without caring which it was — and a request a service handles without ever answering comes straight back as NoReply, naming the service, at once rather than at the deadline. You write nothing to get that.\n\nUnless the envelope leaves the turn. If it goes anywhere else — stored in the returned state, handed to a call the handler does not wait for, passed to a proc of your own — a reply may genuinely still be on its way, and the runtime keeps waiting. That is the deferred reply: hand the envelope to an async call and answer whenever the work is done.\n\nlisten opens this node to others at an endpoint — an ip:port — and node/peers say who it is and who else it is currently connected to. Two nodes need nothing shared beyond a cluster key, written to ~/.hive/syslink.key on first run: copy that file to the second machine, or set HIVE_SYSLINK_KEY the same on both, and every connection between them is TLS 1.3 either way.", ExplanationPt: "hive.syslink dá a um programa serviços endereçáveis, neste processo ou em outra máquina, alcançados pela mesma instrução de qualquer forma. spawn inicia um e register o publica sob um átomo; at alcança um serviço neste nó e on alcança um em outro, ambos sem nenhum I/O. O handler de um serviço é um fold sobre sua caixa de mensagens — proc(State, Message, Envelope): State — então não há mutex em lugar nenhum: o fold é o mutex, e answer dentro dele é como uma resposta adiada é enviada. self lê o próprio endereço de um handler, monitor pede para ser avisado caso outro endereço pare, e stop encerra um serviço. Uma falha dentro de um serviço mata apenas aquele serviço e deixa o resto do nó rodando.\n\nEsquecer a resposta em um ramo é o erro mais fácil de cometer em código de serviço, e esperar um timeout até o fim é uma forma miserável de ser avisado disso: aponta para a rede quando o problema é uma linha que falta. Então answer é um no-op quando quem enviou usou async e nada está esperando — um mesmo handler serve às duas formas sem se importar com qual foi — e uma requisição que um serviço trata sem nunca responder volta na hora como NoReply, nomeando o serviço, em vez de voltar no prazo. Você não escreve nada para ter isso.\n\nA menos que o envelope saia do turno. Se ele for para qualquer outro lugar — guardado no estado retornado, entregue a uma chamada que o handler não espera, passado a um proc seu — uma resposta pode genuinamente ainda estar a caminho, e o runtime continua esperando. Essa é a resposta adiada: entregue o envelope a uma chamada async e responda quando o trabalho terminar.\n\nlisten abre este nó para outros em um endpoint — um ip:port — e node/peers dizem quem ele é e com quem mais está conectado atualmente. Dois nós não precisam de nada compartilhado além de uma chave de cluster, escrita em ~/.hive/syslink.key na primeira execução: copie esse arquivo para a segunda máquina, ou defina HIVE_SYSLINK_KEY da mesma forma nas duas, e toda conexão entre elas é TLS 1.3 de qualquer forma.", Examples: []slides_0_Example{slides_0_Example{Caption: "A service on this node", CaptionPt: "Um serviço neste nó", Code: "type Note {\n\tSay   { text: Str }\n\tCount\n\tTotal { count: Int }\n}\n\nproc tally(state: Int, msg: Note, envelope: hive.syslink.Envelope): Int {\n\tif msg is Note.Say(text) {\n\t\techo text\n\t\treturn state\n\t} else if msg is Note.Count {\n\t\thive.syslink.answer(envelope, Note.Total(state))\n\t\treturn state + 1\n\t}\n\treturn state\n}\n\nproc main(): void {\n\taddress := hive.syslink.spawn(tally, 0)\n\thive.syslink.register(#Cache, address)\n\n\tasync address(Note.Say(\"hi\"))                    // fire and forget\n\treply := address(Note.Count()) with timeout 250   // send and wait, bounded\n\tif reply is Result.Ok(msg) {\n\t\techo msg\n\t}\n}", CodePt: "type Note {\n\tSay   { text: Str }\n\tCount\n\tTotal { count: Int }\n}\n\nproc tally(state: Int, msg: Note, envelope: hive.syslink.Envelope): Int {\n\tif msg is Note.Say(text) {\n\t\techo text\n\t\treturn state\n\t} else if msg is Note.Count {\n\t\thive.syslink.answer(envelope, Note.Total(state))\n\t\treturn state + 1\n\t}\n\treturn state\n}\n\nproc main(): void {\n\taddress := hive.syslink.spawn(tally, 0)\n\thive.syslink.register(#Cache, address)\n\n\tasync address(Note.Say(\"oi\"))                     // dispara e esquece\n\treply := address(Note.Count()) with timeout 250   // envia e espera, limitado\n\tif reply is Result.Ok(msg) {\n\t\techo msg\n\t}\n}"}, slides_0_Example{Caption: "Answering later, and not answering at all", CaptionPt: "Respondendo depois, e não respondendo", Code: "type Note {\n\tAsk\n\tSlowly\n\tIgnore\n\tAnswer { text: Str }\n}\n\nproc desk(state: Int, note: Note, from: hive.syslink.Envelope): Int {\n\tif note is Note.Ask {\n\t\thive.syslink.answer(from, Note.Answer(\"here you go\"))\n\t} else if note is Note.Slowly {\n\t\tasync answerLate(from)      // the envelope leaves the turn\n\t}\n\treturn state + 1                 // Note.Ignore falls through: no answer\n}\n\n// Handed an envelope by a call nothing waits for, so the reply is allowed\n// to arrive whenever this is done.\nproc answerLate(from: hive.syslink.Envelope): void {\n\thive.task.sleep(300)\n\thive.syslink.answer(from, Note.Answer(\"worth the wait\"))\n}\n\nproc main(): void {\n\tdesk := hive.syslink.spawn(desk, 0)\n\n\tif desk(Note.Ask()) with timeout 500 is Result.Ok(now) { echo now }\n\tif desk(Note.Slowly()) with timeout 1000 is Result.Ok(late) { echo late }\n\n\t// Comes back at once, not at the deadline.\n\tif desk(Note.Ignore()) with timeout 5000 is Result.Error(err) { echo err }\n}", CodePt: "type Note {\n\tAsk\n\tSlowly\n\tIgnore\n\tAnswer { text: Str }\n}\n\nproc desk(state: Int, note: Note, from: hive.syslink.Envelope): Int {\n\tif note is Note.Ask {\n\t\thive.syslink.answer(from, Note.Answer(\"aqui está\"))\n\t} else if note is Note.Slowly {\n\t\tasync answerLate(from)      // o envelope sai do turno\n\t}\n\treturn state + 1                 // Note.Ignore passa direto: sem resposta\n}\n\n// Recebeu um envelope de uma chamada que ninguém espera, então a resposta\n// pode chegar quando isto aqui terminar.\nproc answerLate(from: hive.syslink.Envelope): void {\n\thive.task.sleep(300)\n\thive.syslink.answer(from, Note.Answer(\"valeu a espera\"))\n}\n\nproc main(): void {\n\tdesk := hive.syslink.spawn(desk, 0)\n\n\tif desk(Note.Ask()) with timeout 500 is Result.Ok(now) { echo now }\n\tif desk(Note.Slowly()) with timeout 1000 is Result.Ok(late) { echo late }\n\n\t// Volta na hora, não no prazo.\n\tif desk(Note.Ignore()) with timeout 5000 is Result.Error(err) { echo err }\n}"}, slides_0_Example{Caption: "The same service, truly distributed", CaptionPt: "O mesmo serviço, verdadeiramente distribuído", Code: "type Note {\n\tSay     { text: Str }\n\tHowMany\n\tCounted { seen: Int }\n}\n\nproc inbox(seen: Int, note: Note, from: hive.syslink.Envelope): Int {\n\tif note is Note.Say(text) {\n\t\techo \"heard: {text}\"\n\t\treturn seen + 1\n\t} else if note is Note.HowMany {\n\t\thive.syslink.answer(from, Note.Counted(seen))\n\t\treturn seen\n\t}\n\treturn seen\n}\n\n// Run this exact program on two machines: a node is identified by where it\n// is, so nothing about the two processes differs but the addresses typed\n// in below — 192.168.1.10:9100 and 192.168.1.11:9100, one each way round.\nproc main(): void {\n\techo \"This node's own ip:port?\"\n\tme := hive.term.read()\n\techo \"The other node's ip:port?\"\n\tpeer := hive.term.read()\n\n\tif hive.syslink.listen(me) is Result.Error(err) {\n\t\tpanic err\n\t}\n\n\tbox := hive.syslink.spawn(inbox, 0)\n\thive.syslink.register(#Inbox, box)\n\n\tmine := hive.syslink.at(#Inbox)\n\ttheirs := hive.syslink.on(peer, #Inbox)\n\n\tasync theirs(Note.Say(\"hello from {me}\"))\n\thive.task.sleep(500)\n\techo \"peers connected: {len(hive.syslink.peers())}\"\n\n\tif theirs(Note.HowMany()) with timeout 4000 is Result.Ok(reply) {\n\t\techo \"the other node answered: {reply}\"\n\t}\n}", CodePt: "type Note {\n\tSay     { text: Str }\n\tHowMany\n\tCounted { seen: Int }\n}\n\nproc inbox(seen: Int, note: Note, from: hive.syslink.Envelope): Int {\n\tif note is Note.Say(text) {\n\t\techo \"ouvi: {text}\"\n\t\treturn seen + 1\n\t} else if note is Note.HowMany {\n\t\thive.syslink.answer(from, Note.Counted(seen))\n\t\treturn seen\n\t}\n\treturn seen\n}\n\n// Rode este mesmo programa em duas máquinas: um nó é identificado por onde\n// ele está, então nada nos dois processos difere além dos endereços digitados\n// abaixo — 192.168.1.10:9100 e 192.168.1.11:9100, um de cada lado.\nproc main(): void {\n\techo \"O ip:port deste nó?\"\n\tme := hive.term.read()\n\techo \"O ip:port do outro nó?\"\n\tpeer := hive.term.read()\n\n\tif hive.syslink.listen(me) is Result.Error(err) {\n\t\tpanic err\n\t}\n\n\tbox := hive.syslink.spawn(inbox, 0)\n\thive.syslink.register(#Inbox, box)\n\n\tmine := hive.syslink.at(#Inbox)\n\ttheirs := hive.syslink.on(peer, #Inbox)\n\n\tasync theirs(Note.Say(\"olá de {me}\"))\n\thive.task.sleep(500)\n\techo \"peers conectados: {len(hive.syslink.peers())}\"\n\n\tif theirs(Note.HowMany()) with timeout 4000 is Result.Ok(reply) {\n\t\techo \"o outro nó respondeu: {reply}\"\n\t}\n}"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "What an error looks like", TitlePt: "Como um erro se parece", Explanation: "Every diagnostic opens with where it happened — file:line: message — because that is the shape editors expect, so wiring one up needs nothing more than a compile command and a pattern to read its output with. A message that runs on continues on indented lines, so one error stays one entry in a quickfix list rather than several. And a compile error is printed with nothing at all in front of it: prefixing it with a program name would put something before the file name and break the very pattern. Only a usage error — hive build with no entrypoint — says who is complaining, because that is not a diagnostic about anybody's source.\n\nEvery pass reports as much as it can find rather than stopping at the first thing, so a program with three mistakes is a program whose author is told about three.\n\nWhat a message should say is not part of the grammar, but it is the standard the compiler's own messages are held to. Name the thing: `v` is declared Str[3], never \"type mismatch\". Say what to do: a bounds error that cannot prove an index says how to guard it, and a sort that cannot sort in place names both fixes. Say why, when the why is the point — hive.math.max(0, health) is rejected because Hive never widens a number behind your back, and a message that only said \"expected Float, found Int\" would leave the reader hunting for a conversion they were supposed to have known about. And never guess: a miscased keyword is reported as a keyword written the wrong way and as a name that was already taken, because both readings are live and the compiler cannot know which was meant.\n\nTwo limits are worth knowing, and both are the price of one decision — that every pass after the loader sees one flat program. Errors from the passes after parsing land on a declaration rather than on a line inside it, so the declaration a mistake is in is as precise as they get: its line is what the message opens with, and its name is what the message says. And in a program with imports those same errors are reported against the entrypoint, even where the declaration at fault came from an imported module. Errors the lexer, the parser and the import resolver raise do name the right file and the right line, because those three run per file.\n\nOne diagnostic is not an error at all. A test failure is an answer rather than a malfunction, so that report goes to stdout, and only a compiler or toolchain failure goes to stderr.", ExplanationPt: "Todo diagnóstico começa dizendo onde aconteceu — arquivo:linha: mensagem — porque essa é a forma que editores esperam, então integrar um não precisa de mais que um comando de compilação e um padrão para ler a saída dele. Uma mensagem que se estende continua em linhas indentadas, então um erro continua sendo uma entrada só na lista de quickfix em vez de várias. E um erro de compilação é impresso sem absolutamente nada na frente: prefixá-lo com um nome de programa colocaria algo antes do nome do arquivo e quebraria justamente esse padrão. Só um erro de uso — hive build sem entrypoint — diz quem está reclamando, porque isso não é um diagnóstico sobre o código-fonte de ninguém.\n\nToda passagem reporta tudo que consegue encontrar em vez de parar na primeira coisa, então um programa com três erros é um programa cujo autor é avisado sobre os três.\n\nO que uma mensagem deveria dizer não faz parte da gramática, mas é o padrão a que as próprias mensagens do compilador são submetidas. Nomeie a coisa: `v` é declarado Str[3], nunca \"incompatibilidade de tipos\". Diga o que fazer: um erro de limites que não consegue provar um índice diz como colocar uma guarda nele, e um sort que não consegue ordenar no lugar nomeia as duas correções. Diga por quê, quando o porquê é o ponto — hive.math.max(0, health) é recusado porque Hive nunca amplia um número pelas suas costas, e uma mensagem que só dissesse \"esperava Float, encontrou Int\" deixaria quem lê caçando uma conversão que supostamente deveria conhecer. E nunca adivinhe: uma palavra-chave mal grafada é reportada como palavra-chave escrita do jeito errado e como um nome já tomado, porque as duas leituras estão vivas e o compilador não tem como saber qual foi a intenção.\n\nDois limites valem ser conhecidos, e os dois são o preço de uma decisão só — a de que toda passagem depois do carregador vê um programa achatado. Erros das passagens posteriores ao parsing chegam em uma declaração e não em uma linha dentro dela, então a declaração em que o erro está é o máximo de precisão que se consegue: a linha dela é o que a mensagem abre dizendo, e o nome dela é o que a mensagem cita. E em um programa com imports esses mesmos erros são reportados contra o entrypoint, mesmo quando a declaração culpada veio de um módulo importado. Os erros que o lexer, o parser e o resolvedor de imports levantam nomeiam o arquivo e a linha certos, porque esses três rodam por arquivo.\n\nUm diagnóstico não é um erro de forma alguma. A falha de um teste é uma resposta e não um defeito, então esse relatório vai para a stdout, e só uma falha do compilador ou da toolchain vai para a stderr.", Examples: []slides_0_Example{slides_0_Example{Caption: "Three mistakes, and three messages", CaptionPt: "Três erros, e três mensagens", Code: "proc main(): void {\n\tStr[3] parts = split(\"north,south\", \",\")\n\techo parts[0]\n\n\thealth := 3\n\techo hive.math.max(0, health)\n}", CodePt: "proc main(): void {\n\tStr[3] parts = split(\"north,south\", \",\")\n\techo parts[0]\n\n\thealth := 3\n\techo hive.math.max(0, health)\n}"}, slides_0_Example{Caption: "What hive check says about it", CaptionPt: "O que o hive check diz sobre isso", Code: (((((((((((((((((((((((((((("$ hive check colony.hive\ncolony.hive:1: in " + "`") + "main") + "`") + ": the declaration of ") + "`") + "parts") + "` ") + "wants Str[3] and this is Str[dyn]\ncolony.hive:1: in ") + "`") + "main") + "`") + ": the first argument of ") + "`") + "hive.math.max") + "` ") + "wants Float and this is Int\ncolony.hive:1: in ") + "`") + "main") + "`") + ": the second argument of ") + "`") + "hive.math.max") + "` ") + "wants Float and this is Int\n\n# All three land on line 1 — the line ") + "`") + "main") + "` ") + "is declared on. That is the\n# limit above: after the loader, a position is a declaration's."), CodePt: (((((((((((((((((((((((((((("$ hive check colony.hive\ncolony.hive:1: in " + "`") + "main") + "`") + ": the declaration of ") + "`") + "parts") + "` ") + "wants Str[3] and this is Str[dyn]\ncolony.hive:1: in ") + "`") + "main") + "`") + ": the first argument of ") + "`") + "hive.math.max") + "` ") + "wants Float and this is Int\ncolony.hive:1: in ") + "`") + "main") + "`") + ": the second argument of ") + "`") + "hive.math.max") + "` ") + "wants Float and this is Int\n\n# Os três caem na linha 1 — a linha em que ") + "`") + "main") + "` ") + "é declarado. É o limite\n# acima: depois do carregador, uma posição é a de uma declaração.")}, slides_0_Example{Caption: "A message that says what to do", CaptionPt: "Uma mensagem que diz o que fazer", Code: (((((((((((("$ hive check guard.hive\nguard.hive:1: in " + "`") + "main") + "`") + ": cannot prove this index is in range: it is a computed expression, and there is nothing to hang a guard on. Bind it to a name first, then guard that\n\n$ hive check shouted.hive\nshouted.hive:5: ") + "`") + "Proc") + "` ") + "is a keyword written the wrong way: every keyword in Hive is lower case, and only lower case. Write ") + "`") + "proc") + "` ") + "— or, if a name was meant, one the language has not already taken."), CodePt: (((((((((((("$ hive check guard.hive\nguard.hive:1: in " + "`") + "main") + "`") + ": cannot prove this index is in range: it is a computed expression, and there is nothing to hang a guard on. Bind it to a name first, then guard that\n\n$ hive check shouted.hive\nshouted.hive:5: ") + "`") + "Proc") + "` ") + "is a keyword written the wrong way: every keyword in Hive is lower case, and only lower case. Write ") + "`") + "proc") + "` ") + "— or, if a name was meant, one the language has not already taken.")}, slides_0_Example{Caption: "Wiring up an editor", CaptionPt: "Integrando um editor", Code: "\" In Vim or Neovim. hive check runs every compiler pass and stops before\n\" the Go toolchain, so it answers as fast as the front end does and writes\n\" nothing next to the file — which is what makes it usable on every save.\nsetlocal makeprg=hive\\ check\\ %:S\nsetlocal errorformat=%-G\\ %.%#,%f:%l:\\ %m,%-G%.%#\n\n\" :make fills the quickfix list, and :cn walks the errors. The two %-G\n\" items throw away what is not a diagnostic: the first drops the indented\n\" continuation of a message that runs on, the last drops everything else.", CodePt: "\" No Vim ou Neovim. hive check roda todas as passagens do compilador e para\n\" antes da toolchain do Go, então responde tão rápido quanto o front end e\n\" não escreve nada ao lado do arquivo — que é o que o torna usável a cada save.\nsetlocal makeprg=hive\\ check\\ %:S\nsetlocal errorformat=%-G\\ %.%#,%f:%l:\\ %m,%-G%.%#\n\n\" :make preenche a lista de quickfix, e :cn percorre os erros. Os dois itens\n\" %-G descartam o que não é diagnóstico: o primeiro joga fora a continuação\n\" indentada de uma mensagem que se estende, o último joga fora o resto."}}, Download: false, Shot: ""}, slides_0_Slide{Title: "Building for another platform", TitlePt: "Construindo para outra plataforma", Explanation: "Because the last step is the Go toolchain, a Hive program is built for another platform the way any Go program is — by telling the toolchain which one. hive build takes a --target, and nothing else does.\n\nA target is a goos/goarch pair, and the pair is checked before anything is compiled. `go tool dist list` names every one the toolchain knows, and one it does not name is refused there and then rather than ten seconds later, by a build failing in a language you did not write. The list is asked for rather than kept anywhere, so a platform Go learns tomorrow needs no change here. And a platform that does not exist is a mistake in what was typed, so it is reported as one — beside the other command-line mistakes, rather than against a line of a program that has nothing wrong with it.\n\nWhat comes out says what it is for. A host build of main.hive is main, and a cross build is main-linux-arm64: two files that cannot run in the same places must not answer to one name, and a cross build that quietly replaced the local one would only be found out by running it. The .exe follows the target rather than the machine, so --target windows/amd64 writes main-windows-amd64.exe from anywhere.\n\nCGO_ENABLED=0 goes with a cross build and only with one. Everything the runtime is made of is Go, so no Hive program needs a C compiler — and a C cross-compiler is the one thing a machine with the Go toolchain on it cannot be assumed to have. A build for the machine it is running on is given no environment at all, and is therefore byte for byte the build it always was.\n\n--target belongs to the two commands that write something for a platform, build and export. check and emit compile nothing; test and container are both about this machine; and each of the four refuses the flag rather than accepting it and quietly ignoring it. run is the exception that proves the rule: everything after the entrypoint there belongs to the program being run, so a --target written after it is passed straight through, unread.", ExplanationPt: "Como o último passo é a toolchain do Go, um programa Hive é construído para outra plataforma do jeito que qualquer programa Go é — dizendo à toolchain qual é ela. hive build recebe um --target, e nada mais recebe.\n\nUm target é um par goos/goarch, e o par é verificado antes de qualquer coisa ser compilada. `go tool dist list` nomeia toda plataforma que a toolchain conhece, e uma que ele não nomeia é recusada ali mesmo em vez de dez segundos depois, por um build falhando em uma linguagem que você não escreveu. A lista é perguntada em vez de guardada em algum lugar, então uma plataforma que o Go aprender amanhã não exige mudança nenhuma aqui. E uma plataforma que não existe é um erro no que foi digitado, então é reportada como tal — ao lado dos outros erros de linha de comando, e não contra uma linha de um programa que não tem nada de errado.\n\nO que sai diz para que serve. Um build local de main.hive é main, e um build cruzado é main-linux-arm64: dois arquivos que não podem rodar nos mesmos lugares não podem atender pelo mesmo nome, e um build cruzado que substituísse silenciosamente o local só seria descoberto ao rodá-lo. O .exe segue o target e não a máquina, então --target windows/amd64 escreve main-windows-amd64.exe de qualquer lugar.\n\nCGO_ENABLED=0 acompanha um build cruzado e somente um. Tudo de que o runtime é feito é Go, então nenhum programa Hive precisa de um compilador C — e um compilador C cruzado é a única coisa que não se pode presumir em uma máquina que tem a toolchain do Go. Um build para a própria máquina em que roda não recebe ambiente nenhum, e é portanto byte a byte o build que sempre foi.\n\n--target pertence aos dois comandos que escrevem algo para uma plataforma, build e export. check e emit não compilam nada; test e container são sobre esta máquina; e cada um dos quatro recusa a flag em vez de aceitá-la e ignorá-la silenciosamente. run é a exceção que confirma a regra: tudo depois do entrypoint ali pertence ao programa que está sendo rodado, então um --target escrito depois dele é repassado direto, sem ser lido.", Examples: []slides_0_Example{slides_0_Example{Caption: "One machine, three platforms", CaptionPt: "Uma máquina, três plataformas", Code: "$ hive build main.hive\nCompiled main.hive -> main (1s)\n\n$ hive build main.hive --target linux/arm64\nCompiled main.hive -> main-linux-arm64 (2s)\n\n$ hive build main.hive --target=windows/amd64\nCompiled main.hive -> main-windows-amd64.exe (2s)", CodePt: "$ hive build main.hive\nCompiled main.hive -> main (1s)\n\n$ hive build main.hive --target linux/arm64\nCompiled main.hive -> main-linux-arm64 (2s)\n\n$ hive build main.hive --target=windows/amd64\nCompiled main.hive -> main-windows-amd64.exe (2s)"}, slides_0_Example{Caption: "The three ways it says no", CaptionPt: "As três formas de dizer não", Code: (((((((((((((((((((((((((((((((((((((((((((((((((((("$ hive build main.hive --target linux/sparc\nhive: " + "`") + "--target linux/sparc") + "` ") + "is not a platform the Go toolchain builds for. A target is a ") + "`") + "goos/goarch") + "` ") + "pair — ") + "`") + "linux/arm64") + "`") + ", ") + "`") + "darwin/arm64") + "`") + ", ") + "`") + "windows/amd64") + "` ") + "— and ") + "`") + "go tool dist list") + "` ") + "names every one it knows.\n\n$ hive build main.hive --target\nhive: ") + "`") + "--target") + "` ") + "was given no platform to build for. It takes a ") + "`") + "goos/goarch") + "` ") + "pair — ") + "`") + "hive build <entrypoint.hive> --target linux/arm64") + "`") + ".\n\n$ hive check main.hive --target linux/arm64\nhive: ") + "`") + "--target") + "` ") + "is a flag of ") + "`") + "hive build") + "` ") + "and ") + "`") + "hive export") + "`") + ". ") + "`") + "check") + "` ") + "builds nothing for another platform."), CodePt: (((((((((((((((((((((((((((((((((((((((((((((((((((("$ hive build main.hive --target linux/sparc\nhive: " + "`") + "--target linux/sparc") + "` ") + "is not a platform the Go toolchain builds for. A target is a ") + "`") + "goos/goarch") + "` ") + "pair — ") + "`") + "linux/arm64") + "`") + ", ") + "`") + "darwin/arm64") + "`") + ", ") + "`") + "windows/amd64") + "` ") + "— and ") + "`") + "go tool dist list") + "` ") + "names every one it knows.\n\n$ hive build main.hive --target\nhive: ") + "`") + "--target") + "` ") + "was given no platform to build for. It takes a ") + "`") + "goos/goarch") + "` ") + "pair — ") + "`") + "hive build <entrypoint.hive> --target linux/arm64") + "`") + ".\n\n$ hive check main.hive --target linux/arm64\nhive: ") + "`") + "--target") + "` ") + "is a flag of ") + "`") + "hive build") + "` ") + "and ") + "`") + "hive export") + "`") + ". ") + "`") + "check") + "` ") + "builds nothing for another platform.")}}, Download: false, Shot: ""}, slides_0_Slide{Title: "Packaging an app", TitlePt: "Empacotando um app", Explanation: "\n`hive build` writes a program. `hive export` writes an app: `hive export main.hive --target android/arm64` produces main-android-arm64.apk, installable as it stands.nThe window needs nothing new. A hive.ui.window already serves its own page over loopback with every asset embedded, so what shows it on a handset is the WebView the platform already has. The program is never told it is on a phone: it serves the tree it would serve a browser, and the same view function answers both.nandroid/arm64, and no other Android target. It is the only one Go links without a C cross-compiler — the one thing a machine with the Go toolchain on it cannot be assumed to have — so the other three are refused by name rather than failing ten seconds later in a language you did not write. It is also what a handset is.nA program with no window is refused rather than packaged. An app whose activity shows nothing has no way to be started and nothing to see, so export says so and stops; hive build is what compiles a program that serves or prints.nWhat the file holds: the program itself, a fixed WebView host, a binary manifest, and assets/icon.png as the launcher icon where the program ships one. The application id comes from the entrypoint, so chat.hive is hive.chat. Nothing but Go writes any of it — the manifest, the resource table, the archive and the signature over it are the build's own work, the same way the Windows resource object is. No Android SDK, and no network.nThe signature is APK Signature Scheme v2, under a key made on first use at ~/.hive/android.key. One stable key a machine over, so a later build installs over an earlier one instead of being refused as a different app.", ExplanationPt: "\n`hive build` escreve um programa. `hive export` escreve um app: `hive export main.hive --target android/arm64` produz main-android-arm64.apk, instalável do jeito que sai.nA janela não precisa de nada novo. Uma hive.ui.window já serve a própria página em loopback com todos os recursos embutidos, então o que a mostra em um aparelho é a WebView que a plataforma já tem. O programa nunca fica sabendo que está em um telefone: ele serve a mesma árvore que serviria a um navegador, e a mesma função view responde aos dois.nandroid/arm64, e nenhum outro target do Android. É o único que o Go linka sem um compilador C cruzado — a única coisa que não se pode presumir em uma máquina que tem a toolchain do Go — então os outros três são recusados pelo nome, em vez de falharem dez segundos depois em uma linguagem que você não escreveu. E é também o que um aparelho é.nUm programa sem janela é recusado em vez de empacotado. Um app cuja activity não mostra nada não tem como ser iniciado nem o que mostrar, então o export diz isso e para; hive build é o que compila um programa que serve ou imprime.nO que o arquivo carrega: o programa em si, um hospedeiro WebView fixo, um manifesto binário, e assets/icon.png como ícone do lançador quando o programa traz um. O id da aplicação vem do entrypoint, então chat.hive é hive.chat. Nada além do Go escreve qualquer parte disso — o manifesto, a tabela de recursos, o arquivo compactado e a assinatura sobre ele são trabalho do próprio build, do mesmo jeito que o objeto de recurso do Windows é. Nenhum SDK do Android, e nenhuma rede.nA assinatura é o APK Signature Scheme v2, sob uma chave criada no primeiro uso em ~/.hive/android.key. Uma chave estável por máquina, então um build posterior instala sobre um anterior em vez de ser recusado como um app diferente.", Examples: []slides_0_Example{slides_0_Example{Caption: "A program that is also an app", CaptionPt: "Um programa que também é um app", Code: "import hive.ui\n\ntype Msg {\n\tTapped\n}\n\nfunc view(count: Int): ui.View {\n\treturn ui.column([ui.pad(24), ui.gap(12), ui.align(ui.Align.Center())], [\n\t\tui.text([ui.size(ui.TextSize.Title())], \"Tapped {count} times\"),\n\t\tui.button([ui.on(Msg.Tapped()), ui.tone(ui.Tone.Good())], \"Tap\"),\n\t])\n}\n\nproc update(count: Int, msg: Msg, from: hive.syslink.Envelope): Int {\n\tif msg is Msg.Tapped {\n\t\treturn count + 1\n\t}\n\treturn count\n}\n\nproc main(): void {\n\tui.window(\"Taps\", view, update, 0)\n}", CodePt: "import hive.ui\n\ntype Msg {\n\tTapped\n}\n\nfunc view(count: Int): ui.View {\n\treturn ui.column([ui.pad(24), ui.gap(12), ui.align(ui.Align.Center())], [\n\t\tui.text([ui.size(ui.TextSize.Title())], \"Tocado {count} vezes\"),\n\t\tui.button([ui.on(Msg.Tapped()), ui.tone(ui.Tone.Good())], \"Toque\"),\n\t])\n}\n\nproc update(count: Int, msg: Msg, from: hive.syslink.Envelope): Int {\n\tif msg is Msg.Tapped {\n\t\treturn count + 1\n\t}\n\treturn count\n}\n\nproc main(): void {\n\tui.window(\"Toques\", view, update, 0)\n}"}, slides_0_Example{Caption: "Run it here, install it there", CaptionPt: "Rode aqui, instale lá", Code: "$ hive run taps.hive          # a window on this machine\n$ hive export taps.hive --target android/arm64\nPackaged taps.hive -> taps-android-arm64.apk (4s)\n\n$ adb install taps-android-arm64.apk\nSuccess", CodePt: "$ hive run taps.hive          # uma janela nesta máquina\n$ hive export taps.hive --target android/arm64\nPackaged taps.hive -> taps-android-arm64.apk (4s)\n\n$ adb install taps-android-arm64.apk\nSuccess"}, slides_0_Example{Caption: "The four ways it says no", CaptionPt: "As quatro formas de dizer não", Code: (((((((((((((((((((((((((((((((((((("$ hive export taps.hive\nhive: " + "`") + "hive export") + "`") + "needs a target, and the one it knows is ") + "`") + "--target android/arm64") + "`") + ".\n\n$ hive export taps.hive --target android/amd64\nhive: ") + "`") + "--target android/amd64") + "`") + "needs a C cross-compiler, which is the one thing a machine with the Go toolchain on it cannot be assumed to have. ") + "`") + "android/arm64") + "`") + " is the only Android target Go links on its own, and it is what a handset is.\n\n$ hive export taps.hive --target linux/arm64\nhive: ") + "`") + "hive export") + "`") + "knows one format, and it is Android's. It takes ") + "`") + "--target android/arm64") + "`") + "; ") + "`") + "linux/arm64") + "`") + " has no app to be packaged into.\n\n$ hive export server.hive --target android/arm64\nserver.hive:1: ") + "`") + "server.hive") + "`") + "opens no window, so there is nothing for an app to show and no way to start it. ") + "`") + "hive build") + "`") + "is what compiles a program that serves or prints."), CodePt: (((((((((((((((((((((((((((((((((((("$ hive export taps.hive\nhive: " + "`") + "hive export") + "`") + "needs a target, and the one it knows is ") + "`") + "--target android/arm64") + "`") + ".\n\n$ hive export taps.hive --target android/amd64\nhive: ") + "`") + "--target android/amd64") + "`") + "needs a C cross-compiler, which is the one thing a machine with the Go toolchain on it cannot be assumed to have. ") + "`") + "android/arm64") + "`") + " is the only Android target Go links on its own, and it is what a handset is.\n\n$ hive export taps.hive --target linux/arm64\nhive: ") + "`") + "hive export") + "`") + "knows one format, and it is Android's. It takes ") + "`") + "--target android/arm64") + "`") + "; ") + "`") + "linux/arm64") + "`") + " has no app to be packaged into.\n\n$ hive export server.hive --target android/arm64\nserver.hive:1: ") + "`") + "server.hive") + "`") + "opens no window, so there is nothing for an app to show and no way to start it. ") + "`") + "hive build") + "`") + "is what compiles a program that serves or prints.")}}, Download: false, Shot: ""}, slides_0_Slide{Title: "Containers", TitlePt: "Contêineres", Explanation: "`hive container entrypoint.hive` writes a Dockerfile for the program, into the folder the command was run in — which is also the build's context. Nothing has to be installed to build the image but Docker itself: the first stage downloads Go and the compiler's latest release for the platform being built for — amd64 on an ordinary machine, arm64 on an Apple-silicon one, so the build is native either way — and compiles the program with the two of them. The second stage is the executable and nothing else, on distroless/static: no Go, no compiler, not even a shell.\n\nThe parts of the file that are not a template are read off the program rather than guessed at. A hive.net.httpServe(8080, ...) becomes an EXPOSE 8080 that says in a comment where the number came from; an import that names a repository puts git in the build stage, since the compiler clones it while the image builds; a program that opens a database says why go mod tidy runs before anything compiles. Everything beside the Dockerfile goes into the build, so a .dockerignore is what narrows that.\n\nWhat comes out is an ordinary Dockerfile, and editing it is expected. A Dockerfile already in the folder is never written over — ours is called Dockerfile-hive-container instead, and the command says which of the two it wrote. This tour is served out of an image built exactly this way.", ExplanationPt: "`hive container entrypoint.hive` escreve um Dockerfile para o programa, na pasta em que o comando foi rodado — que é também o contexto do build. Nada precisa estar instalado para construir a imagem além do próprio Docker: o primeiro estágio baixa o Go e o último lançamento do compilador para a plataforma para a qual se está construindo — amd64 numa máquina comum, arm64 numa de silício Apple, então o build é nativo de qualquer jeito — e compila o programa com os dois. O segundo estágio é o executável e nada mais, sobre distroless/static: sem Go, sem compilador, nem sequer um shell.\n\nAs partes do arquivo que não são um template são lidas do programa em vez de adivinhadas. Um hive.net.httpServe(8080, ...) vira um EXPOSE 8080 que diz num comentário de onde o número veio; um import que nomeia um repositório coloca git no estágio de build, já que o compilador o clona enquanto a imagem é construída; um programa que abre um banco de dados diz por que go mod tidy roda antes de qualquer coisa ser compilada. Tudo que está ao lado do Dockerfile entra no build, então um .dockerignore é o que estreita isso.\n\nO que sai é um Dockerfile comum, e editá-lo é esperado. Um Dockerfile que já esteja na pasta nunca é sobrescrito — o nosso se chama Dockerfile-hive-container em vez disso, e o comando diz qual dos dois escreveu. Este tour é servido a partir de uma imagem construída exatamente assim.", Examples: []slides_0_Example{slides_0_Example{Caption: "Writing it, and running what it wrote", CaptionPt: "Escrevendo, e rodando o que foi escrito", Code: "$ hive container main.hive\nWrote Dockerfile for main.hive (0s)\n$ docker build -t main .\n$ docker run --rm -p 8080:8080 main", CodePt: "$ hive container main.hive\nWrote Dockerfile for main.hive (0s)\n$ docker build -t main .\n$ docker run --rm -p 8080:8080 main"}, slides_0_Example{Caption: "What it writes", CaptionPt: "O que ele escreve", Code: "# syntax=docker/dockerfile:1\n#\n# Builds and runs main.hive, from nothing but this folder.\n#\n#   docker build -t main .\n#   docker run --rm -p 8080:8080 main\n\n##############################################################################\n# Stage 1 — download Go and the compiler, then build\n##############################################################################\nFROM debian:bookworm-slim AS builder\n\n# What Docker is building for: amd64 on an ordinary machine, arm64 on an\n# Apple-silicon one or a Graviton.\nARG TARGETARCH\n\n# ... Go itself, then the compiler's latest release for that architecture ...\n\nWORKDIR /app\n\n# The whole folder, because a program is its entrypoint and whatever that\n# imported. A .dockerignore is what narrows this.\nCOPY . .\n\nRUN hivec build /app/main.hive \\\n && test -x /app/main\n\n##############################################################################\n# Stage 2 — runtime\n##############################################################################\nFROM gcr.io/distroless/static-debian12 AS runtime\nWORKDIR /app\nCOPY --from=builder /app/main /usr/local/bin/main\n\n# 8080 is where this program serves: hive.net.httpServe(8080, ...) says so.\nEXPOSE 8080\n\nENTRYPOINT [\"/usr/local/bin/main\"]", CodePt: "# syntax=docker/dockerfile:1\n#\n# Builds and runs main.hive, from nothing but this folder.\n#\n#   docker build -t main .\n#   docker run --rm -p 8080:8080 main\n\n##############################################################################\n# Stage 1 — download Go and the compiler, then build\n##############################################################################\nFROM debian:bookworm-slim AS builder\n\n# What Docker is building for: amd64 on an ordinary machine, arm64 on an\n# Apple-silicon one or a Graviton.\nARG TARGETARCH\n\n# ... Go itself, then the compiler's latest release for that architecture ...\n\nWORKDIR /app\n\n# The whole folder, because a program is its entrypoint and whatever that\n# imported. A .dockerignore is what narrows this.\nCOPY . .\n\nRUN hivec build /app/main.hive \\\n && test -x /app/main\n\n##############################################################################\n# Stage 2 — runtime\n##############################################################################\nFROM gcr.io/distroless/static-debian12 AS runtime\nWORKDIR /app\nCOPY --from=builder /app/main /usr/local/bin/main\n\n# 8080 is where this program serves: hive.net.httpServe(8080, ...) says so.\nEXPOSE 8080\n\nENTRYPOINT [\"/usr/local/bin/main\"]"}}, Download: false, Shot: ""}, slides_0_Slide{Title: "Where to go next", TitlePt: "Para onde ir agora", Explanation: "That is the tour. Everything in it runs on the same compiler that served it to you — one executable, and nothing else to install beside it. The download link below is the latest release, built for every platform Go targets; put it on your PATH and every example here is one file away from running for real.\n\nThe repository is the reference this tour is not. spec/ is the language written down properly, one numbered document per part — the grammar, the type system, the mutability rules, the bounds pass, the standard library, the lowering to Go — and it is where to look once the tour has told you a thing is true and you want to know exactly how true. examples/ holds whole programs rather than fragments, numbered and each one runnable, and they double as the compiler's own test suite, which is what keeps them honest.\n\nAnd the compiler is itself written in Hive and builds itself, so src/ is also the largest Hive program there is to read.", ExplanationPt: "Este é o tour. Tudo nele roda no mesmo compilador que o serviu a você — um único executável, e nada mais para instalar ao lado dele. O link de download abaixo é o último lançamento, construído para toda plataforma que o Go alcança; coloque-o no seu PATH e todo exemplo daqui está a um arquivo de rodar de verdade.\n\nO repositório é a referência que este tour não é. spec/ é a linguagem escrita direito, um documento numerado por parte — a gramática, o sistema de tipos, as regras de mutabilidade, a passagem de limites, a biblioteca padrão, a tradução para Go — e é onde procurar quando o tour lhe disse que algo é verdade e você quer saber exatamente o quanto. examples/ guarda programas inteiros em vez de fragmentos, numerados e cada um executável, e eles servem também como a suíte de testes do próprio compilador, que é o que os mantém honestos.\n\nE o compilador é ele mesmo escrito em Hive e se constrói sozinho, então src/ é também o maior programa Hive que existe para ler.", Examples: []slides_0_Example{slides_0_Example{Caption: "", CaptionPt: "", Code: "$ hive check hello.hive\nNo problems found in hello.hive (0s)\n\n$ hive run hello.hive\nit works\n\n$ hive test hello.hive\n  1 test: 1 passed\n  coverage: 100.0% of statements (3/3)\n\n$ hive container hello.hive\nWrote Dockerfile for hello.hive (0s)", CodePt: "$ hive check hello.hive\nNo problems found in hello.hive (0s)\n\n$ hive run hello.hive\nfunciona\n\n$ hive test hello.hive\n  1 test: 1 passed\n  coverage: 100.0% of statements (3/3)\n\n$ hive container hello.hive\nWrote Dockerfile for hello.hive (0s)"}}, Download: true, Shot: ""}}
	panic("hive: unreachable")
}

func main() {
	hive.InitAtoms([]string{"Nil"})
	var n int = 0
	_ = n
	for _, slide := range slides_0_all() {
		_ = slide
		n = (n + 1)
		hive.Echo("=== " + hive.ToStr(n) + " " + slide.Title + " ===")
		for _, example := range slide.Examples {
			_ = example
			hive.Echo("--- en: " + example.Caption)
			hive.Echo(example.Code)
			hive.Echo("--- pt: " + example.CaptionPt)
			hive.Echo(example.CodePt)
		}
	}
}
